/**
 * STUB — replaced by Plan 04-05 with the real product create/edit form.
 * Renders plain section content; the route wraps it in AdminLayout.
 */
export default function ProductForm() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">New product</h1>
      <p className="text-muted-foreground">
        Product form — built in 04-05.
      </p>
    </section>
  );
}
