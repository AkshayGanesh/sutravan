/**
 * STUB — replaced by Plan 04-08 with the real submissions inbox (read-only).
 * Renders plain section content; the route wraps it in AdminLayout.
 */
export default function Submissions() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Submissions</h1>
      <p className="text-muted-foreground">
        Customization submissions — built in 04-08.
      </p>
    </section>
  );
}
