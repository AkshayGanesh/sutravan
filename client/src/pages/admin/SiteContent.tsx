/**
 * STUB — replaced by Plan 04-07 with the real site-content editor (plain fields
 * + Our Story rich text). Renders plain section content; the route wraps it in
 * AdminLayout.
 */
export default function SiteContent() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Site content</h1>
      <p className="text-muted-foreground">
        Site content editing — built in 04-07.
      </p>
    </section>
  );
}
