/**
 * STUB — replaced by Plan 04-05 with the real product list (table, skeleton,
 * empty/error states, published toggle, delete). Renders plain section content;
 * the route wraps it in AdminLayout.
 */
export default function ProductsList() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Products</h1>
      <p className="text-muted-foreground">
        Product management — built in 04-05.
      </p>
    </section>
  );
}
