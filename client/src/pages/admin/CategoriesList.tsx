/**
 * STUB — replaced by Plan 04-06 with the real categories management surface.
 * Renders plain section content; the route wraps it in AdminLayout.
 */
export default function CategoriesList() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Categories</h1>
      <p className="text-muted-foreground">
        Categories management — built in 04-06.
      </p>
    </section>
  );
}
