---
phase: quick-260602-u0q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ProductDetail.tsx
autonomous: true
requirements: [QUICK-U0Q-01]

must_haves:
  truths:
    - "The 'Tell us your skin type' helper line in the product detail modal is a clickable, keyboard-focusable link"
    - "Clicking the link navigates to the /questionnaire SPA route (Skin Guide page)"
    - "Clicking the link closes the open product detail modal via the existing onClose() prop"
    - "The link uses Wouter internal routing (not a hardcoded absolute URL), so it respects the GitHub Pages base path in both dev and production"
    - "The helper line keeps its muted styling but is visibly distinguishable as a link"
  artifacts:
    - path: "client/src/components/ProductDetail.tsx"
      provides: "Product detail modal with the skin-type helper line rendered as a Wouter Link to /questionnaire"
      contains: "from \"wouter\""
  key_links:
    - from: "client/src/components/ProductDetail.tsx"
      to: "/questionnaire"
      via: "Wouter Link href + onClick calling onClose()"
      pattern: "href=\"/questionnaire\""
---

<objective>
In the product detail modal (`ProductDetail.tsx`), turn the static helper paragraph
"Tell us your skin type — we'll customize the formulation for you." into a link to
the Skin Guide questionnaire page (`/questionnaire`).

Purpose: Give shoppers a one-click path from a product they're considering straight
into the customization questionnaire, where they can request a custom formulation.

Output: An updated `ProductDetail.tsx` where the helper line is a Wouter `Link`
that navigates to `/questionnaire` and closes the modal on click.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The file being edited (note ~line 262-265, inside a Radix Dialog; props: product, open, onClose)
@client/src/components/ProductDetail.tsx

# The pattern to mirror: Wouter Link import (line 1), internal href="/questionnaire"
# (navLinks line 24, label "Skin Guide"), className passed directly, and onClick
# used to run cleanup on navigation (mobile menu: onClick={() => setOpen(false)} — the
# exact analog of needing onClose() here)
@client/src/components/Navbar.tsx

# The route definition: <Route path="/questionnaire" component={Questionnaire} />.
# Base path is handled centrally by <WouterRouter base={import.meta.env.BASE_URL...}>,
# so an internal href="/questionnaire" is correct — do NOT hardcode https://sutravan.in/...
@client/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link the skin-type helper line to the questionnaire and close the modal on click</name>
  <files>client/src/components/ProductDetail.tsx</files>
  <action>
Import `Link` from "wouter" at the top of `client/src/components/ProductDetail.tsx`
(add `import { Link } from "wouter";` — mirror the exact import Navbar uses on line 1;
do NOT import `useLocation`, it is not needed here).

Replace the static helper paragraph currently at ~lines 262-265:
the `<p className="text-center text-xs text-foreground/50 mt-3">` containing
"Tell us your skin type — we'll customize the formulation for you."

Render the whole helper line as a Wouter `Link` to the INTERNAL route `/questionnaire`
(NOT a hardcoded absolute `https://sutravan.in/...` URL — the App-level
`<WouterRouter base={import.meta.env.BASE_URL...}>` already applies the GitHub Pages
base path, exactly as it does for the Navbar "Skin Guide" link). Mirror the Navbar's
Link usage: pass `href="/questionnaire"` and `className` directly to `Link` (the Wouter
Link renders its own anchor, so it is a real, keyboard-focusable link — do not wrap it
in an extra `<a>`).

On the Link add `onClick={onClose}` so navigating to the questionnaire also closes the
open product detail modal — Wouter navigation does NOT auto-close the Radix Dialog. This
is the direct analog of the Navbar mobile menu's `onClick={() => setOpen(false)}` cleanup
on navigation.

Preserve the existing muted helper styling and make it clearly a link: keep
`block text-center text-xs text-foreground/50 mt-3` (use `block` so the centered line
keeps its full-width centering as an anchor) and append the codebase's standard link
affordance `underline underline-offset-2 hover:text-secondary transition-colors`
(this matches the `hover:text-secondary transition-colors` convention used throughout
Navbar.tsx). Keep the existing text and HTML entities exactly:
"Tell us your skin type &mdash; we&rsquo;ll customize the formulation for you."

Do NOT change `ProductDetailProps`, the `onClose` signature, the `Dialog`/`DialogContent`
wrappers, or any other part of the component. This is a single, presentational edit.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>grep -q 'from "wouter"' client/src/components/ProductDetail.tsx && grep -q 'href="/questionnaire"' client/src/components/ProductDetail.tsx && grep -q 'onClick={onClose}' client/src/components/ProductDetail.tsx && echo OK</automated>
    <automated>! grep -q 'sutravan.in/questionnaire' client/src/components/ProductDetail.tsx && echo "no-hardcoded-absolute-url"</automated>
  </verify>
  <done>
`npm run check` passes (tsc strict, no new type errors). The skin-type helper line in
`ProductDetail.tsx` is a Wouter `Link` with `href="/questionnaire"` and `onClick={onClose}`,
keeps its muted centered styling plus a visible underline/hover affordance, and uses the
internal route (no hardcoded `https://sutravan.in/...`).
  </done>
</task>

</tasks>

<verification>
- `npm run check` passes with no new TypeScript errors.
- Manual (optional sanity, not gating): open any product modal on `/shop`, click the
  helper line — the app navigates to `/questionnaire` and the modal is closed. The same
  works under the deployed GitHub Pages base path because routing is internal via Wouter.
</verification>

<success_criteria>
- The "Tell us your skin type…" helper line in the product detail modal is a real,
  keyboard-focusable link to the `/questionnaire` SPA route.
- Clicking it navigates internally (Wouter `Link`, no hardcoded absolute URL) AND closes
  the modal via `onClose()`.
- The line retains its muted helper styling while being visibly a link.
- `npm run check` passes.
</success_criteria>

<output>
Create `.planning/quick/260602-u0q-link-the-tell-us-your-skin-type-note-in-/260602-u0q-SUMMARY.md` when done.
</output>
