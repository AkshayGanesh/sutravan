---
phase: quick-260706-cl1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/siteContent.ts
  - client/src/pages/Questionnaire.tsx
  - client/src/pages/admin/SiteContent.tsx
autonomous: false
requirements:
  - QUICK-260706-cl1
must_haves:
  truths:
    - "Admin sees a 'Skin Guide' section on /admin/content with 5 fields (title, subtitle, caveat, thank-you title, thank-you body), prefilled with current values"
    - "Saving the Skin Guide fields upserts site_content and the public /questionnaire intro header/subtext/caveat reflect the change live, no redeploy"
    - "The post-submission thank-you title and body on /questionnaire reflect the admin-edited values"
    - "Before any row is saved (or while the query loads / on error), /questionnaire and the admin form render the current hardcoded defaults — never blank (D-20 fallback)"
    - "The conditional login/account CTA under the thank-you message stays code-managed (NOT editable)"
  artifacts:
    - path: "client/src/lib/siteContent.ts"
      provides: "5 new questionnaire_* keys in SITE_CONTENT_DEFAULTS, caveat default imported from copy.ts"
      contains: "questionnaire_title"
    - path: "client/src/pages/Questionnaire.tsx"
      provides: "Intro + QuestionnaireForm consume the 5 keys via useSiteContent() with SITE_CONTENT_DEFAULTS fallback"
      contains: "useSiteContent"
    - path: "client/src/pages/admin/SiteContent.tsx"
      provides: "Skin Guide fieldset (5 fields) wired into the existing form schema, defaults, reset() prefill and single Save"
      contains: "questionnaire_title"
  key_links:
    - from: "client/src/pages/Questionnaire.tsx"
      to: "SITE_CONTENT_DEFAULTS"
      via: "useSiteContent() read with ?? fallback"
      pattern: "questionnaire_(title|subtitle|caveat|thankyou_title|thankyou_body)"
    - from: "client/src/pages/admin/SiteContent.tsx"
      to: "useSaveSiteContent"
      via: "onSubmit upsert of questionnaire_* keys -> ['siteContent'] invalidation"
      pattern: "questionnaire_thankyou_(title|body)"
    - from: "client/src/lib/siteContent.ts"
      to: "CUSTOMIZATION_PRICING_CAVEAT"
      via: "import from ./copy as the caveat default"
      pattern: "CUSTOMIZATION_PRICING_CAVEAT"
---

<objective>
Make the Skin Guide (/questionnaire) intro header, subtext and pricing caveat, plus the post-submission thank-you title and message, admin-editable through the existing Site Content page — reusing the `site_content` key/value store and the D-20 mandatory-fallback pattern. No new DB table, no migration, no new admin route, no new npm dependency.

Purpose: The owner can retune the Skin Guide's framing copy and post-submit confirmation wording without a code change or redeploy — extending the core "owner self-manages content" value to the questionnaire surface.

Output: 5 new `site_content` keys wired through defaults → public consumers → admin editor.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@client/src/lib/siteContent.ts
@client/src/lib/copy.ts
@client/src/pages/Questionnaire.tsx
@client/src/pages/admin/SiteContent.tsx
@client/src/lib/admin.ts

# Key facts verified while planning (do NOT re-derive):
# - Intro() is a standalone component at Questionnaire.tsx:55-73 (h1 title 60-62,
#   p subtitle 64-67, p caveat 68-70). It is a component -> calling useSiteContent()
#   INSIDE it is valid and cleaner than prop-drilling. It renders in ALL states
#   (loading/error/empty/form) via <Intro /> at line 99; the ?? fallback covers the
#   pre-load flash so no state ever renders blank.
# - The thank-you block is Questionnaire.tsx:312-333 inside QuestionnaireForm
#   (h2 title 315-317, p body 318-320). That component already uses hooks.
# - CUSTOMIZATION_PRICING_CAVEAT (copy.ts:7) is used in THREE places: the Intro
#   caveat (69), the Review-step caveat (448) AND ProductDetail.tsx:146. It is NOT
#   dead — ProductDetail keeps reading it from copy.ts. Leave copy.ts intact; import
#   it into siteContent.ts as the caveat default (one source of truth).
# - useSaveSiteContent (admin.ts:793) upserts ARBITRARY key/value rows and
#   invalidates ['siteContent'] — NO admin.ts change needed.
# - Existing keys were seeded in migrations (0006/0014) but D-20 makes seeding
#   optional; defaults fallback covers pre-save. This task adds NO migration/seed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 questionnaire keys to defaults, consume them on /questionnaire, and add the admin Skin Guide fieldset</name>
  <files>client/src/lib/siteContent.ts, client/src/pages/Questionnaire.tsx, client/src/pages/admin/SiteContent.tsx</files>
  <action>
Do these three edits in order (defaults first so the consumers type-check against them).

STEP 1 — client/src/lib/siteContent.ts (define the contract):
Import CUSTOMIZATION_PRICING_CAVEAT from "./copy" at the top. Add 5 keys to the SITE_CONTENT_DEFAULTS object (append a "Questionnaire / Skin Guide" comment group after the delivery keys):
- questionnaire_title: "Customize your blend"
- questionnaire_subtitle: "Tell us about your skin and what you're looking for — we craft each batch by hand to suit you." (single line, the JSX line-break in Intro is just source wrapping — no newline in the value; keep the em dash — verbatim)
- questionnaire_caveat: CUSTOMIZATION_PRICING_CAVEAT (reuse the imported constant, do NOT re-type the emoji string — one source of truth)
- questionnaire_thankyou_title: "Thank you — your request is in." (em dash verbatim)
- questionnaire_thankyou_body: "We'll review your customization request and get back to you by email."
Keep the snake_case naming consistent with hero_title / our_story_body.

STEP 2 — client/src/pages/Questionnaire.tsx (consume with mandatory fallback, D-20):
Add `import { useSiteContent, SITE_CONTENT_DEFAULTS } from "@/lib/siteContent";`.
In Intro(): call `const { data } = useSiteContent();` at the top of the component, then replace the three hardcoded strings — the h1 (60-62) with `data?.questionnaire_title ?? SITE_CONTENT_DEFAULTS.questionnaire_title`, the subtitle p (64-67) with `data?.questionnaire_subtitle ?? SITE_CONTENT_DEFAULTS.questionnaire_subtitle`, and the caveat p (68-70) with `data?.questionnaire_caveat ?? SITE_CONTENT_DEFAULTS.questionnaire_caveat` (drop the {CUSTOMIZATION_PRICING_CAVEAT} reference here). Keep all existing className/markup unchanged.
In QuestionnaireForm: add `const { data: content } = useSiteContent();` near the other hooks (use a distinct name from the existing `data` questions prop-derived variables — the component already has `data` in scope via useQuestionnaireQuestions in the PARENT; inside QuestionnaireForm the questions arrive as the `questions` prop, so `data` is free, but name it `content` to avoid confusion). Then:
  - Replace the thank-you h2 text (316) with `content?.questionnaire_thankyou_title ?? SITE_CONTENT_DEFAULTS.questionnaire_thankyou_title`.
  - Replace the thank-you body p text (319) with `content?.questionnaire_thankyou_body ?? SITE_CONTENT_DEFAULTS.questionnaire_thankyou_body`.
  - Replace the Review-step caveat text (448) with `content?.questionnaire_caveat ?? SITE_CONTENT_DEFAULTS.questionnaire_caveat` — so the admin-edited caveat stays coherent across the intro AND review step.
Leave the CUSTOMIZATION_PRICING_CAVEAT import in Questionnaire.tsx ONLY if still referenced after these edits; both its uses (69, 448) are being replaced, so remove the now-unused import at line 45 (tsc/unused-import — verify it is not referenced elsewhere in the file before removing). Do NOT touch the conditional isLoggedIn login/account CTA (321-331) — it stays code-managed per the locked scope.

STEP 3 — client/src/pages/admin/SiteContent.tsx (add the editor fields):
Extend the existing single form — do NOT add a new route or nav item.
  - Add 5 fields to `contentSchema`: questionnaire_title, questionnaire_thankyou_title (z.string().trim().min(1, "…") each), questionnaire_subtitle, questionnaire_caveat, questionnaire_thankyou_body (z.string().trim().min(1, "…") each — textareas). Use friendly messages e.g. "Enter the Skin Guide heading."
  - Add the 5 keys to the useForm defaultValues (from SITE_CONTENT_DEFAULTS).
  - Add the 5 keys to the reset() prefill inside the useEffect, each via valueFor(data, "…").
  - Add the 5 keys to the onSubmit save.mutate({...}) payload (useSaveSiteContent upserts them + invalidates ['siteContent']).
  - Add a new `<fieldset>` titled "Skin Guide" (mirror the Homepage-hero fieldset markup: legend + Label + Input/Textarea + inline errors block) with: title (Input), subtitle (Textarea rows={3}), caveat (Textarea rows={3}), thank-you title (Input), thank-you message (Textarea rows={3}). Place it after the "Contact & social" fieldset, before the Save button. Update the page intro <p> to mention the Skin Guide is now editable too (optional wording tweak).
`valueFor`'s key param is `keyof ContentValues`, which now includes the new keys — no signature change needed.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
`npm run check` (tsc) passes with zero errors. SITE_CONTENT_DEFAULTS holds all 5 questionnaire_* keys (caveat === CUSTOMIZATION_PRICING_CAVEAT via import). Questionnaire.tsx renders intro title/subtitle/caveat and thank-you title/body/review-caveat from useSiteContent() with SITE_CONTENT_DEFAULTS fallback, and the unused CUSTOMIZATION_PRICING_CAVEAT import is removed. Admin SiteContent.tsx has a "Skin Guide" fieldset with 5 fields wired into schema, defaults, reset() prefill and the single Save. The isLoggedIn CTA is untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The 5 new site_content keys flow end to end: admin edits them on /admin/content and the public /questionnaire intro header/subtext/caveat plus the post-submit thank-you title/message update live (upsert + ['siteContent'] invalidation, no redeploy). Defaults keep everything populated before any row is saved.</what-built>
  <how-to-verify>
1. Run `npm run dev:client` (or the running dev server) and open /questionnaire — confirm the intro title ("Customize your blend"), subtext and the caveat small-print render exactly as before (defaults, no blank flash).
2. Log in as the admin and open /admin/content — confirm a new "Skin Guide" section shows 5 fields prefilled with the current values (title, subtitle, caveat, thank-you title, thank-you message).
3. Change each of the 5 fields to a clearly different test value and click Save (expect the "Site content updated." toast).
4. Return to /questionnaire (no redeploy, no hard refresh needed beyond the cache invalidation) — confirm the intro title/subtext/caveat now show your edited values. Also confirm the Review-step caveat matches.
5. Complete and submit the questionnaire (or reach the thank-you step) — confirm the thank-you heading and message show your edited values, and the login/account CTA under it still appears based on login state (it is intentionally NOT editable).
6. Confirm nothing else regressed on the page (wizard steps, progress bar, Turnstile).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what did not match.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → site_content upsert | Admin-authored copy crosses into Postgres; already gated by the existing site_content admin-write RLS policy (same policy that gates hero_title). No new boundary. |
| anon browser → site_content read | Public read of site copy (already anon-readable — this is intentionally public marketing copy). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cl1-01 | Elevation | site_content upsert of questionnaire_* keys | mitigate | Reuse existing useSaveSiteContent → existing admin-only RLS write policy; keys are plain rows, no schema/policy change, non-admins cannot write. |
| T-cl1-02 | Tampering | Public copy rendered on /questionnaire | accept | Copy is public, non-sensitive brand text; the same DOM-text rendering already used for hero_title. No HTML injection surface (plain text nodes, not dangerouslySetInnerHTML). |
| T-cl1-03 | Denial (blank UI) | Missing/loading site_content row | mitigate | D-20 mandatory fallback: every consumer reads `data?.key ?? SITE_CONTENT_DEFAULTS.key`, so no key ever renders blank. |
| T-cl1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this task (constraint: no new npm dependency) — supply-chain surface unchanged. |
</threat_model>

<verification>
- `npm run check` passes (tsc, strict mode).
- All 5 `questionnaire_*` keys present in SITE_CONTENT_DEFAULTS; caveat default sourced from the imported CUSTOMIZATION_PRICING_CAVEAT.
- Every public consumer read pairs the live value with a `?? SITE_CONTENT_DEFAULTS.*` fallback (D-20).
- Admin "Skin Guide" fieldset wired into schema + defaults + reset() + Save; no new route, table, migration or admin.ts change.
- Human-verify checkpoint confirms the live admin→public round-trip with no redeploy.
</verification>

<success_criteria>
- Owner can edit the Skin Guide intro title, subtitle, pricing caveat, thank-you title and thank-you message on /admin/content in one Save.
- Edited values appear on the public /questionnaire (intro + review caveat + thank-you) live, without a redeploy.
- Nothing renders blank before a row is saved or while the query loads/errors (defaults hold).
- The conditional login/account CTA under the thank-you message remains code-managed and unchanged.
- `npm run check` is green.
</success_criteria>

<output>
Create `.planning/quick/260706-cl1-skin-guide-questionnaire-editable-header/260706-cl1-SUMMARY.md` when done.
</output>
