---
phase: 09-admin-delivery-settings-cod-rules
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified: []
autonomous: false
requirements: [DLVR-01, DLVR-02, DLVR-04]

must_haves:
  truths:
    - "The edited delivery-estimate edge function is deployed live to project ref wfbnrcnmpcqzeyjlfflv"
    - "From /admin/delivery, clicking Preview returns an estimate with NO Turnstile challenge and reflects the typed (unsaved) origin (SC1/D-07/D-08)"
    - "A token-less anon delivery-estimate call still returns captcha_failed (public path unchanged)"
    - "Editing COD/weight/lead then Save updates the customer estimator immediately — cache purged, no redeploy (SC2/SC3/SC5)"
    - "An empty free-shipping threshold shows no messaging; a set threshold surfaces static 'free over ₹X' (SC4)"
  artifacts: []
  key_links:
    - from: "deployed delivery-estimate function"
      to: "live customer estimator (product detail + navbar)"
      via: "saved site_content settings + purged cache → next lookup recomputes"
      pattern: "delivery-estimate"
---

<objective>
Deploy the plan 09-02 edge-function edits to the live Supabase project and run the
consolidated end-of-phase human UAT that proves SC1/SC2/SC3/SC5 live (the admin
preview loop, the cache-purge-on-save loop, and the unchanged public Turnstile
path). This is the BLOCKING-HUMAN gate: the executing agent has NO Supabase
credentials, so the owner runs the deploy and exercises the live loop. Preview and
purge cannot be verified until the function is live.

Purpose: Close the loop — make SC1/SC3/SC5 actually true against the live function.
Output: A deployed function + a signed-off UAT (SUMMARY records the outcome).
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-admin-delivery-settings-cod-rules/09-CONTEXT.md
@.planning/phases/09-admin-delivery-settings-cod-rules/09-VALIDATION.md
</context>

<artifacts_produced>
No new source symbols — this plan deploys plan 09-02's edited `delivery-estimate`
function and validates the full phase live. See plans 09-01/09-02/09-03 for the
symbol roster.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Deploy the edited delivery-estimate function live (owner runs — agent has no creds)</name>
  <files>supabase/functions/delivery-estimate/index.ts (deploy target — no edit here)</files>
  <read_first>
    - Memory `supabase-live-ops.md` — how to push edge-function changes to the live project (cached CLI auth; project ref wfbnrcnmpcqzeyjlfflv)
    - .planning/phases/09-admin-delivery-settings-cod-rules/09-RESEARCH.md § Runtime State Inventory (redeploy is the one live step) + Environment Availability (supabase CLI v2.102.0; DEVIATION: `functions deploy` rejects `--linked`, deploys to the linked project by default)
  </read_first>
  <what-built>Plans 09-02/09-03 edited `supabase/functions/delivery-estimate/index.ts` (admin branch + purge) and shipped the /admin/delivery page. The code is committed but NOT deployed — the live function still runs the pre-Phase-9 version.</what-built>
  <action>
    BLOCKING-HUMAN: the owner (who holds the Supabase creds) runs the deploy — the agent cannot. Present the exact steps and pause for
    the resume signal. Steps: (1) from the repo root run `supabase functions deploy delivery-estimate` (NO `--linked` flag — CLI v2.102.0
    deploys to the linked ref by default; see the Phase 5 deviation note in supabase-live-ops.md); (2) confirm the deploy reports success
    against ref `wfbnrcnmpcqzeyjlfflv`. No new secrets (`TURNSTILE_SECRET_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` already in the
    function env, unchanged). No migration to push — this phase edits the 5 existing site_content keys' VALUES via the admin UI only; it
    adds no new keys, tables, or migrations. Do NOT proceed to Task 2 until the deploy is confirmed.
  </action>
  <how-to-verify>
    1. `supabase functions deploy delivery-estimate` (no `--linked`) reports success against ref `wfbnrcnmpcqzeyjlfflv`.
    2. No secret/migration changes required (confirmed above).
  </how-to-verify>
  <acceptance_criteria>
    - `supabase functions deploy delivery-estimate` reports success against ref wfbnrcnmpcqzeyjlfflv (owner confirms).
    - No secret or migration changes were needed (values-only phase).
  </acceptance_criteria>
  <verify>
    <human-check>Owner confirms the deploy succeeded against ref wfbnrcnmpcqzeyjlfflv.</human-check>
  </verify>
  <resume-signal>Type "deployed" once `supabase functions deploy delivery-estimate` reports success, or paste any deploy error.</resume-signal>
  <done>The Phase-9 delivery-estimate function (admin branch + purge) is live on ref wfbnrcnmpcqzeyjlfflv.</done>
</task>

<task type="auto">
  <name>Task 2: End-of-phase live UAT sign-off (SC1/SC2/SC3/SC4/SC5 + public-path safety)</name>
  <files>.planning/phases/09-admin-delivery-settings-cod-rules/09-04-SUMMARY.md (records results)</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 9 (SC1–SC5)
    - .planning/phases/09-admin-delivery-settings-cod-rules/09-VALIDATION.md § Manual-Only Verifications (the exact live scenarios)
  </read_first>
  <action>
    Guide the owner through the live UAT after deploy. This is the consolidated end-of-phase human verification (human_verify_mode:end-of-phase).
    Record each result in the SUMMARY. Scenarios:
    (a) SC1 admin preview: at /admin/delivery, type a real origin (e.g. the owner's dispatch city) WITHOUT saving, enter a "Test against"
        destination, click Preview → an estimate returns with NO Turnstile challenge and the ₹/ETA reflect the TYPED origin (D-07/D-08).
    (b) Public path unchanged: a token-less anon call to delivery-estimate still returns `captcha_failed` (D-07 security).
    (c) SC1 origin gate: entering 000000 or a non-serviceable pincode shows ✗ and disables Save; a real serviceable pincode shows ✓ and enables Save (D-10).
    (d) SC5 cache purge: change the COD fee, Save, then re-estimate a route that was previously cached on the customer estimator → the new
        COD/fee appears immediately (not after the 24h TTL) — proves purge + no-redeploy (D-11).
    (e) SC2: change default weight + dispatch lead, Save, re-estimate → cost/ETA reflect the new values.
    (f) SC3: toggle COD off, Save, re-estimate → estimator shows COD unavailable; toggle back on → fee/cap retained (D-13).
    (g) SC4: set a free-shipping threshold, Save → static "free over ₹X" messaging appears; clear it, Save → messaging disappears (D-14/D-19).
  </action>
  <acceptance_criteria>
    - Each scenario (a)-(g) has a recorded pass/fail in the SUMMARY.
    - Any failure blocks phase close and routes back to 09-02 (function) or 09-03 (UI).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Deno edge function + live estimate loop have no in-repo harness (09-VALIDATION.md § Manual-Only); validated by the human-check below.</automated>
    <human-check>
      Confirm each scenario (a)-(g) passes live. Sign off: "Phase 9 SC1–SC5 verified live" or list failing scenarios.
    </human-check>
  </verify>
  <done>All seven live scenarios pass: admin preview skips Turnstile and honors the typed origin, the public path still requires Turnstile, and every settings edit (origin/weight/lead/COD/free-ship) appears in live estimates immediately after Save with no redeploy.</done>
</task>

</tasks>

<verification>
- Function deployed live to ref wfbnrcnmpcqzeyjlfflv.
- All UAT scenarios (a)-(g) signed off in the SUMMARY.
- No new migrations/keys/secrets introduced (confirmed — edits are values only).
</verification>

<success_criteria>
- SC1: admin sets/edits origin with validation + live preview (no Turnstile, honors typed origin).
- SC2: default weight + dispatch lead flow into live estimates.
- SC3: COD toggle/fee/cap reflected in the customer estimator.
- SC4: free-ship threshold surfaces static "free over ₹X".
- SC5: all settings ride site_content + cache purge → live with no redeploy.
- Public Turnstile path unchanged (token-less anon still captcha_failed).
</success_criteria>

<output>
Create `.planning/phases/09-admin-delivery-settings-cod-rules/09-04-SUMMARY.md` when done, recording the deploy confirmation and the pass/fail of each UAT scenario.
</output>
