---
phase: 09-admin-delivery-settings-cod-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/admin/deliverySchema.ts
  - client/src/pages/admin/deliverySchema.test.ts
  - client/src/lib/codRules.ts
  - client/src/lib/codRules.test.ts
  - client/src/lib/pincodes.ts
  - client/src/lib/pincodes.test.ts
  - client/src/lib/delivery.ts
  - client/src/lib/delivery.test.ts
  - client/src/lib/siteContent.ts
autonomous: true
requirements: [DLVR-01, DLVR-02, DLVR-04]

must_haves:
  truths:
    - "deliverySchema rejects a non-6-digit origin, the 000000 placeholder, decimals, negatives, and out-of-range weight (1-2000) / lead (0-14)"
    - "COD codec round-trips {enabled,fee,valueCap}; blank cap serializes to null; malformed JSON parses to COD off"
    - "checkServiceable returns {known,serviceable,label} from a single pincodes lookup"
    - "previewDelivery invokes delivery-estimate with {originPincode,destPincode} and NO token field"
    - "SITE_CONTENT_DEFAULTS carries all five delivery keys mirroring the 0014 seed"
  artifacts:
    - path: "client/src/pages/admin/deliverySchema.ts"
      provides: "deliverySchema (Zod D-15 bounds) + formatPreviewLine (SC1/D-06 string)"
      exports: ["deliverySchema", "formatPreviewLine"]
    - path: "client/src/lib/codRules.ts"
      provides: "parseCodRules / serializeCodRules JSON-in-text codec (D-09/D-14)"
      exports: ["parseCodRules", "serializeCodRules"]
    - path: "client/src/lib/pincodes.ts"
      provides: "checkServiceable serviceability lookup (D-09)"
      exports: ["checkServiceable"]
    - path: "client/src/lib/delivery.ts"
      provides: "previewDelivery admin/preview invoke wrapper (D-06/D-08)"
      exports: ["previewDelivery"]
    - path: "client/src/lib/siteContent.ts"
      provides: "5 delivery keys appended to SITE_CONTENT_DEFAULTS (D-03)"
      contains: "delivery_origin_pincode"
  key_links:
    - from: "client/src/lib/delivery.ts (previewDelivery)"
      to: "delivery-estimate edge function"
      via: "supabase.functions.invoke body {originPincode,destPincode} (no token)"
      pattern: "functions\\.invoke\\(\"delivery-estimate\""
    - from: "client/src/lib/pincodes.ts (checkServiceable)"
      to: "public.pincodes"
      via: "supabase.from('pincodes').eq('pincode', p).maybeSingle()"
      pattern: "from\\(\"pincodes\"\\)"
---

<objective>
Land the test-first, pure-logic foundation for the admin delivery settings page:
the Zod validation schema (D-15 bounds), the COD JSON-in-text codec (D-09/D-14),
the pincode serviceability lookup (D-09), the admin preview invoke wrapper
(D-06/D-08), and the five delivery keys in SITE_CONTENT_DEFAULTS (D-03). Every
piece here is a pure/isolated module unit-tested by vitest — this plan creates all
four Wave-0 test files the phase VALIDATION strategy requires so the UI plan
(09-03) consumes proven contracts, not scavenger-hunt guesses.

Purpose: Establish the deterministic validation + serialization contracts SC1/SC2/SC3/SC4
depend on, with automated coverage, before any UI or edge-function change.
Output: deliverySchema.ts, codRules.ts, pincodes.ts (+ tests), previewDelivery in
delivery.ts (+ extended delivery.test.ts), and the extended SITE_CONTENT_DEFAULTS.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-delivery-settings-cod-rules/09-CONTEXT.md
@.planning/phases/09-admin-delivery-settings-cod-rules/09-RESEARCH.md
@.planning/phases/09-admin-delivery-settings-cod-rules/09-PATTERNS.md
@.planning/phases/09-admin-delivery-settings-cod-rules/09-VALIDATION.md
</context>

<artifacts_produced>
Phase-wide symbol roster (this plan produces the ones marked ★):
- ★ `client/src/pages/admin/deliverySchema.ts` — `deliverySchema` (Zod), `formatPreviewLine`
- ★ `client/src/lib/codRules.ts` — `parseCodRules`, `serializeCodRules`
- ★ `client/src/lib/pincodes.ts` — `checkServiceable`
- ★ `client/src/lib/delivery.ts` — `previewDelivery` (new export alongside existing `estimateDelivery`)
- ★ `client/src/lib/siteContent.ts` — 5 new `SITE_CONTENT_DEFAULTS` keys (`delivery_origin_pincode`, `delivery_default_weight_g`, `delivery_dispatch_lead_days`, `delivery_cod_rules`, `delivery_free_ship_threshold`)
- `client/src/lib/admin.ts` — `useSaveDeliverySettings` (plan 09-03)
- `client/src/pages/admin/Delivery.tsx` (plan 09-03)
- `Delivery` NAV_ITEMS entry + `/admin/delivery` route (plan 09-03)
- `supabase/functions/delivery-estimate/index.ts` admin branch: `isAdmin` detection, `originPincode` override, `purge` branch (plan 09-02)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod delivery schema (D-15 bounds) + preview formatter (D-06)</name>
  <files>client/src/pages/admin/deliverySchema.ts, client/src/pages/admin/deliverySchema.test.ts</files>
  <read_first>
    - .planning/phases/09-admin-delivery-settings-cod-rules/09-RESEARCH.md § "Code Examples" (Zod schema encoding D-15) + § "Common Pitfalls" Pitfall 6 (coercion of numeric strings)
    - .planning/phases/09-admin-delivery-settings-cod-rules/09-CONTEXT.md D-15 (exact bounds), D-10 (000000 reject), D-06 (preview string)
    - client/src/pages/admin/SiteContent.tsx L25-39 (existing zod object idiom in this repo)
    - client/src/lib/format.ts (formatPrice — reuse for the ₹ figure, NEVER re-round)
    - client/src/components/delivery/DeliveryEstimate.tsx L217-221 ("Arrives in {min}–{max} working days" wording to mirror)
  </read_first>
  <behavior>
    - deliverySchema.parse rejects originPincode "12345" and "1234567" ("Enter a 6-digit pincode"); accepts "560001".
    - deliverySchema rejects originPincode "000000" with a distinct message ("Enter a real origin pincode") — D-10.
    - defaultWeightG: accepts 1 and 2000; rejects 0, 2001, -1, and "2.5" (decimals) — D-15.
    - dispatchLeadDays: accepts 0 and 14; rejects -1, 15, and "1.5" — D-15.
    - codFee: when codEnabled true, blank/NaN fee raises an issue on path ["codFee"] ("Enter a COD fee (₹0 or more)"); accepts 0; rejects -1 — D-15.
    - codValueCap: "" and null → null; "5000" → 5000; 0 and -1 rejected (must be > 0 or blank) — D-14/D-15.
    - freeShipThreshold: "" → null; "999" → 999; 0 rejected — D-14/D-15.
    - formatPreviewLine(origin,dest,{cost,etaDays,codAvailable}) returns exactly "From 560001 to 110001: ₹80, 3–5 working days · COD available" for a serviceable result (uses formatPrice for the ₹ figure); COD false → "· COD not available"; non-serviceable result → "From 560001 to 110001: not serviceable".
  </behavior>
  <action>
    Create `deliverySchema.ts` exporting `deliverySchema` (a `z.object`) and a pure `formatPreviewLine`.
    Encode D-15 verbatim: originPincode `z.string().regex(/^\d{6}$/, ...)` with a `.refine` rejecting "000000";
    defaultWeightG `z.coerce.number().int(...).min(1).max(2000)`; dispatchLeadDays `z.coerce.number().int(...).min(0).max(14)`;
    codEnabled `z.boolean()`; codFee `z.coerce.number().int().min(0)`; codValueCap and freeShipThreshold each
    `z.preprocess(emptyToNull, z.coerce.number().int().positive().nullable())` where `emptyToNull` maps "" / null → null (D-14);
    a `.superRefine` that adds an issue on ["codFee"] when `codEnabled` is true and codFee is null/NaN.
    Export `type DeliveryValues = z.infer<typeof deliverySchema>`.
    `formatPreviewLine(origin, dest, result)` builds the SC1/D-06 string using `formatPrice(result.cost)` for the
    rupee figure and `result.etaDays.min–result.etaDays.max working days`, appending "· COD available" / "· COD not available";
    for a `serviceable:false` result return the "not serviceable" variant. Use `import type` for the estimate result shape so no
    runtime supabase import is pulled into the test. Write `deliverySchema.test.ts` covering the behavior cases above FIRST (RED),
    then implement to GREEN. Reject decimals via explicit `.int(...)` (Pitfall 6).
  </action>
  <acceptance_criteria>
    - `npx vitest run client/src/pages/admin/deliverySchema.test.ts` exits 0 with all behavior cases green.
    - `grep -q "000000" client/src/pages/admin/deliverySchema.ts` (placeholder reject present).
    - `grep -q "formatPreviewLine" client/src/pages/admin/deliverySchema.ts` and it is exported.
    - `npm run check` passes (no tsc errors introduced).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run client/src/pages/admin/deliverySchema.test.ts && npm run check</automated>
  </verify>
  <done>deliverySchema enforces every D-15 bound (int/min/max, 000000 reject, blank→null, COD-fee-required-when-enabled) and formatPreviewLine renders the exact SC1 string, all pinned by passing vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: COD JSON codec (D-09/D-14) + pincode serviceability lookup (D-09)</name>
  <files>client/src/lib/codRules.ts, client/src/lib/codRules.test.ts, client/src/lib/pincodes.ts, client/src/lib/pincodes.test.ts</files>
  <read_first>
    - supabase/functions/delivery-estimate/index.ts L215-225 (canonical CodRules parse contract `{enabled,fee,valueCap}`) and L75 (`type CodRules`)
    - supabase/migrations/0014_delivery_settings_seed.sql L21 (seeded delivery_cod_rules string)
    - .planning/phases/09-admin-delivery-settings-cod-rules/09-RESEARCH.md § "Code Examples" (checkServiceable) + Pattern 2 (COD codec)
    - client/src/lib/siteContent.ts L36-47 (fetchSiteContent — the supabase.from().select() idiom to mirror)
    - client/src/lib/delivery.test.ts (existing vi.mock("@/lib/supabase") pattern for testing supabase-touching modules)
    - supabase/migrations/0015_pincodes.sql (pincodes public-read columns: pincode, district/state, serviceable)
  </read_first>
  <behavior>
    - parseCodRules('{"enabled":true,"fee":30,"valueCap":5000}') → {enabled:true, fee:30, valueCap:5000}.
    - parseCodRules('') and parseCodRules(undefined) and parseCodRules('not json') → {enabled:false, fee:0, valueCap:null} (malformed → off, mirrors function L221-224).
    - parseCodRules('{"enabled":true,"fee":30}') → valueCap null (missing cap tolerated).
    - serializeCodRules({enabled:true, fee:30, valueCap:5000}) → the string round-trips through parseCodRules to an equal object.
    - serializeCodRules({enabled:true, fee:30, valueCap:null}) keeps valueCap null in the JSON (D-14 blank cap → null).
    - serializeCodRules({enabled:false, fee:30, valueCap:5000}) → still serializes the retained fee/cap (D-13 retain), enabled:false.
    - checkServiceable("560001") on a mocked serviceable row → {known:true, serviceable:true, label:"<district-or-state>"}.
    - checkServiceable("999999") on a mocked null row → {known:false, serviceable:false, label:null}.
    - checkServiceable on a mocked serviceable:false row → {known:true, serviceable:false, label:...}.
  </behavior>
  <action>
    Create `codRules.ts` exporting pure `parseCodRules(raw: string | null | undefined)` and `serializeCodRules(v)`.
    parseCodRules JSON.parses inside try/catch, returning `{enabled:false, fee:0, valueCap:null}` on any failure or falsy input,
    else `{enabled: !!p.enabled, fee: p.fee ?? 0, valueCap: p.valueCap ?? null}` — identical to the function's L221-224 tolerance.
    serializeCodRules returns `JSON.stringify({enabled, fee, valueCap})` matching the canonical `{enabled,fee,valueCap}` contract.
    Create `pincodes.ts` exporting async `checkServiceable(pincode)`: a single `supabase.from("pincodes").select("pincode, district, state, serviceable").eq("pincode", pincode).maybeSingle()`,
    returning `{ known: !!data, serviceable: data?.serviceable === true, label: data ? (data.district ?? data.state) : null }` — no throw (absence = not serviceable, D-09).
    Write codRules.test.ts (pure, no mock) and pincodes.test.ts (`vi.mock("@/lib/supabase", ...)` returning a stubbed `from().select().eq().maybeSingle()` chain, per the delivery.test.ts precedent) FIRST (RED), then implement to GREEN.
  </action>
  <acceptance_criteria>
    - `npx vitest run client/src/lib/codRules.test.ts` exits 0 (round-trip, malformed→off, blank cap→null all green).
    - `npx vitest run client/src/lib/pincodes.test.ts` exits 0 (known/unknown/serviceable/label mapping green).
    - `grep -q "maybeSingle" client/src/lib/pincodes.ts` (single-row lookup) and `grep -q "pincodes" client/src/lib/pincodes.ts`.
    - `npm run check` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run client/src/lib/codRules.test.ts client/src/lib/pincodes.test.ts && npm run check</automated>
  </verify>
  <done>The COD JSON codec matches the edge function's canonical parse contract and checkServiceable maps the pincodes public-read lookup, both pinned by passing vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: previewDelivery invoke wrapper (D-06/D-08) + SITE_CONTENT_DEFAULTS delivery keys (D-03)</name>
  <files>client/src/lib/delivery.ts, client/src/lib/delivery.test.ts, client/src/lib/siteContent.ts</files>
  <read_first>
    - client/src/lib/delivery.ts L62-84 (estimateDelivery — the invoke + error-mapping block to mirror) and L23-52 (DeliveryEstimateResult, EstimateError, mapEstimateError)
    - client/src/lib/delivery.test.ts (existing test file to extend; keep the vi.mock hoist)
    - .planning/phases/09-admin-delivery-settings-cod-rules/09-RESEARCH.md Pattern 5 (previewDelivery) + Assumption A1 (auto-bearer)
    - client/src/lib/siteContent.ts L21-34 (SITE_CONTENT_DEFAULTS object to extend)
    - supabase/migrations/0014_delivery_settings_seed.sql (exact seed values to mirror in defaults)
  </read_first>
  <behavior>
    - previewDelivery("560001","110001") calls supabase.functions.invoke("delivery-estimate", { body }) where body has originPincode and destPincode and NO `token` key (assert `"token" in body` is false).
    - On a mocked `{ data: <result>, error: null }` → previewDelivery resolves the DeliveryEstimateResult unchanged.
    - On a mocked `{ error: { context: { json: () => ({error:"bad_request"}) } } }` → previewDelivery throws EstimateError with code "invalid-format" (reuses mapEstimateError).
    - On a mocked error with unreadable body → throws EstimateError code "retry".
    - SITE_CONTENT_DEFAULTS has delivery_origin_pincode "000000", delivery_default_weight_g "250", delivery_dispatch_lead_days "1", delivery_cod_rules '{"enabled":true,"fee":30,"valueCap":5000}', delivery_free_ship_threshold "".
  </behavior>
  <action>
    Add `previewDelivery(originPincode: string, destPincode: string)` to delivery.ts as a sibling of estimateDelivery.
    It calls `supabase.functions.invoke("delivery-estimate", { body: { originPincode, destPincode } })` — NO token field
    (the admin branch skips Turnstile per D-07; the session JWT is auto-attached, A1). Reuse the SAME `{ error }` →
    `error.context.json()` → `mapEstimateError` → `throw new EstimateError(...)` block from estimateDelivery (L69-82); return `data as DeliveryEstimateResult`.
    Leave estimateDelivery + useDeliveryEstimate UNCHANGED (public path). Then extend delivery.test.ts: upgrade the existing
    `vi.mock("@/lib/supabase")` to expose a controllable `functions.invoke` spy so the body-shape and error-mapping cases above are asserted;
    keep the existing mapEstimateError tests. Finally append the 5 keys to SITE_CONTENT_DEFAULTS in siteContent.ts (values mirroring 0014, D-03;
    free-ship "" for null/off per D-14/D-19). Write the new test cases RED first, then implement.
  </action>
  <acceptance_criteria>
    - `npx vitest run client/src/lib/delivery.test.ts` exits 0 including a case asserting the invoke body has no `token` key.
    - `grep -q "previewDelivery" client/src/lib/delivery.ts` and it is exported; existing `estimateDelivery` signature unchanged (`grep -q "export async function estimateDelivery" client/src/lib/delivery.ts`).
    - `grep -c "delivery_" client/src/lib/siteContent.ts` returns 5 (five delivery keys present; run against the SITE_CONTENT_DEFAULTS object).
    - `npm run check` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run client/src/lib/delivery.test.ts && npm run check</automated>
  </verify>
  <done>previewDelivery sends {originPincode,destPincode} with no token and reuses the estimate error mapping; the 5 delivery defaults are present, all pinned by vitest + tsc.</done>
</task>

</tasks>

<verification>
- `npm test` (full vitest suite) green — the three new test files + extended delivery.test.ts + all prior tests pass.
- `npm run check` clean (tsc).
- No change to estimateDelivery / useDeliveryEstimate (public estimator path untouched).
</verification>

<success_criteria>
- deliverySchema enforces D-15 (weight 1-2000 int, lead 0-14 int, COD fee ≥0 required-when-enabled, cap/threshold >0-or-blank→null, origin 6-digit + 000000 reject).
- COD codec round-trips the canonical `{enabled,fee,valueCap}` contract; malformed→off; blank cap→null.
- checkServiceable maps the pincodes public-read lookup to {known,serviceable,label}.
- previewDelivery invokes the function with {originPincode,destPincode} and no token.
- SITE_CONTENT_DEFAULTS carries all five delivery keys.
</success_criteria>

<output>
Create `.planning/phases/09-admin-delivery-settings-cod-rules/09-01-SUMMARY.md` when done.
</output>
