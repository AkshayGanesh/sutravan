---
phase: 03-authentication-roles
plan: 06
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/promote-admin.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
user_setup:
  - service: supabase
    why: "The bootstrap script needs the service-role key (out-of-band admin promotion) — never bundled, never VITE_-prefixed."
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL (reuse the value already in .env.seed.local)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret (reuse the value already in .env.seed.local); store in a gitignored .env.promote.local"
must_haves:
  truths:
    - "Running `node --env-file=.env.promote.local scripts/promote-admin.ts <email>` sets that user's profiles.role to 'admin' (success criterion #5, D-03)"
    - "The script is idempotent — a second run is a no-op (role already admin)"
    - "No UI/code path grants admin; the service-role key never reaches the client bundle (check-no-secret.sh PASS)"
  artifacts:
    - path: "scripts/promote-admin.ts"
      provides: "Out-of-band first-admin bootstrap (service-role, mirrors seed.ts)"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
  key_links:
    - from: "scripts/promote-admin.ts"
      to: "public.profiles"
      via: "service-role update role='admin' where email=<arg>"
      pattern: "role: 'admin'"
---

<objective>
Ship `scripts/promote-admin.ts` — the out-of-band first-admin bootstrap (success criterion #5, D-03). The owner registers normally (getting a `customer` row via the Plan 01 trigger), then runs this local script with the service-role key to flip that email's `profiles.role` to `'admin'`. There is NO self-serve UI/code path to admin. The script mirrors `scripts/seed.ts` exactly (native Node 22 `--env-file`, service-role from non-`VITE_` env, idempotent), and relies on Plan 01's role-lock trigger carve-out (`auth.uid()` null for the service-role caller) so the legitimate promotion is not blocked (Pitfall 4).

Purpose: Provide the only way an account becomes admin — a trusted local script — so the admin guard (Plan 04) has a real admin to gate for, without ever exposing an admin-granting path to clients.
Output: `scripts/promote-admin.ts`.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-authentication-roles/03-CONTEXT.md
@.planning/phases/03-authentication-roles/03-RESEARCH.md
@.planning/phases/03-authentication-roles/03-PATTERNS.md
@.planning/phases/03-authentication-roles/03-VALIDATION.md

# Depends on Plan 01's migration being live (role-lock trigger allows null-uid service-role promote):
@.planning/phases/03-authentication-roles/03-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's contribution)

- `scripts/promote-admin.ts` — service-role bootstrap script. Reads `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` from non-`VITE_` `process.env`; takes an email arg; idempotently sets `profiles.role='admin'` for that email. Run: `node --env-file=.env.promote.local scripts/promote-admin.ts <email>`.

<tasks>

<task type="auto">
  <name>Task 1: promote-admin.ts — idempotent service-role admin bootstrap (D-03)</name>
  <files>scripts/promote-admin.ts</files>
  <read_first>
    - scripts/seed.ts (the EXACT mirror: lines 1-16 JSDoc header with the SECURITY note + run command; lines 17-31 native Node imports + `createClient` + the `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` non-VITE_ env-guard with fail-fast; lines 115-124 the OK-log + `process.exit` + `main().catch(...)` wrapper)
    - scripts/check-no-secret.sh (the guard that proves `scripts/` is never bundled into `dist/` — the script must remain un-imported by client code)
    - .planning/phases/03-authentication-roles/03-RESEARCH.md (Code Examples lines 398-414 the promote body; Pitfall 4 lines 360-364 the role-lock trigger carve-out the script relies on)
    - .planning/phases/03-authentication-roles/03-PATTERNS.md (lines 68-88: promote-admin.ts = seed.ts mirror — imports/env-guard, header/run-doc, idempotent mutation + exit convention, gitignored .env.promote.local)
  </read_first>
  <action>
    Create `scripts/promote-admin.ts` mirroring `scripts/seed.ts`:
    HEADER: a JSDoc block stating the purpose (out-of-band first-admin bootstrap, D-03 / success criterion #5), the SECURITY note (reads the service-role key from non-`VITE_` `process.env` only; never imported by client code so it never reaches `dist/`, enforced by `scripts/check-no-secret.sh`), and the exact run command `node --env-file=.env.promote.local scripts/promote-admin.ts owner@example.com`.
    BODY: `import { createClient } from '@supabase/supabase-js'`. Read `const url = process.env.SUPABASE_URL;` and `const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY;` (NON-`VITE_`). Fail-fast: if either missing, `console.error('FAIL: set SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY'); process.exit(1);`. Read the email arg: `const email = process.argv[2]; if (!email) { console.error('FAIL: usage: promote-admin <email>'); process.exit(1); }`. Create `const admin = createClient(url, serviceKey, { auth: { persistSession: false } });`. Perform the idempotent mutation: `await admin.from('profiles').update({ role: 'admin' }).eq('email', email)`; on `error` throw `new Error(...)`; on success `console.log(\`OK: ${email} is now admin (idempotent).\`); process.exit(0);`. Wrap in a `main().catch(...)` that logs `FAIL:` and `process.exit(1)` (mirror seed.ts lines 121-124). The service-role caller has no JWT so `auth.uid()` is null -> Plan 01's `enforce_profile_role_lock` carve-out permits this update (Pitfall 4). Document that `.env.promote.local` is gitignored (root/supabase `.gitignore` already cover `.env*.local`).
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -5; grep -q "SUPABASE_SERVICE_ROLE_KEY" scripts/promote-admin.ts && grep -q "process.env.SUPABASE_URL" scripts/promote-admin.ts && grep -q "role: 'admin'\|role:'admin'" scripts/promote-admin.ts && grep -q "process.argv\[2\]" scripts/promote-admin.ts && ! grep -q "VITE_SUPABASE_SERVICE\|import.meta.env" scripts/promote-admin.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Reads `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` from non-`VITE_` `process.env`; fails fast if missing; NEVER uses a `VITE_`-prefixed var or `import.meta.env`.
    - Takes an email arg (`process.argv[2]`), fails fast with a usage message if absent.
    - Idempotently sets `role: 'admin'` on the profiles row matching the email (a re-run is a no-op).
    - Mirrors seed.ts structure (JSDoc header + run command, `createClient` with `persistSession:false`, OK/FAIL logging, `process.exit`, `main().catch` wrapper).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>promote-admin.ts is an idempotent service-role bootstrap mirroring seed.ts, with the email arg, fail-fast env guard, and no VITE_/client exposure.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the bootstrap end-to-end + secret hygiene (success criterion #5, D-03)</name>
  <files>scripts/promote-admin.ts</files>
  <read_first>
    - scripts/promote-admin.ts (the script just written)
    - scripts/check-no-secret.sh (the build-and-scan secret guard to run)
    - .env.seed.local (the existing gitignored env file holding SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY — copy/reuse its values into .env.promote.local)
    - .planning/phases/03-authentication-roles/03-VALIDATION.md (the D-03 manual check: run twice -> second is a no-op; check-no-secret.sh PASS)
  </read_first>
  <action>
    Create a gitignored `.env.promote.local` (reuse the `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` values from `.env.seed.local`). Register a throwaway test user (or use the owner's registered email from the Plan 03 register flow). Run `node --env-file=.env.promote.local scripts/promote-admin.ts <email>` -> expect `OK: <email> is now admin (idempotent).` and exit 0. Re-query (Supabase SQL editor): `select role from public.profiles where email='<email>'` -> `admin`. Run the script a SECOND time -> still exits 0 (idempotent no-op), role stays `admin` (this also proves the Plan 01 role-lock carve-out for null-uid service-role updates — Pitfall 4). Run `bash scripts/check-no-secret.sh` -> must PASS (no `service_role` token in `dist/`; the script is never imported by client code). Confirm `.env.promote.local` is NOT tracked by git (`git status --porcelain .env.promote.local` shows nothing / it is ignored).
  </action>
  <verify>
    <automated>bash scripts/check-no-secret.sh 2>&1 | tail -3; git check-ignore .env.promote.local >/dev/null 2>&1 && echo "env-file ignored OK" || echo "WARN: .env.promote.local not gitignored"</automated>
    <human-check>Manual (per VALIDATION.md, success criterion #5 / D-03, requires Plan 01 live + a registered user): run `node --env-file=.env.promote.local scripts/promote-admin.ts <email>` -> `OK` + exit 0; `select role from public.profiles where email='<email>'` returns `admin`; run it again -> still `OK`/no-op, role stays `admin`. Confirm there is NO UI or code path anywhere that grants admin. After promotion, the admin user can reach `/admin` (Plan 04 guard) and can write catalog data (RLS allows admins).</human-check>
  </verify>
  <acceptance_criteria>
    - The script promotes the target email to `admin` and exits 0; re-running is an idempotent no-op (role stays admin) — proving Plan 01's null-uid carve-out works.
    - `scripts/check-no-secret.sh` PASSES (no `service_role` in `dist/`).
    - `.env.promote.local` is gitignored (not tracked).
    - No UI/code path grants admin (success criterion #5 / D-03) — promotion only via this local service-role script.
  </acceptance_criteria>
  <done>The bootstrap promotes an email to admin idempotently, the secret guard passes, the env-file is gitignored, and admin is reachable ONLY via this out-of-band script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local operator (service-role key) -> public.profiles | Out-of-band trusted context; bypasses RLS but still fires the role-lock trigger. The service-role key must never leave the local machine / reach the bundle. |
| scripts/ -> dist/ bundle | The script must never be imported by client code, or the service-role key could be inlined by Vite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-17 | Information Disclosure | service-role key leaks into the public client bundle | mitigate | Key read from non-`VITE_` `process.env` only; script never imported by client code; `scripts/check-no-secret.sh` scans `dist/` for `service_role` and fails the build if found (mirrors seed.ts hygiene). |
| T-3-18 | Elevation of Privilege | a self-serve UI/code path grants admin | mitigate | Admin is granted ONLY by this local script (D-03/success #5); no UI ever sets role=admin; the trigger + RLS reject client role changes (T-3-01). |
| T-3-05 | Denial of Service | role-lock trigger blocks the legitimate service-role promote (Pitfall 4) | mitigate | Plan 01's `enforce_profile_role_lock` carve-out permits the change when `auth.uid()` is null (service-role); verified by the idempotent second run. |
| T-3-19 | Information Disclosure | `.env.promote.local` committed to git | mitigate | File is gitignored (root/supabase `.gitignore` cover `.env*.local`); verified via `git check-ignore`. |
| T-3-SC | Tampering | npm installs | accept | No new package installs — `@supabase/supabase-js` is pre-existing (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npm run check` exits 0; `scripts/check-no-secret.sh` PASSES; `.env.promote.local` is gitignored.
- grep gates confirm non-`VITE_` service-role usage, the email arg, the `role:'admin'` mutation, and no `import.meta.env`.
- Manual (VALIDATION.md, success #5 / D-03): promote an email -> role=admin; second run is a no-op; admin reaches /admin (Plan 04) and can write catalog (RLS); no UI path grants admin.
</verification>

<success_criteria>
- The first admin is bootstrapped out-of-band via a local service-role script (success criterion #5, D-03); no self-serve admin path exists.
- The script is idempotent and the service-role key never reaches the client bundle (check-no-secret PASS).
- Plan 01's role-lock carve-out lets the promote succeed (Pitfall 4), proven by the idempotent re-run.
</success_criteria>

<output>
Create `.planning/phases/03-authentication-roles/03-06-SUMMARY.md` when done. Record the exact run command, the idempotency + check-no-secret results, and confirmation that admin is reachable only via this script.
</output>
