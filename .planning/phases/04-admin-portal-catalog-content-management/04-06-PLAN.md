---
phase: 04-admin-portal-catalog-content-management
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - client/src/pages/admin/CategoriesList.tsx
autonomous: false
requirements: [ADMIN-04]
must_haves:
  truths:
    - "An admin can create a category (name + display order) and it appears in the public Shop category tabs in the chosen order"
    - "An admin can edit a category's name and display order"
    - "An admin can delete a category that has no products"
    - "Deleting a category that still has products is blocked with the friendly message 'This category has {N} products — move or delete them first.' (no orphaned products)"
    - "Category slug is auto-derived and hidden; there is no slug UI"
  artifacts:
    - path: "client/src/pages/admin/CategoriesList.tsx"
      provides: "Category list + create/edit form + in-use-protected delete (ADMIN-04, D-15/D-16)"
      min_lines: 70
  key_links:
    - from: "client/src/pages/admin/CategoriesList.tsx"
      to: "lib/admin.ts useAdminCategories + useUpsertCategory + useDeleteCategory"
      via: "list + form + delete hooks"
      pattern: "useAdminCategories|useUpsertCategory|useDeleteCategory"
    - from: "lib/admin.ts useDeleteCategory"
      to: "categories FK 23503"
      via: "friendly in-use error"
      pattern: "23503"
---

<objective>
Deliver the category-management vertical slice: an admin creates/edits/deletes categories (name + display order, slug auto-hidden), with in-use delete protection that surfaces a friendly message instead of orphaning products or exposing a raw FK error (ADMIN-04).

Purpose: Categories group the catalog and drive the Shop tabs. The FK constraint on products.category_id (Phase 1 D-04) is the real protection; this slice surfaces the friendly D-15 translation. Changes reflect on the live Shop via the lib/admin.ts cache invalidation from Plan 03.
Output: CategoriesList page (list + create/edit form + protected delete).
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-admin-portal-catalog-content-management/04-UI-SPEC.md
@.planning/phases/04-admin-portal-catalog-content-management/04-PATTERNS.md
@client/src/lib/admin.ts
</context>

<artifacts_this_phase_produces>
This plan replaces the Plan-04 stub page with a real implementation (no new files, no App.tsx edits):
- `client/src/pages/admin/CategoriesList.tsx` (real)
- Consumes: useAdminCategories/useUpsertCategory/useDeleteCategory/CategoryFormValues from lib/admin.ts; ConfirmDialog from components/admin
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CategoriesList — list + create/edit form + in-use-protected delete</name>
  <files>client/src/pages/admin/CategoriesList.tsx</files>
  <read_first>
    - client/src/lib/admin.ts (useAdminCategories — list ordered by sort_order; useUpsertCategory — name + sortOrder, slug auto via slugify on create; useDeleteCategory — FK 23503 → friendly message with the product count; the CategoryFormValues type — all created in Plan 03)
    - client/src/pages/Login.tsx (the RHF+Zod form pattern for the small create/edit category form)
    - client/src/components/ui/table.tsx, switch.tsx (not needed — no visibility), skeleton.tsx, empty.tsx, dialog.tsx or the inline form pattern
    - client/src/components/admin/ConfirmDialog.tsx (delete confirm — created in Plan 04)
    - .planning/phases/04-admin-portal-catalog-content-management/04-UI-SPEC.md (Tables states; Empty "No categories yet" → "+ Add category"; Primary CTA "+ Add category"; Destructive confirm "Delete this category?" body/labels verbatim; in-use delete copy "This category has {N} products — move or delete them first."; error/success toasts)
    - .planning/phases/04-admin-portal-catalog-content-management/04-CONTEXT.md (D-15 block-delete-when-in-use; D-16 editable fields = name + display order; slug auto/hidden; no description, no visibility field)
  </read_first>
  <action>
    Replace the Plan-04 CategoriesList stub. Render a shadcn Table (or list) of categories from useAdminCategories, ordered by sort_order, columns: name, display order (sort_order), product count (optional, if cheaply available), and edit/delete actions. Page header title "Categories" + primary CTA "+ Add category". A create/edit form (inline section or a Dialog) with two fields only: name (required) and display order (number, default next order); NO slug field (slug is auto-derived/hidden per D-16). Use RHF+Zod mirroring Login.tsx; submit calls useUpsertCategory().mutate (slug generated on create via slugify inside admin.ts; on edit the slug stays fixed). Delete uses ConfirmDialog (title "Delete this category?", body "This category will be removed from the Shop tabs. This can't be undone.", confirm "Delete category") → on confirm call useDeleteCategory().mutate(id). When the category is in use, the FK 23503 path in admin.ts throws the friendly "This category has {N} products — move or delete them first." message which surfaces as an error toast (and/or an inline notice) — the deletion does NOT proceed and no products are orphaned (D-15). States: loading → Skeleton rows; error → inline + Retry refetch(); empty → shadcn `empty` "No categories yet" / "Add a category so you can group your products." + the CTA. Default export CategoriesList.
  </action>
  <verify>
    <automated>npm run check && grep -q "useAdminCategories" client/src/pages/admin/CategoriesList.tsx && grep -q "useUpsertCategory" client/src/pages/admin/CategoriesList.tsx && grep -q "useDeleteCategory" client/src/pages/admin/CategoriesList.tsx && grep -q "ConfirmDialog" client/src/pages/admin/CategoriesList.tsx && ! grep -qi "slug" client/src/pages/admin/CategoriesList.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CategoriesList renders categories from useAdminCategories ordered by display order
    - the create/edit form has exactly name + display order fields and NO slug field (grep finds no "slug" in the file)
    - submit calls useUpsertCategory; delete is guarded by ConfirmDialog and calls useDeleteCategory
    - in-use delete surfaces the friendly "{N} products — move or delete them first" message and does not delete
    - loading/error/empty states present (Skeleton, Retry refetch, "No categories yet")
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Category create/edit/delete with auto-hidden slug and friendly in-use delete protection is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify category slice end-to-end (create order → Shop tabs; in-use delete blocked)</name>
  <what-built>
    CategoriesList does category CRUD with display-order control and in-use delete protection, riding lib/admin.ts.
  </what-built>
  <read_first>
    - client/src/pages/Shop.tsx (the public category tabs that consume useCategories, ordered by sort_order)
    - .planning/phases/04-admin-portal-catalog-content-management/04-VALIDATION.md (Manual-Only: in-use category delete blocked)
  </read_first>
  <action>
    Run the end-to-end category-slice verification below (npm run check + build, then create/edit display order reflected on /shop, delete empty category, and confirm an in-use category delete is blocked with the friendly {N}-products message) and approve only when all pass.
  </action>
  <how-to-verify>
    1. `npm run check` exits 0; `npm run build` succeeds.
    2. `npm run dev`, log in as admin, go to /admin/categories → the three existing categories (soap/scrub/cream) list in their sort_order.
    3. Edit one category's display order so the order changes; Save → toast "Category saved."; reload /shop → the category tab order reflects the new display order (no redeploy).
    4. Create a new empty category (name + order); Save → appears in the list and as a /shop tab.
    5. Delete the new (empty) category via the confirm dialog → toast "Category deleted."; gone from list and /shop.
    6. Attempt to delete a category that HAS products (e.g. soap) → blocked with "This category has {N} products — move or delete them first."; the category is NOT deleted and its products remain intact.
  </how-to-verify>
  <acceptance_criteria>
    - create/edit display order → /shop tab order updates without redeploy
    - delete an empty category (with confirm) → removed from list and /shop
    - delete an in-use category → blocked with the friendly {N}-products message; category and its products survive
    - every write toasts; `npm run build` succeeds
  </acceptance_criteria>
  <resume-signal>Type "approved" once create/edit/reorder works on /shop and the in-use delete is blocked with the friendly message, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form → categories table | Writes ride the admin JWT; RLS is_admin() gates them |
| category delete → products FK | The FK is the real integrity guard against orphaning products |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Integrity / Tampering | category delete | mitigate | Rely on the products.category_id FK (23503) — the UI never works around it; useDeleteCategory translates 23503 to the friendly D-15 message and aborts (no orphaned products) |
| T-04-17 | Elevation of Privilege | category writes | mitigate | categories_admin_write RLS (0002) rejects non-admin writes server-side; UI guard is convenience |
| T-04-SC | Tampering | npm installs | n/a | No installs in this plan |
</threat_model>

<verification>
- `npm run check` exits 0; `npm run build` succeeds.
- CategoriesList consumes the admin.ts category hooks; no slug UI; delete confirm-gated.
- Manual: create/edit/reorder → /shop; in-use delete blocked.
</verification>

<success_criteria>
- An admin manages categories (name + order) end-to-end and the Shop tabs reflect it live.
- In-use categories cannot be deleted; products are never orphaned.
- Slug stays auto/hidden.
</success_criteria>

<output>
Create `.planning/phases/04-admin-portal-catalog-content-management/04-06-SUMMARY.md` when done.
</output>
