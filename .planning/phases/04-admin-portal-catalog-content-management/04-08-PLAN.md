---
phase: 04-admin-portal-catalog-content-management
plan: 08
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - client/src/pages/admin/Submissions.tsx
  - client/src/lib/submissions.ts
autonomous: false
requirements: [ADMIN-07]
must_haves:
  truths:
    - "An admin can view a list of customer customization submissions (name, date, message snippet), newest first"
    - "An admin can open a submission to read the full request detail"
    - "Until Phase 5 ships the questionnaire, the inbox shows a clear empty state (no error)"
    - "A non-admin cannot read submissions (admin-read RLS already enforces this server-side)"
  artifacts:
    - path: "client/src/pages/admin/Submissions.tsx"
      provides: "Read-only submissions inbox: list (newest-first) + detail view + empty state (ADMIN-07, D-17)"
      min_lines: 60
    - path: "client/src/lib/submissions.ts"
      provides: "Read hook for customization_submissions (admin-read RLS), newest-first"
      exports: ["useSubmissions"]
  key_links:
    - from: "client/src/pages/admin/Submissions.tsx"
      to: "lib/submissions.ts useSubmissions"
      via: "list query"
      pattern: "useSubmissions"
    - from: "client/src/lib/submissions.ts"
      to: "customization_submissions table"
      via: "select ordered by created_at desc"
      pattern: "customization_submissions"
---

<objective>
Deliver the submissions-inbox vertical slice: an admin views customer customization submissions in a read-only list + detail view, newest-first, with a clear empty state (ADMIN-07). The screen reads through the existing admin-read RLS on customization_submissions (Phase 1 D-12).

Purpose: This is the read surface for what Phase 5's native questionnaire (CUST-03) will write. It will have no data until then — the inbox is built ready for it, with an empty state that explains that, so it is not mistaken for a bug. No status/"mark handled" column this phase (D-17 — that needs a schema change, deferred).
Output: useSubmissions read hook + Submissions inbox page (list + detail + empty state).
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-admin-portal-catalog-content-management/04-UI-SPEC.md
@.planning/phases/04-admin-portal-catalog-content-management/04-PATTERNS.md
@client/src/lib/catalog.ts
</context>

<artifacts_this_phase_produces>
This plan creates a read hook and replaces the Plan-04 stub page (no App.tsx edits):
- `client/src/lib/submissions.ts` → `useSubmissions`
- `client/src/pages/admin/Submissions.tsx` (real)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: useSubmissions read hook + Submissions inbox (list + detail + empty state)</name>
  <files>client/src/lib/submissions.ts, client/src/pages/admin/Submissions.tsx</files>
  <read_first>
    - client/src/lib/catalog.ts (the useQuery + fetch split to mirror; queryKey convention)
    - client/src/lib/supabase.ts (the singleton)
    - supabase/migrations/0001_init_schema.sql (lines 87-96: customization_submissions columns id/user_id/name/email/skin_type/message/payload/created_at)
    - supabase/migrations/0002_rls_policies.sql (lines 111-114: customization_submissions_admin_or_owner_read — admin reads all; no insert policy this phase, so the table is empty until Phase 5)
    - client/src/components/ui/table.tsx, skeleton.tsx, empty.tsx, dialog.tsx or sheet.tsx (for the detail view)
    - .planning/phases/04-admin-portal-catalog-content-management/04-UI-SPEC.md (Tables/lists states; Empty "No submissions yet" / "Customer customization requests will appear here once the questionnaire is live." (NO CTA — read-only); Submissions list = name/date/snippet, detail opens full message)
    - .planning/phases/04-admin-portal-catalog-content-management/04-CONTEXT.md (D-17 read-only list+detail this phase; no status column; empty until Phase 5)
  </read_first>
  <action>
    Create client/src/lib/submissions.ts exporting `useSubmissions()` — a useQuery (key ['submissions']) selecting id, name, email, skin_type, message, payload, created_at from customization_submissions ordered by created_at descending (newest first). This rides the existing admin-read RLS (no extra auth code). Then replace the Plan-04 Submissions.tsx stub: a read-only inbox. Page title "Submissions". List rows show name (or email fallback), the created_at date (format with date-fns, already a dep), and a one-line message snippet. Clicking a row opens a detail view (shadcn Dialog or Sheet) showing the full name/email/skin_type/message/payload. States: loading → Skeleton rows; error → inline message + Retry refetch(); empty → shadcn `empty` heading "No submissions yet" / body "Customer customization requests will appear here once the questionnaire is live." with NO CTA (read-only). No edit/delete/status controls (D-17). Default export Submissions.
  </action>
  <verify>
    <automated>npm run check && grep -q "useSubmissions" client/src/pages/admin/Submissions.tsx && grep -q "customization_submissions" client/src/lib/submissions.ts && grep -q "created_at" client/src/lib/submissions.ts && grep -q "No submissions yet" client/src/pages/admin/Submissions.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - client/src/lib/submissions.ts exports useSubmissions querying customization_submissions ordered by created_at desc (newest first)
    - Submissions.tsx renders a read-only list (name/date/snippet) with a detail view and NO edit/delete/status controls
    - empty state shows "No submissions yet" / the Phase-5 explanation with no CTA
    - loading (Skeleton) and error (Retry refetch) states present
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The read-only submissions inbox (list + detail + empty state) is complete, riding the admin-read RLS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify submissions inbox (empty state now; one test row renders newest-first)</name>
  <what-built>
    The submissions inbox reads customization_submissions via admin-read RLS, showing an empty state until Phase 5 and a list+detail when rows exist.
  </what-built>
  <read_first>
    - supabase/migrations/0002_rls_policies.sql (admin-or-owner read policy — to confirm a manually-inserted row is visible to the admin)
    - .planning/phases/04-admin-portal-catalog-content-management/04-VALIDATION.md (Manual-Only: submissions read-only inbox; insert a test row → appears newest-first; detail opens)
  </read_first>
  <action>
    Run the submissions-inbox verification below (npm run check + build, confirm the empty state, insert one or two test rows via the dashboard, confirm they render newest-first with a working detail view and that non-admin/anon cannot read them, then remove the test rows) and approve only when all pass.
  </action>
  <how-to-verify>
    1. `npm run check` exits 0; `npm run build` succeeds.
    2. `npm run dev`, log in as admin, /admin/submissions → empty state "No submissions yet" with the Phase-5 explanation (no error, no CTA).
    3. Insert one or two test rows into customization_submissions via the Supabase dashboard SQL editor (with the service role / dashboard, since there is no insert policy yet), with different created_at values and a name/email/skin_type/message.
    4. Reload /admin/submissions → the rows appear NEWEST FIRST with name/date/snippet; click a row → the detail view shows the full message and fields.
    5. Optional: log in as a non-admin customer and confirm /admin redirects (AdminGuard) — and that even a direct anon PostgREST select on customization_submissions returns nothing (admin-read RLS).
    6. Remove the test rows afterward.
  </how-to-verify>
  <acceptance_criteria>
    - empty inbox shows the "No submissions yet" state (no error)
    - manually-inserted rows appear newest-first with name/date/snippet; detail view opens the full submission
    - non-admin/anon cannot read submissions (RLS), and /admin is guarded
    - test rows removed; `npm run build` succeeds
  </acceptance_criteria>
  <resume-signal>Type "approved" once the empty state shows and inserted test rows render newest-first with a working detail view, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/customer → customization_submissions | Submissions are private; only admins (and the owning customer in Phase 5) may read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Information Disclosure | submissions read | mitigate | customization_submissions_admin_or_owner_read RLS (0002) restricts reads to admins/owner; the inbox query relies on it — an anon/customer select returns nothing (verified Task 2 step 5) |
| T-04-23 | Information Disclosure | admin route access | mitigate | /admin/submissions stays behind AdminGuard (Plan 04) + RLS |
| T-04-SC | Tampering | npm installs | n/a | No installs in this plan |
</threat_model>

<verification>
- `npm run check` exits 0; `npm run build` succeeds.
- Submissions reads newest-first via admin-read RLS; empty state correct.
- Manual: empty now; test rows render newest-first with detail.
</verification>

<success_criteria>
- An admin can read customization submissions in a clean read-only inbox (list + detail), newest-first.
- The empty state explains the inbox fills once Phase 5 ships the questionnaire.
- Non-admins cannot read submissions (RLS).
</success_criteria>

<output>
Create `.planning/phases/04-admin-portal-catalog-content-management/04-08-SUMMARY.md` when done.
</output>
