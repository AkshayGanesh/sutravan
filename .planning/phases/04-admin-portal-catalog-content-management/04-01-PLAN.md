---
phase: 04-admin-portal-catalog-content-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_cr01_products_public_read.sql
  - supabase/migrations/0006_seed_site_content.sql
autonomous: false
requirements: [ADMIN-05, ADMIN-06, ADMIN-08]
must_haves:
  truths:
    - "A draft product (is_active=false) is unreachable via a raw anon PostgREST select that omits the is_active filter"
    - "An admin session still reads draft products (the FOR ALL admin policy is unaffected by the tightened public-read policy)"
    - "The site_content table contains seven rows (hero_title, hero_subtitle, hero_cta, our_story_body, email, instagram_url, youtube_url) after migration 0006 runs"
    - "Re-running migration 0006 does not overwrite owner-edited site_content values (on conflict do nothing)"
  artifacts:
    - path: "supabase/migrations/0005_cr01_products_public_read.sql"
      provides: "CR-01 RLS tightening: products_public_read using (is_active = true)"
      contains: "using (is_active = true)"
    - path: "supabase/migrations/0006_seed_site_content.sql"
      provides: "Idempotent seed of the seven site_content key/value rows from current hardcoded strings"
      contains: "on conflict (key) do nothing"
  key_links:
    - from: "supabase/migrations/0005_cr01_products_public_read.sql"
      to: "public.products RLS"
      via: "drop + recreate products_public_read policy"
      pattern: "create policy \"products_public_read\""
    - from: "supabase/migrations/0006_seed_site_content.sql"
      to: "public.site_content rows"
      via: "insert ... on conflict (key) do nothing"
      pattern: "insert into public.site_content"
---

<objective>
Ship the two new numbered migrations this phase's data layer depends on, then push them to the live Supabase project and prove the security invariant.

This is the foundation wave for the admin portal: (1) the CR-01 RLS hardening (D-14, LOCKED) that makes draft products genuinely unreachable via raw PostgREST — not just filtered query-side in catalog.ts — which the ADMIN-08 draft/published toggle relies on for real security; and (2) the idempotent site_content seed (D-18) that populates the seven editable keys from the current hardcoded strings so ADMIN-05/06 editing and the D-20 public rewire have data to read.

Purpose: Without 0005, the moment ADMIN-08 introduces a draft toggle, a draft product becomes an Information-Disclosure leak via any direct PostgREST call that omits `.eq('is_active', true)`. Without 0006, the site-content editor and the public useSiteContent rewire have no rows to read.
Output: migrations 0005 + 0006 authored, pushed live via `supabase db push`, and the draft-isolation + admin-still-sees-drafts invariants verified against the live project.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-portal-catalog-content-management/04-CONTEXT.md
@.planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md
@.planning/phases/04-admin-portal-catalog-content-management/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates these new symbols (exclude from drift verification):
- `supabase/migrations/0005_cr01_products_public_read.sql` — migration file
- `supabase/migrations/0006_seed_site_content.sql` — migration file
- RLS policy `products_public_read` (recreated with `using (is_active = true)`)
- site_content keys: `hero_title`, `hero_subtitle`, `hero_cta`, `our_story_body`, `email`, `instagram_url`, `youtube_url`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0005 — CR-01 products_public_read tightening (D-14)</name>
  <files>supabase/migrations/0005_cr01_products_public_read.sql</files>
  <read_first>
    - supabase/migrations/0002_rls_policies.sql (the current `products_public_read` at lines 28-31 uses `using (true)`; the `products_admin_write` FOR ALL policy at lines 33-37 grants admin read regardless of is_active — DO NOT touch it)
    - supabase/migrations/0004_auth_profiles.sql (lines 1-20: the migration header-comment convention — file name, phase/plan/task, decision IDs, net invariant, source line ref)
    - .planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md (the "CR-01 migration (0005)" SQL example; Pitfall 4: admins read drafts via the separate FOR ALL policy)
  </read_first>
  <action>
    Create supabase/migrations/0005_cr01_products_public_read.sql. Start with a header comment block matching the 0002/0004 convention: file name, "Phase 4 / Plan 01 / Task 1", decision id D-14, the net invariant ("draft (is_active=false) products are unreachable via the anon/authenticated public-read path; admins still read drafts via products_admin_write FOR ALL"), and the source line ref (02-REVIEW.md CR-01 finding). Then `drop policy "products_public_read" on public.products;` and recreate it: `create policy "products_public_read" on public.products for select to anon, authenticated using (is_active = true);`. Do NOT modify products_admin_write — admins must keep seeing drafts. Do NOT add any DELETE/UPDATE changes. Keep the lowercase SQL style of the existing migrations.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0005_cr01_products_public_read.sql && grep -q 'using (is_active = true)' supabase/migrations/0005_cr01_products_public_read.sql && grep -q 'drop policy "products_public_read"' supabase/migrations/0005_cr01_products_public_read.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0005_cr01_products_public_read.sql exists
    - file contains the literal `using (is_active = true)`
    - file contains `drop policy "products_public_read" on public.products`
    - file contains `create policy "products_public_read"` with `for select` and `to anon, authenticated`
    - file does NOT contain any reference to `products_admin_write` being altered/dropped
  </acceptance_criteria>
  <done>Migration 0005 authored: products_public_read recreated as published-only, admin write policy untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 0006 — idempotent site_content seed (D-18)</name>
  <files>supabase/migrations/0006_seed_site_content.sql</files>
  <read_first>
    - supabase/migrations/0003_storage_buckets.sql (lines 21-24: the idempotent `insert ... on conflict (id) do nothing` pattern to mirror with `on conflict (key)`)
    - supabase/migrations/0001_init_schema.sql (lines 77-81: site_content is `key text primary key, value text` — conflict target is `key`)
    - client/src/components/Footer.tsx (lines 3-5: exact INSTAGRAM_URL = "https://www.instagram.com/sutravan.in", YOUTUBE_URL = "https://youtube.com/@sutravan?si=0ne7zUvFEh70AF6j", EMAIL = "sutravan.in@gmail.com")
    - client/src/components/Hero.tsx (the hero h1 "Formulas Born From The Purity of Earth", the subtitle paragraph, the CTA "Explore Collection")
    - client/src/pages/OurStory.tsx (the body copy across "The Beginning" / "Discovery" / "Journey" / "What We Create" / "Belief" sections to compose our_story_body as sanitized HTML)
  </read_first>
  <action>
    Create supabase/migrations/0006_seed_site_content.sql with the 0003-style header comment (file name, "Phase 4 / Plan 01 / Task 2", D-18, invariant "seeds the seven editable site_content keys from current code; idempotent — never clobbers owner edits"). Insert seven rows into public.site_content (key, value): `hero_title` = 'Formulas Born From The Purity of Earth' (the Hero h1 text, the two lines joined); `hero_subtitle` = the Hero subtitle paragraph verbatim ("Experience earthen luxury with our handcrafted botanical skincare. Nourishing soaps, revitalizing scrubs, and rich creams."); `hero_cta` = 'Explore Collection'; `our_story_body` = the OurStory body sections composed as a single sanitized HTML string using only these tags: p, strong, em, ul, ol, li, h2, h3, br, a (this is what TipTap/DOMPurify allow in Plan 07 — keep paragraphs as `<p>...</p>`, the existing `<strong>`/`<em>` emphasis, and headings as `<h2>`); `email` = 'sutravan.in@gmail.com'; `instagram_url` = 'https://www.instagram.com/sutravan.in'; `youtube_url` = 'https://youtube.com/@sutravan?si=0ne7zUvFEh70AF6j'. End the insert with `on conflict (key) do nothing;` so re-running never overwrites later admin edits. Escape single quotes in the body text by doubling them per SQL.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0006_seed_site_content.sql && grep -q 'on conflict (key) do nothing' supabase/migrations/0006_seed_site_content.sql && grep -q "'hero_title'" supabase/migrations/0006_seed_site_content.sql && grep -q "'our_story_body'" supabase/migrations/0006_seed_site_content.sql && grep -q "'instagram_url'" supabase/migrations/0006_seed_site_content.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0006_seed_site_content.sql exists
    - file contains `insert into public.site_content`
    - file contains all seven keys: 'hero_title', 'hero_subtitle', 'hero_cta', 'our_story_body', 'email', 'instagram_url', 'youtube_url'
    - file ends the insert with `on conflict (key) do nothing`
    - email value is 'sutravan.in@gmail.com'; instagram_url and youtube_url match Footer.tsx:3-5 verbatim
    - our_story_body value uses only the tags p/strong/em/ul/ol/li/h2/h3/br/a (no script, style, onerror, etc.)
  </acceptance_criteria>
  <done>Migration 0006 authored: seven site_content keys seeded idempotently from current hardcoded strings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Push migrations 0005 + 0006 live and verify CR-01 draft isolation</name>
  <what-built>
    Two new migrations (0005 CR-01 RLS tightening, 0006 site_content seed) are authored and ready to push to the live Supabase project. This task pushes them and proves the security invariant that ADMIN-08 depends on.
  </what-built>
  <read_first>
    - supabase/config.toml (project_id = "wfbnrcnmpcqzeyjlfflv"; the linked project the push targets)
    - scripts/promote-admin.ts (how service-role / SUPABASE env vars are read from non-VITE_ process.env in this repo)
    - .planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md (Environment Availability: supabase CLI 2.102.0 is a devDep; fallback = apply SQL via dashboard SQL editor)
  </read_first>
  <action>
    Apply the verification steps below as the work of this task: run the schema push (npx supabase db push --linked, or the dashboard SQL editor / SUPABASE_ACCESS_TOKEN fallback), then prove the CR-01 draft-isolation invariant and the seven-key seed before approving.
  </action>
  <how-to-verify>
    1. Run the schema push. Preferred: `npx supabase db push --linked` (the project is already linked, config.toml project_id wfbnrcnmpcqzeyjlfflv). If the CLI requires interactive login/prompts that cannot be suppressed, set `SUPABASE_ACCESS_TOKEN` in the environment first, or fall back to pasting the contents of 0005 and 0006 into the Supabase Dashboard SQL editor (run 0005 then 0006). Confirm the push reports 0005 and 0006 applied (or the SQL editor returns success).
    2. Verify CR-01 draft isolation as ANON (no auth). Using the anon key, run a raw PostgREST select that does NOT filter is_active, against a temporarily-inserted draft row OR an existing row flipped to is_active=false via the dashboard. Expected: the draft row is NOT returned (only is_active=true rows come back). Command shape: `curl -s "$VITE_SUPABASE_URL/rest/v1/products?select=slug,is_active" -H "apikey: $VITE_SUPABASE_ANON_KEY"` returns only published rows. Restore any test row to is_active=true afterward.
    3. Verify the seed landed: `curl -s "$VITE_SUPABASE_URL/rest/v1/site_content?select=key" -H "apikey: $VITE_SUPABASE_ANON_KEY"` returns the seven keys (hero_title, hero_subtitle, hero_cta, our_story_body, email, instagram_url, youtube_url).
    4. (Sanity, optional) Confirm an admin session still sees drafts — this is fully proven in Plan 05 when the admin list query (no is_active filter) shows draft rows; note it here as expected.
  </how-to-verify>
  <acceptance_criteria>
    - `npx supabase db push --linked` (or dashboard SQL editor apply) reports 0005 and 0006 applied with no error
    - anon raw PostgREST select on products WITHOUT an is_active filter returns zero is_active=false rows (draft isolation proven)
    - anon PostgREST select on site_content returns exactly the seven seeded keys
    - no test draft row is left in is_active=false state after verification
  </acceptance_criteria>
  <resume-signal>Type "approved" once the push succeeded and draft isolation + seven seed keys are confirmed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → PostgREST (products) | Untrusted public read; must never see draft rows |
| admin JWT → PostgREST (products) | Trusted admin read; must still see drafts |
| migration runner (service-role/CLI) → DB | Trusted, local/CI; applies schema changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `products_public_read` policy | mitigate | 0005 tightens to `using (is_active = true)` so a raw PostgREST select omitting the filter cannot leak drafts (the whole point of CR-01/D-14); verified in Task 3 step 2 |
| T-04-02 | Elevation of Privilege | admin draft read | mitigate (regression guard) | `products_admin_write` FOR ALL policy is left untouched so admins keep reading drafts; Task 1 acceptance forbids altering it |
| T-04-03 | Tampering | site_content seed re-run | mitigate | `on conflict (key) do nothing` guarantees re-running 0006 never overwrites owner-edited content |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (SQL + CLI push only) |
</threat_model>

<verification>
- `npm run check` passes (no TS touched, but confirms repo still type-clean).
- Migrations 0005 + 0006 exist and contain the required clauses (Tasks 1-2 automated greps).
- Live push applied; anon draft isolation proven; seven seed keys present (Task 3).
</verification>

<success_criteria>
- products_public_read enforces `is_active = true` on the live project; drafts are unreachable to anon via raw PostgREST.
- Admin sessions still read drafts (admin FOR ALL policy intact).
- site_content holds the seven editable keys, seeded idempotently.
</success_criteria>

<output>
Create `.planning/phases/04-admin-portal-catalog-content-management/04-01-SUMMARY.md` when done.
</output>
