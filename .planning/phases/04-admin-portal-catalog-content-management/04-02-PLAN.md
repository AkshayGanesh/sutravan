---
phase: 04-admin-portal-catalog-content-management
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - client/src/lib/slug.ts
  - client/src/lib/slug.test.ts
  - client/src/lib/sanitizeHtml.ts
  - client/src/lib/sanitizeHtml.test.ts
  - client/src/lib/imagePipeline.ts
  - client/src/lib/imagePipeline.test.ts
  - client/src/lib/adminErrors.ts
  - client/src/lib/adminErrors.test.ts
autonomous: false
requirements: [ADMIN-01, ADMIN-03, ADMIN-04, ADMIN-05]
must_haves:
  truths:
    - "slugify('Neem & Tulsi Soap!') returns 'neem-tulsi-soap' (lowercase, hyphenated, punctuation stripped, no trailing hyphen)"
    - "sanitizeRichText strips a <script> tag and an onerror attribute but keeps <p>, <strong>, <a href>"
    - "sanitizeRichText forces rel=\"noopener noreferrer\" and target=\"_blank\" on external <a> links"
    - "the image size/type guard rejects a >10MB file and an unsupported MIME, and accepts JPEG/PNG/WebP/HEIC"
    - "mapWriteError maps PostgREST 23503 to the in-use-category copy and 23505 to a slug-collision signal"
  artifacts:
    - path: "client/src/lib/slug.ts"
      provides: "slugify(name) pure util (D-07)"
      exports: ["slugify"]
    - path: "client/src/lib/sanitizeHtml.ts"
      provides: "DOMPurify allow-list wrapper for public rich-text render (D-19)"
      exports: ["sanitizeRichText"]
    - path: "client/src/lib/imagePipeline.ts"
      provides: "size/type guard + lazy HEIC convert + compress (D-11/D-12)"
      exports: ["assertImageAllowed", "processImage", "MAX_IMAGE_BYTES", "ACCEPTED_IMAGE_TYPES"]
    - path: "client/src/lib/adminErrors.ts"
      provides: "mapWriteError PostgREST-code → friendly copy (mirrors authErrors.ts)"
      exports: ["mapWriteError"]
    - path: "vitest.config.ts"
      provides: "Vitest config with @ alias for the four unit suites"
  key_links:
    - from: "client/src/lib/imagePipeline.ts"
      to: "heic2any + browser-image-compression"
      via: "dynamic import inside processImage (kept out of public bundle)"
      pattern: "await import\\('(heic2any|browser-image-compression)'\\)"
    - from: "client/src/lib/sanitizeHtml.ts"
      to: "dompurify"
      via: "DOMPurify.sanitize allow-list"
      pattern: "DOMPurify.sanitize"
---

<objective>
Build and unit-test the four pure logic modules the whole admin write layer rests on, and stand up the minimal Vitest footing the validation strategy (Wave 0) calls for. These are the regression-prone, cheap-to-test functions RESEARCH isolated: slug generation, HTML sanitization, the image size/type guard, and PostgREST-error mapping.

Purpose: Plan 03's lib/admin.ts and Plan 07's public Our Story render consume these directly. Writing them test-first (RED→GREEN) locks their contracts before any UI builds against them, and gives execution an automated `npx vitest run` signal beyond `tsc`. The new npm deps (heic2any, browser-image-compression, @tiptap/*, dompurify) are already in package.json and node_modules — but their NAMES were [ASSUMED] in research, so a human-verify checkpoint gates acceptance of them before they are exercised.
Output: vitest installed + configured; slug.ts, sanitizeHtml.ts, imagePipeline.ts, adminErrors.ts with passing unit tests.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md
@.planning/phases/04-admin-portal-catalog-content-management/04-PATTERNS.md
@.planning/phases/04-admin-portal-catalog-content-management/04-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates these new symbols (exclude from drift verification):
- `client/src/lib/slug.ts` → `slugify`
- `client/src/lib/sanitizeHtml.ts` → `sanitizeRichText`
- `client/src/lib/imagePipeline.ts` → `assertImageAllowed`, `processImage`, `MAX_IMAGE_BYTES`, `ACCEPTED_IMAGE_TYPES`
- `client/src/lib/adminErrors.ts` → `mapWriteError`
- `vitest.config.ts`; test files `*.test.ts` for each module
- npm script `test` (vitest run)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [GATE] Verify the [ASSUMED] new image/rich-text packages before exercising them</name>
  <what-built>
    package.json already lists @tiptap/react, @tiptap/starter-kit, browser-image-compression, heic2any, heic-to, and dompurify, and node_modules contains them. RESEARCH tagged every package name [ASSUMED] (discovered via web/training, not official docs) and slopcheck returned [OK] for all. Per the package-legitimacy gate, a human confirms legitimacy before code exercises them.
  </what-built>
  <read_first>
    - .planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md (## Package Legitimacy Audit — the slopcheck [OK] table; Assumptions Log A1-A4)
    - package.json (dependencies block — confirm the six packages and their versions)
  </read_first>
  <action>
    Confirm the legitimacy of the [ASSUMED] packages (dompurify, browser-image-compression, heic2any, /react, /starter-kit) against their registries/source repos per the steps below; approve heic2any as the HEIC primary before any code exercises these libs.
  </action>
  <how-to-verify>
    1. Confirm each package exists on its registry with a real source repo:
       - dompurify → https://www.npmjs.com/package/dompurify (github.com/cure53/DOMPurify)
       - browser-image-compression → https://www.npmjs.com/package/browser-image-compression (github.com/Donaldcwl/browser-image-compression)
       - heic2any → https://www.npmjs.com/package/heic2any (github.com/alexcorvi/heic2any)
       - @tiptap/react and @tiptap/starter-kit → https://www.npmjs.com/package/@tiptap/react (github.com/ueberdosis/tiptap)
    2. Confirm versions in package.json match what RESEARCH approved (dompurify ^3.4.7, @tiptap ^3.24.0, browser-image-compression ^2.0.2, heic2any ^0.0.4).
    3. Confirm none added a postinstall script (RESEARCH: none found).
    4. Decision: keep heic2any as primary (heic-to is the documented fallback if HEIC edge cases fail later).
  </how-to-verify>
  <acceptance_criteria>
    - All six packages confirmed present on npm with the source repos above
    - package.json versions match the RESEARCH-approved versions
    - No postinstall script introduced by any of them
    - Owner approves proceeding with heic2any as the HEIC primary
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm the packages are legitimate and proceed, or name any package to swap.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest footing + slug.ts + adminErrors.ts (RED→GREEN)</name>
  <files>vitest.config.ts, package.json, client/src/lib/slug.ts, client/src/lib/slug.test.ts, client/src/lib/adminErrors.ts, client/src/lib/adminErrors.test.ts</files>
  <read_first>
    - client/src/lib/format.ts (the pure-util convention: self-documenting header comment, named export, imports nothing)
    - client/src/lib/authErrors.ts (the str→str error-mapper convention to mirror as mapWriteError: lowercase message extraction, ordered if-branches, GENERIC_FALLBACK)
    - vite.config.ts (the @ alias = ./client/src/* and @shared/@assets aliases — vitest.config.ts must replicate the @ alias resolution)
    - tsconfig.json (path aliases @/* and @shared/*)
    - .planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md (slugify example; in-use category friendly-error example with error.code 23503; Pitfall 6 slug collision 23505)
    - .planning/phases/04-admin-portal-catalog-content-management/04-UI-SPEC.md (Copywriting Contract → Error states: "This category has {N} products — move or delete them first.")
  </read_first>
  <behavior>
    slug.test.ts:
    - slugify('Neem Soap') === 'neem-soap'
    - slugify('Neem & Tulsi Soap!') === 'neem-tulsi-soap'
    - slugify('  Multani  Mitti  ') === 'multani-mitti' (collapses whitespace, trims)
    - slugify('Rose---Cream') === 'rose-cream' (collapses repeated separators, no leading/trailing hyphen)
    adminErrors.test.ts:
    - mapWriteError({ code: '23503' }) returns a string containing 'move or delete them first'
    - mapWriteError({ code: '23505' }) returns a string indicating a name/slug already-in-use collision
    - mapWriteError({ message: 'Failed to fetch' }) returns the network copy
    - mapWriteError(unknown) returns the generic "Something went wrong while saving. Please try again." fallback (UI-SPEC verbatim)
  </behavior>
  <action>
    First install + configure Vitest: `npm install -D vitest` (and `@vitejs/plugin-react` is already present). Create vitest.config.ts with `test.environment: 'node'` for these pure suites, and a `resolve.alias` mapping `@` → the absolute `client/src` path (mirror vite.config.ts). Add a `"test": "vitest run"` script to package.json (do not remove existing scripts). Write slug.test.ts and adminErrors.test.ts FIRST (RED). Then implement client/src/lib/slug.ts exporting `slugify(name: string): string` — lowercase, trim, replace `/[^a-z0-9]+/g` with '-', strip leading/trailing hyphens (D-07; slug stays stable on rename — that stability is enforced by the caller in admin.ts, not here). Implement client/src/lib/adminErrors.ts mirroring authErrors.ts as a pure `mapWriteError(error: unknown): string`: read `error.code` and `error.message`; map PostgREST `23503` (FK violation) → "This category has products — move or delete them first." (note: the {N} count is filled by the caller in admin.ts which knows the product count; this base copy carries the actionable instruction), `23505` (unique violation) → a collision message, network/fetch failures → "Network error — please check your connection and try again.", everything else → "Something went wrong while saving. Please try again." Keep each file's header comment in the format.ts/authErrors.ts style.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/slug.test.ts client/src/lib/adminErrors.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run client/src/lib/slug.test.ts client/src/lib/adminErrors.test.ts` exits 0 with all cases green
    - client/src/lib/slug.ts contains `export function slugify(`
    - client/src/lib/adminErrors.ts contains `export function mapWriteError(` and references both '23503' and '23505'
    - package.json contains a `"test"` script running `vitest run`
    - vitest.config.ts resolves the `@` alias to client/src
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Vitest configured; slugify and mapWriteError implemented with green tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sanitizeHtml.ts + imagePipeline.ts guard (RED→GREEN)</name>
  <files>client/src/lib/sanitizeHtml.ts, client/src/lib/sanitizeHtml.test.ts, client/src/lib/imagePipeline.ts, client/src/lib/imagePipeline.test.ts, vitest.config.ts</files>
  <read_first>
    - client/src/lib/authErrors.ts (pure str→str convention to mirror for sanitizeHtml)
    - .planning/phases/04-admin-portal-catalog-content-management/04-RESEARCH.md (Pattern 4 DOMPurify allow-list ALLOWED_TAGS/ALLOWED_ATTR + the afterSanitizeAttributes hook forcing rel="noopener noreferrer"; the Image pipeline code example with MAX_BYTES, ACCEPTED, the HEIC branch lazy-import, browser-image-compression options)
    - .planning/phases/04-admin-portal-catalog-content-management/04-UI-SPEC.md (Image dropzone error copy; rich-text editor allowed marks: bold/italic/link/bullet/ordered list — the sanitizer allow-list must match these)
    - vitest.config.ts (created in Task 2 — sanitizeHtml.test.ts needs the jsdom/happy-dom environment because DOMPurify uses the DOM; add a per-file environment or switch the suite)
  </read_first>
  <behavior>
    sanitizeHtml.test.ts:
    - sanitizeRichText('<p>hi</p><script>alert(1)</script>') does NOT contain '<script'
    - sanitizeRichText('<img src=x onerror=alert(1)>') does NOT contain 'onerror'
    - sanitizeRichText('<p><strong>bold</strong> <em>it</em></p>') keeps strong and em
    - sanitizeRichText('<a href="https://x.com">x</a>') keeps the href AND adds rel="noopener noreferrer" and target="_blank"
    imagePipeline.test.ts (guard only — do NOT invoke the lazy convert/compress in tests; mock or test assertImageAllowed alone):
    - assertImageAllowed for an 11MB File throws (message contains 'too large' / '10')
    - assertImageAllowed for a text/plain file throws (unsupported type)
    - assertImageAllowed for image/jpeg, image/png, image/webp, image/heic does NOT throw
    - MAX_IMAGE_BYTES === 10 * 1024 * 1024; ACCEPTED_IMAGE_TYPES includes the four image MIME families + heif
  </behavior>
  <action>
    Install a DOM test env: `npm install -D jsdom` (or happy-dom) and set the sanitizeHtml suite to `environment: 'jsdom'` (use a `// @vitest-environment jsdom` file pragma at the top of sanitizeHtml.test.ts, or a `test.environmentMatchGlobs` entry). Write sanitizeHtml.test.ts and imagePipeline.test.ts FIRST (RED). Implement client/src/lib/sanitizeHtml.ts exporting `sanitizeRichText(html: string): string` using `import DOMPurify from 'dompurify'` with ALLOWED_TAGS ['p','br','strong','em','b','i','u','a','ul','ol','li','h2','h3'] and ALLOWED_ATTR ['href','target','rel']; register a DOMPurify `afterSanitizeAttributes` hook that, for any node with an href, sets target="_blank" and rel="noopener noreferrer" (RESEARCH Pattern 4). Implement client/src/lib/imagePipeline.ts: export `MAX_IMAGE_BYTES = 10 * 1024 * 1024`, `ACCEPTED_IMAGE_TYPES = ['image/jpeg','image/png','image/webp','image/heic','image/heif']`, a pure synchronous `assertImageAllowed(file: { size: number; type: string; name: string }): void` that throws Error('That photo is too large (max 10 MB). Try a smaller one.') when size > MAX_IMAGE_BYTES and Error('That file type isn’t supported. Use JPEG, PNG, WebP or HEIC.') when the type is not accepted and the name does not end .heic/.heif (UI-SPEC copy), and an async `processImage(file: File): Promise<Blob>` that calls assertImageAllowed, then for HEIC input does `(await import('heic2any')).default(...)` to JPEG, then `(await import('browser-image-compression')).default(blob, { maxSizeMB: 1, maxWidthOrHeight: 1600, useWebWorker: true, fileType: 'image/jpeg' })`. Wrap the HEIC convert in try/catch and throw Error('Couldn’t process this photo — try a JPEG or PNG.') on failure (D-11 fallback). The dynamic imports keep heic2any/compression out of the public bundle.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/sanitizeHtml.test.ts client/src/lib/imagePipeline.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run client/src/lib/sanitizeHtml.test.ts client/src/lib/imagePipeline.test.ts` exits 0
    - sanitizeHtml.ts contains `DOMPurify.sanitize` and an `afterSanitizeAttributes` hook setting rel="noopener noreferrer"
    - imagePipeline.ts exports MAX_IMAGE_BYTES, ACCEPTED_IMAGE_TYPES, assertImageAllowed, processImage
    - imagePipeline.ts uses `await import('heic2any')` and `await import('browser-image-compression')` (dynamic, not top-level static imports)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>sanitizeRichText (XSS-safe, link-hardened) and the image guard/pipeline implemented with green tests; heavy libs lazy-imported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-authored HTML → public render | site_content.value HTML rendered to every visitor via dangerouslySetInnerHTML |
| uploaded file bytes → image pipeline | Untrusted file from the owner's device (may be oversized or non-image) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering / Elevation (XSS) | sanitizeRichText | mitigate | DOMPurify allow-list strips script/onerror/style; only safe inline tags + href survive; tested (Task 3) |
| T-04-05 | Tampering | rich-text link injection | mitigate | href-only ALLOWED_ATTR + forced rel="noopener noreferrer"/target="_blank" via afterSanitizeAttributes hook |
| T-04-06 | Tampering / DoS | image upload guard | mitigate | assertImageAllowed rejects >10MB and non-image MIME BEFORE any conversion CPU; re-encode to JPEG strips active content |
| T-04-SC | Tampering | npm packages (heic2any, dompurify, tiptap, browser-image-compression) | mitigate | Task 1 blocking-human checkpoint verifies legitimacy ([ASSUMED] names, slopcheck [OK]); never auto-approved |
</threat_model>

<verification>
- `npx vitest run` green across all four suites.
- `npm run check` exits 0.
- Lazy-import assertion: imagePipeline.ts contains no top-level `import ... from 'heic2any'` / `'browser-image-compression'`.
</verification>

<success_criteria>
- The four pure modules exist with their documented exports and passing unit tests.
- Vitest is installed and runnable via `npm run test`.
- Heavy image/HEIC libs are dynamically imported (bundle discipline).
</success_criteria>

<output>
Create `.planning/phases/04-admin-portal-catalog-content-management/04-02-SUMMARY.md` when done.
</output>
