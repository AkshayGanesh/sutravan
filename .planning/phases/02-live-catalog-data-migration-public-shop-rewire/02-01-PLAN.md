---
phase: 02-live-catalog-data-migration-public-shop-rewire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/data/catalog-data.ts
  - scripts/seed.ts
  - scripts/verify-seed.ts
autonomous: false
requirements: [DATA-03, PUB-02]
user_setup:
  - service: supabase
    why: "The one-time seed writes catalog rows + uploads soap images using the service-role key, which bypasses admin-only write RLS. This key is never shipped and must be supplied locally."
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> service_role (secret)"

must_haves:
  truths:
    - "Running the seed inserts exactly 28 products (13 soap + 10 scrub + 5 cream) and 3 categories into Supabase"
    - "Re-running the seed yields 28 products / 3 categories, not duplicates (idempotent upsert on slug)"
    - "Existing soap images are uploaded to the product-images bucket under products/{slug}/{filename} and their Storage paths are recorded on the soap rows"
    - "Scrub and cream products are seeded with an empty images[] array"
    - "Every product price is seeded as null (no placeholder/zero)"
    - "An anonymous (anon-key) client can read the seeded products and categories; an is_active=false row is absent from an anon select"
  artifacts:
    - path: "client/src/data/catalog-data.ts"
      provides: "Vite-independent (no import.meta.*) product + category metadata + SLUG_TO_SOAP_FOLDER map; importable by both Node and Vite"
      contains: "productMeta"
    - path: "scripts/seed.ts"
      provides: "Idempotent service-role seed: upsert categories then products on slug, upload soap images with explicit contentType"
      contains: "onConflict"
    - path: "scripts/verify-seed.ts"
      provides: "Anon-key verification: asserts 28 products / 3 categories and that an is_active=false row is hidden from an anon select"
      contains: "28"
  key_links:
    - from: "scripts/seed.ts"
      to: "client/src/data/catalog-data.ts"
      via: "Node import of glob-free metadata"
      pattern: "from ['\"].*catalog-data['\"]"
    - from: "scripts/seed.ts"
      to: "product-images bucket"
      via: "storage upload with contentType + upsert"
      pattern: "\\.upload\\("
    - from: "scripts/seed.ts"
      to: "public.products / public.categories"
      via: "upsert on slug"
      pattern: "onConflict.*slug"
---

<objective>
Deliver the **seed vertical slice**: a one-time, local, service-role script that lands the existing 28-product catalog (13 soap + 10 scrub + 5 cream) and 3 categories into the live Supabase project, uploads the existing soap images into the `product-images` Storage bucket, and records their Storage paths on the soap rows. The slice proves end-to-end that data lands in Postgres + Storage and that an anonymous client can read it — the foundation the public read-path rewire (Plans 02-03) renders.

Purpose: Implements DATA-03 (idempotent seed via service-role key, never shipped) and locks in the server-side `is_active` filter behavior that PUB-02 depends on. After this plan, the catalog exists in Supabase and re-running the seed converges (28/3), not duplicates.

Output: `client/src/data/catalog-data.ts` (glob-free metadata, also imported by Plans 02-03), `scripts/seed.ts`, `scripts/verify-seed.ts`, and a seeded live Supabase project (28 products / 3 categories + ~84 soap images in Storage).

CRITICAL count correction: the catalog is **28 products (13 soap + 10 scrub + 5 cream)**, NOT 68. ROADMAP/REQUIREMENTS say "68" but that referred to soap *images* (actual count ~84 jpgs across 13 folders). Every assertion targets **28 / 3**. (Verified: `grep -oE "category: '(soap|scrub|cream)'" client/src/data/products.ts` -> 13 soap / 10 scrub / 5 cream; 3 category entries.)
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-CONTEXT.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `client/src/data/catalog-data.ts` — exports `productMeta` (28 entries, each: `slug`, `name`, `subtitle`, `category`, `benefits[]`, `ingredients[]`, optional `tips[]`, `shelfLife`, `batchNote`), `categoryMeta` (3 entries: `slug`, `label`, `description`, `sortOrder`), `SLUG_TO_SOAP_FOLDER` map, and `BATCH_NOTE`. Contains NO `import.meta.*`. Re-exported by `products.ts` in Plan 02.
- `scripts/seed.ts` — service-role seed runner (`node --env-file=.env.seed.local scripts/seed.ts`).
- `scripts/verify-seed.ts` — anon-key verification of counts + published-only filter.

Consumed downstream by: Plan 02 (`products.ts` re-exports `catalog-data.ts`), Plan 02 read layer (`catalog.ts` reads the seeded rows / images).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract glob-free catalog metadata into client/src/data/catalog-data.ts</name>
  <files>client/src/data/catalog-data.ts</files>
  <read_first>
    - client/src/data/products.ts (FULL — the 28 product objects at lines 64-696, the `Category` type at line 18, the `categories` array at lines 41-60, the `BATCH_NOTE` constant at line 62, and the `import.meta.glob` block at lines 5-16 that must NOT be carried over)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md (section "client/src/data/catalog-data.ts (NEW — glob-free metadata)")
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md (Pattern 1 — including the SLUG_TO_SOAP_FOLDER table and the slug note: keep category-prefixed unique slugs verbatim)
  </read_first>
  <action>
    Create `client/src/data/catalog-data.ts` as a pure data module with NO `import.meta.*` (so both Node and Vite can import it — RESEARCH Pitfall 2; the seed imports THIS file, never products.ts).

    Export, all named exports, camelCase per project conventions:
    - `BATCH_NOTE` — the string constant copied verbatim from products.ts:62 ("Freshly handmade in small batches.").
    - `categoryMeta` — array of 3 objects, each `{ slug, label, description, sortOrder }`. Use the existing `categories` data (products.ts:41-60): soap/Soaps (sortOrder 0), scrub/Scrubs (sortOrder 1), cream/Creams (sortOrder 2). Carry the existing `description` strings verbatim. Do NOT include the `image` field (it referenced bundled PNGs via import — out of this module's scope; the showcase image is handled in Plan 03).
    - `SLUG_TO_SOAP_FOLDER` — `Record<string,string>` mapping each soap product slug -> its disk folder name under `client/src/assets/images/products/Soap/`. The 13 soap slugs and their folders: `soap-neem`->`Neem`, `soap-turmeric`->`Turmeric`, `soap-aloe-vera`->`AloeVera`, `soap-multani-mitti`->`Fuller_s Earth`, `soap-orange-peel`->`Orange`, `soap-sandalwood`->`Sandalwood`, `soap-charcoal`->`Charcoal`, `soap-rose`->`Rose`, `soap-lemon-peel`->`Lemon`, `soap-rice`->`Rice`, `soap-milk`->`Milk`, `soap-oats`->`Oats`, `soap-coffee`->`Coffee`. (Verified folders on disk match.)
    - `productMeta` — array of all 28 product objects copied verbatim from products.ts:64-696, with two changes per object: (a) DROP the `images` field entirely (images come from fs at seed time / Storage at read time); (b) RENAME the field key `id` to `slug` keeping the value verbatim (e.g. `soap-neem` stays `soap-neem` — these are globally unique, RESEARCH Pattern 1 / A2; do NOT collapse to bare `neem`). Keep `name`, `subtitle`, `category`, `benefits`, `ingredients`, optional `tips`, `shelfLife`, `batchNote` exactly as-is.

    Re-export the `Category` type alias (`'soap' | 'scrub' | 'cream'`) from this module so the seed and Plan 02 can import it. Do NOT seed any `price` here — price handling is decided at seed time (null, D-09).

    This module must NOT import any image asset and must NOT call `import.meta.glob`. It is plain data + maps only.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node -e "const m = require('child_process').execSync('node --input-type=module -e \"import(process.cwd()+ String.fromCharCode(47) +\\\"client/src/data/catalog-data.ts\\\").then(x=>{const ok = x.productMeta.length===28 && x.categoryMeta.length===3 && Object.keys(x.SLUG_TO_SOAP_FOLDER).length===13; if(!ok){console.error('FAIL counts', x.productMeta.length, x.categoryMeta.length, Object.keys(x.SLUG_TO_SOAP_FOLDER).length); process.exit(1);} console.log('OK 28/3/13');})\"'); process.stdout.write(m)"</automated>
    <human-check>Open catalog-data.ts and confirm no `import.meta` token appears (grep '`import.meta`' returns nothing) and that productMeta has 28 entries.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "import.meta" client/src/data/catalog-data.ts` returns 0 (no Vite-only macro).
    - `productMeta` has exactly 28 entries; counted by category: 13 with `category: 'soap'`, 10 `'scrub'`, 5 `'cream'`.
    - `categoryMeta` has exactly 3 entries with `slug` in {soap, scrub, cream} and `sortOrder` 0/1/2 respectively.
    - `SLUG_TO_SOAP_FOLDER` has exactly 13 keys, each value matching an existing directory under `client/src/assets/images/products/Soap/`.
    - Each `productMeta` entry has a `slug` field (no `id` field) and NO `images` field.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>catalog-data.ts exists, is glob-free, importable by Node, and exposes 28 products / 3 categories / 13 soap-folder mappings with verbatim metadata and globally-unique category-prefixed slugs.</done>
</task>

<task type="auto">
  <name>Task 2: Write the idempotent service-role seed (scripts/seed.ts) and the anon verify script (scripts/verify-seed.ts)</name>
  <files>scripts/seed.ts, scripts/verify-seed.ts</files>
  <read_first>
    - scripts/verify-skeleton.ts (FULL — the exact env-guard, client-construction, main()/process.exit, and `main().catch(...)` idiom to mirror)
    - scripts/check-no-secret.sh (the existing build-scan guard that must continue to pass — the seed must never be importable by client code)
    - client/src/data/catalog-data.ts (the metadata + SLUG_TO_SOAP_FOLDER created in Task 1 — the seed imports this)
    - supabase/migrations/0001_init_schema.sql (the exact products + categories columns the seed writes: products = slug, name, subtitle, category_id, price numeric(10,2) nullable, benefits[], ingredients[], tips[], shelf_life, batch_note, images[], is_active default true; categories = slug, label, description, sort_order; products.slug + categories.slug are UNIQUE)
    - supabase/migrations/0003_storage_buckets.sql (confirms bucket id `product-images`, public-read + admin-write — seed uploads here via service-role)
    - .env.example (documents SUPABASE_SERVICE_ROLE_KEY as a non-VITE_ runtime-only secret)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md (Patterns 2, 3, 4; Pitfalls 3 spaces/parens in filenames, 4 contentType gotcha, 6 deterministic order)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md (sections for scripts/seed.ts and scripts/verify-seed.ts)
  </read_first>
  <action>
    Create `scripts/seed.ts`, run as `node --env-file=.env.seed.local scripts/seed.ts` (native Node 22 — no tsx, no dotenv; mirrors verify-skeleton.ts structure).

    Env guard (copy verify-skeleton.ts:18-24, swap to service-role): read `process.env.SUPABASE_URL` and `process.env.SUPABASE_SERVICE_ROLE_KEY` (both non-`VITE_`); if either missing, `console.error('FAIL: set SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY')` and `process.exit(1)`. Construct `createClient(url, serviceKey, { auth: { persistSession: false } })`.

    Import `productMeta`, `categoryMeta`, `SLUG_TO_SOAP_FOLDER` from `../client/src/data/catalog-data.ts`. Use `node:fs` (`readFileSync`, `readdirSync`) and `node:path` (`join`).

    Seed order (RESEARCH Pattern 4 — categories first, products reference category_id):
    1. Upsert categories: `admin.from('categories').upsert(categoryMeta.map(c => ({ slug: c.slug, label: c.label, description: c.description, sort_order: c.sortOrder })), { onConflict: 'slug' }).select('id, slug')`. Build a `slug -> id` map from the returned rows.
    2. For each soap product (category === 'soap'), resolve its folder via `SLUG_TO_SOAP_FOLDER[slug]`, `readdirSync` the folder under `client/src/assets/images/products/Soap/<folder>`, filter `.jpg`, `.sort()` for stable display order (D-03). For each file, upload to Storage path `products/${slug}/${file}` via `admin.storage.from('product-images').upload(path, readFileSync(join(dir, file)), { contentType: 'image/jpeg', upsert: true })`. MANDATORY: pass explicit `contentType: 'image/jpeg'` (Pitfall 4 — Buffer uploads default to application/json). Collect `data.path` from each successful upload into a `string[]` keyed by slug. On upload error, throw.
    3. Scrub and cream products: no disk images -> `images: []` (D-03 placeholder substitution happens at render time in Plan 03).
    4. Upsert products: `admin.from('products').upsert(productMeta.map(p => ({ slug: p.slug, name: p.name, subtitle: p.subtitle, category_id: catId[p.category], price: null, benefits: p.benefits, ingredients: p.ingredients, tips: p.tips ?? [], shelf_life: p.shelfLife, batch_note: p.batchNote, images: imagePathsBySlug[p.slug] ?? [] })), { onConflict: 'slug' })`. Note `price: null` for EVERY product (D-09) and the snake_case column names `category_id`, `shelf_life`, `batch_note`, `is_active` (leave is_active to its default true). On error, throw.

    Wrap in `main()` that logs a summary (`console.log` count of categories upserted, products upserted, images uploaded) and `process.exit(0)` on success; mirror the `main().catch(err => { console.error(...); process.exit(1); })` tail from verify-skeleton.ts.

    Create `scripts/verify-seed.ts` (EXACT structural copy of verify-skeleton.ts, ANON key). Run as `node --env-file=.env.seed.local scripts/verify-seed.ts` (the seed env file also carries SUPABASE_ANON_KEY, OR pass it inline). Env guard reads `process.env.SUPABASE_URL` + `process.env.SUPABASE_ANON_KEY`. In `main()`:
    - Anon-select count products: `supabase.from('products').select('slug', { count: 'exact', head: true })`; assert `count === 28` else `console.error('FAIL: expected 28 products, got ' + count)` and exit 1.
    - Anon-select count categories: assert `count === 3` else exit 1.
    - Published-only check (PUB-02): the script needs a service-role client too (read `process.env.SUPABASE_SERVICE_ROLE_KEY`; if present, run the check, else skip with a logged note). Pick one seeded product slug, flip it `is_active=false` via service-role update, then anon-select that slug filtered `.eq('is_active', true)` and assert it returns 0 rows; then restore `is_active=true` via service-role. This proves the published-only filter PUB-02 will rely on.
    - On all-pass: `console.log('PASS: 28 products / 3 categories; is_active=false row hidden from anon')` and `process.exit(0)`. Mirror the `.catch` tail.

    Do NOT import either script from any client-bundled code. Do NOT add tsx/dotenv/vitest to package.json.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node --check scripts/seed.ts 2>/dev/null || node --experimental-strip-types --check scripts/seed.ts</automated>
    <automated>bash scripts/check-no-secret.sh</automated>
    <human-check>Confirm scripts/seed.ts passes contentType 'image/jpeg' on every upload and price: null on every product upsert; confirm onConflict: 'slug' on both upserts.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "contentType" scripts/seed.ts` >= 1 and the value is `'image/jpeg'` on the storage upload.
    - `grep -c "onConflict" scripts/seed.ts` == 2 (categories upsert + products upsert), each `'slug'`.
    - `grep -c "price: null" scripts/seed.ts` >= 1 (D-09 — no zero/placeholder).
    - seed.ts imports from `../client/src/data/catalog-data` and NOT from `../client/src/data/products` (no `import.meta.glob` crash).
    - seed.ts reads `SUPABASE_SERVICE_ROLE_KEY` from `process.env` and contains no `VITE_` prefix and no hardcoded key.
    - `verify-seed.ts` asserts `=== 28` (products) and `=== 3` (categories) and contains an `is_active` published-only assertion.
    - `npm run check` exits 0; `bash scripts/check-no-secret.sh` prints PASS (no service_role token in dist/).
  </acceptance_criteria>
  <done>seed.ts upserts categories then products on slug with price null and explicit-contentType soap image uploads; verify-seed.ts asserts 28/3 + published-only via anon. Both compile, neither leaks into the bundle.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task: Owner supplies the Supabase service-role key into a gitignored .env.seed.local</name>
  <action>This is a human-action checkpoint — Claude cannot retrieve the service-role secret. Pause and have the owner create `.env.seed.local` with SUPABASE_URL, SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY (see how-to-verify), confirm it is gitignored, then resume. No Claude-executable work in this step.</action>
  <what-built>scripts/seed.ts (idempotent service-role seed) and scripts/verify-seed.ts (anon count + published-only verification) are written and type-check clean. They need the service-role key, which only the project owner can retrieve from the Supabase dashboard, and which must never be committed.</what-built>
  <decision>Supply the Supabase service-role key locally so the seed can run.</decision>
  <context>
The service-role key bypasses RLS and is a full-access secret (SECURITY: never commit, never VITE_-prefix, never ship). `.env*.local` is already gitignored (verified). The seed reads the key at runtime via `node --env-file`. This is the only blocking external input for the phase (RESEARCH Environment Availability).
  </context>
  <how-to-verify>
1. Create a gitignored file `.env.seed.local` in the repo root containing three lines (no quotes, no trailing spaces):
   - `SUPABASE_URL=https://wfbnrcnmpcqzeyjlfflv.supabase.co` (Dashboard -> Project Settings -> API -> Project URL; confirm the ref matches your project)
   - `SUPABASE_ANON_KEY=...` (Dashboard -> Project Settings -> API -> Project API keys -> anon/public — same value already in `.env.local` as VITE_SUPABASE_ANON_KEY)
   - `SUPABASE_SERVICE_ROLE_KEY=...` (Dashboard -> Project Settings -> API -> Project API keys -> service_role / secret)
2. Confirm the file is gitignored: `git check-ignore .env.seed.local` prints the filename (it is covered by `.env*.local`).
3. Type "ready" so the executor can run the seed.
  </how-to-verify>
  <resume-signal>Type "ready" once .env.seed.local exists with all three values and is gitignored.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the seed, verify 28/3 counts + idempotency + published-only filter</name>
  <files>scripts/seed.ts, scripts/verify-seed.ts</files>
  <read_first>
    - scripts/seed.ts (the runner created in Task 2)
    - scripts/verify-seed.ts (the verification created in Task 2)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md (section "Running the seed" + Pitfall 1 — assert 28, never 68)
  </read_first>
  <action>
    Execute the seed against the live project, then prove convergence and the published-only invariant. No code changes in this task unless a run surfaces a bug (if so, fix in seed.ts/verify-seed.ts and re-run).

    1. First run: `node --env-file=.env.seed.local scripts/seed.ts`. Expect a success summary (3 categories, 28 products, ~84 soap images uploaded) and exit 0.
    2. Verify: `node --env-file=.env.seed.local scripts/verify-seed.ts`. Expect PASS: 28 products / 3 categories / is_active=false row hidden from anon, exit 0.
    3. Idempotency: run the seed a SECOND time, then verify-seed again. Counts MUST stay at 28 products / 3 categories (upsert on slug converges — RESEARCH Pattern 4, DATA-03 success criterion #2). RLS stays enabled throughout (the seed uses service-role; it never disables RLS).
    4. Sanity-check Storage: confirm via the verify output (or a one-off anon read) that soap rows carry non-empty `images[]` paths under `products/{slug}/` and scrub/cream rows carry `images: []`.

    If any count is not 28/3, do NOT adjust the target to match — the target is fixed at 28/3 (Pitfall 1). Diagnose the seed instead.
  </action>
  <verify>
    <automated>node --env-file=.env.seed.local scripts/seed.ts</automated>
    <automated>node --env-file=.env.seed.local scripts/verify-seed.ts</automated>
    <automated>node --env-file=.env.seed.local scripts/seed.ts && node --env-file=.env.seed.local scripts/verify-seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - First `scripts/seed.ts` run exits 0 with a summary reporting 3 categories + 28 products + soap images uploaded.
    - `scripts/verify-seed.ts` exits 0 and prints PASS with counts 28 products / 3 categories.
    - Second consecutive seed + verify still reports 28 / 3 (idempotent — no duplicates).
    - The published-only assertion passes (an `is_active=false` row is absent from an anon `.eq('is_active', true)` select; the row is restored to active afterward).
    - No step references or asserts "68".
  </acceptance_criteria>
  <done>The live Supabase project holds exactly 28 products and 3 categories with soap Storage paths recorded and scrub/cream images empty; re-running converges; anon read works and the is_active filter hides drafts. DATA-03 satisfied; PUB-02 filter behavior proven at the data layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local seed script -> Supabase (service-role) | Privileged write path; runs only on the owner's machine, never in the browser. The service-role key bypasses admin-only write RLS. |
| anonymous browser -> Supabase (anon key) | Untrusted public read path; only public-read RLS policies apply. Verified here via verify-seed.ts (anon select). |
| repository / build output (dist/) | Anything VITE_-prefixed or imported by client code is inlined into the public bundle. The seed + service-role key must stay out of it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure / Elevation | service_role key in seed.ts | mitigate | Key read from non-`VITE_` `process.env.SUPABASE_SERVICE_ROLE_KEY` supplied via gitignored `.env.seed.local`; seed.ts never imported by client code; `bash scripts/check-no-secret.sh` (Phase 1) scans dist/ for `service_role` and fails the build if present. (HIGH if violated) |
| T-02-02 | Information Disclosure | committed secrets | mitigate | `.env*.local` already gitignored (verified `.gitignore` line); checkpoint instructs `git check-ignore .env.seed.local` before running. |
| T-02-03 | Information Disclosure | unpublished/draft rows reachable by anon | mitigate | verify-seed.ts flips one row `is_active=false` and asserts it is absent from an anon `.eq('is_active', true)` select — proving the PUB-02 server-side filter the read layer (Plan 02) will rely on. (HIGH if violated) |
| T-02-04 | Tampering | anon write attempt to catalog tables | accept | Already mitigated by Phase 1 RLS (catalog tables are admin-write-only via `private.is_admin()`); no new work this phase. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this phase (`@supabase/supabase-js` already installed + slopcheck `[OK]` per RESEARCH Package Legitimacy Audit; seed runs on native Node 22 — no tsx/dotenv). No install task, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run check` exits 0 after each task (tsc is the only automated type gate).
- `bash scripts/check-no-secret.sh` prints PASS (no service_role token in dist/).
- `node --env-file=.env.seed.local scripts/seed.ts` exits 0; re-run converges.
- `node --env-file=.env.seed.local scripts/verify-seed.ts` prints PASS (28 products / 3 categories; is_active=false hidden from anon).
- Wave 0 validation artifact `scripts/verify-seed.ts` created and exercised (per 02-VALIDATION.md).
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion #1: seed inserts all 28 products + 3 categories, uploads soap images, records Storage paths, scrub/cream seeded empty.
- ROADMAP Phase 2 success criterion #2: seed idempotent (upsert on slug) — re-run yields 28 rows, RLS stays enabled.
- DATA-03 satisfied; PUB-02 server-side `is_active` filter behavior proven at the data layer.
- Service-role key never enters the bundle (check-no-secret.sh PASS); never committed (.env.seed.local gitignored).
</success_criteria>

<output>
Create `.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-01-SUMMARY.md` when done.
</output>
