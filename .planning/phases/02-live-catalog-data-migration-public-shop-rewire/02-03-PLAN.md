---
phase: 02-live-catalog-data-migration-public-shop-rewire
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - client/src/components/ProductCard.tsx
  - client/src/components/ProductDetail.tsx
  - client/src/components/ProductGrid.tsx
  - client/src/pages/Shop.tsx
  - client/src/pages/Home.tsx
autonomous: true

requirements: [PUB-01, PUB-02]

must_haves:
  truths:
    - "The Shop renders live products and categories from Supabase with correct per-category tabs and counts"
    - "The Home featured grid shows the first published product per category, deterministically ordered, always 3 cards"
    - "The product detail modal renders entirely from Supabase data (only is_active=true products are reachable)"
    - "Loading shows skeleton cards mirroring the grid (no layout shift); error shows an inline message + working Retry; empty shows friendly copy"
    - "Every price renders through formatPrice (null -> 'Price on request', set -> ₹ whole rupees) — no raw {product.price} remains"
    - "Soap images load from Storage; scrub/cream show the bundled category placeholder; no static products.ts data array is read at runtime"
  artifacts:
    - path: "client/src/pages/Shop.tsx"
      provides: "Live Shop with category tabs/counts, loading/empty/error states, detail modal"
      contains: "useProducts"
    - path: "client/src/components/ProductGrid.tsx"
      provides: "Live featured grid (first published per category, sort_order)"
      contains: "useProducts"
    - path: "client/src/components/ProductCard.tsx"
      provides: "Card rendering live image + formatPrice"
      contains: "formatPrice"
    - path: "client/src/components/ProductDetail.tsx"
      provides: "Detail modal rendering live data + formatPrice"
      contains: "formatPrice"
  key_links:
    - from: "client/src/pages/Shop.tsx"
      to: "client/src/lib/catalog.ts"
      via: "useProducts() + useCategories()"
      pattern: "useProducts|useCategories"
    - from: "client/src/components/ProductCard.tsx"
      to: "client/src/lib/format.ts"
      via: "formatPrice(product.price)"
      pattern: "formatPrice\\("
    - from: "client/src/pages/Shop.tsx"
      to: "shadcn Skeleton"
      via: "loading-state skeleton grid"
      pattern: "Skeleton"
---

<objective>
Complete the **public read-path rewire**: Shop, Home, ProductGrid, ProductCard, and ProductDetail now consume the live `catalog.ts` hooks and `formatPrice` from Plan 02 (instead of the static `products.ts` array), with working loading (skeleton), empty, and error+retry states and no UX regression. After this plan a real anonymous visitor sees the entire catalog rendered live from Supabase — soap photos from Storage, scrub/cream behind category placeholders, "Price on request" everywhere — proving the no-redeploy promise end to end.

Purpose: Completes PUB-01 (Shop/Home read live data) and PUB-02 (detail renders from Supabase, only published products shown). Removes the static `products.ts` data array from the runtime read path (ROADMAP success criterion #5).

Output: 5 modified component/page files reading live data. No new files. Depends on Plan 01 (seeded data to render) and Plan 02 (the hooks + formatPrice + adapted Product type).

CRITICAL: route EVERY price render through `formatPrice` (Pitfall 5) — no raw `{product.price}` may remain. The seed wrote null for all prices, so the live site shows "Price on request" everywhere until the admin sets prices in Phase 4.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-CONTEXT.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md
@.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan modifies (no new symbols, consumes Plan 02's):
- `client/src/components/ProductCard.tsx` — imports adapted `Product` type + `formatPrice`; renders `formatPrice(product.price)` at the price line.
- `client/src/components/ProductDetail.tsx` — imports `Product` + `formatPrice`; renders `formatPrice(product.price)`; carousel unchanged (single placeholder keeps hasMany false).
- `client/src/components/ProductGrid.tsx` — uses `useProducts()` + `useCategories()`, derives featured (first published per category by sort_order), with loading skeleton.
- `client/src/pages/Shop.tsx` — uses `useProducts()` + `useCategories()`; tabs/counts/filter from live data; loading/empty/error+retry states.
- `client/src/pages/Home.tsx` — uses `useCategories()` for the Category Showcase.

After this plan the static `products`/`getFeaturedProducts`/`getProductsByCategory` exports in `products.ts` are dead code (no runtime consumer).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Route price + type through Plan 02 contracts in ProductCard + ProductDetail</name>
  <files>client/src/components/ProductCard.tsx, client/src/components/ProductDetail.tsx</files>
  <read_first>
    - client/src/components/ProductCard.tsx (FULL — type import line 1, image render line 21 `product.images[0]`, raw price render line 42 `{product.price}`)
    - client/src/components/ProductDetail.tsx (FULL — type import line 2, carousel lines 30-34 over `product.images`, `hasMany` line 31, raw price render lines 96-98 `{product.price}`)
    - client/src/lib/format.ts (formatPrice — the single price render path from Plan 02)
    - client/src/lib/catalog.ts (productImageUrls already resolved images to URLs in the mapper, so `product.images[0]` is a usable src)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md (sections "ProductCard.tsx" and "ProductDetail.tsx" — minimal self-edits; D-03 hasMany note)
  </read_first>
  <action>
    These are minimal presentational edits — the live `Product` shape (from catalog.ts mapper) is component-compatible, so only the price render path and (optionally) the type import source change.

    ProductCard.tsx:
    - Import `formatPrice` from `@/lib/format`. Keep importing the `Product` type from `@/data/products` (it now resolves to `price: number | null` after Plan 02). 
    - Line 42: replace `{product.price}` with `{formatPrice(product.price)}` (D-01/D-02 via the single formatter — Pitfall 5). No other change; `product.images[0]` (line 21) already holds a resolved URL (soap Storage URL or category placeholder).

    ProductDetail.tsx:
    - Import `formatPrice` from `@/lib/format`. Keep the `Product` type import from `@/data/products`.
    - Lines 96-98: replace `{product.price}` with `{formatPrice(product.price)}`.
    - Do NOT change the carousel logic: `images = product.images`, `hasMany = images.length > 1` (line 31) stays — for scrub/cream the mapper returns exactly one placeholder so `hasMany` is false and arrows/dots correctly hide (D-03, no logic change needed).

    No raw `{product.price}` may remain in either file.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node -e "for(const f of ['client/src/components/ProductCard.tsx','client/src/components/ProductDetail.tsx']){const s=require('fs').readFileSync(f,'utf8'); if(/\{\s*product\.price\s*\}/.test(s)){console.error('FAIL raw {product.price} remains in '+f);process.exit(1)} if(!/formatPrice\(/.test(s)){console.error('FAIL formatPrice not used in '+f);process.exit(1)}} console.log('OK ProductCard + ProductDetail route price through formatPrice')"</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains a raw `{product.price}` (grep-verified); both call `formatPrice(product.price)`.
    - Both import `formatPrice` from `@/lib/format`.
    - ProductDetail carousel logic is unchanged (hasMany derives from images.length > 1).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>ProductCard and ProductDetail render price exclusively through formatPrice and consume the adapted live Product shape; carousel handles the single-placeholder case correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire Shop.tsx to live data with loading / empty / error+retry states</name>
  <files>client/src/pages/Shop.tsx</files>
  <read_first>
    - client/src/pages/Shop.tsx (FULL — static imports lines 6-11, category init lines 19-26, filter lines 29-31, count helpers lines 42-43/70/82, grid line 88, existing empty-state pattern lines 98-102 to REUSE)
    - client/src/lib/catalog.ts (useProducts/useCategories — data, isLoading, isError, refetch; products already published-filtered + ordered)
    - client/src/components/ui/skeleton.tsx (the shadcn Skeleton to reuse for loading cards — do NOT re-add)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-CONTEXT.md (D-05 skeleton no-layout-shift; D-06 inline error + Retry via refetch; D-07 reuse "No products found in this category." + a globally-empty variant)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md (Pattern 9 loading/empty/error states — skeleton must mirror grid columns)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md (section "client/src/pages/Shop.tsx")
  </read_first>
  <action>
    Replace the static `products`/`categories`/`type` imports (lines 6-11) with `useProducts`/`useCategories` from `@/lib/catalog` and the `Product`/`Category` types from `@/data/products`.

    Call both hooks. Destructure `products`/`isLoading`/`isError`/`refetch` from `useProducts()` and `categories` from `useCategories()`.

    Keep the existing category-tab UI and `activeCategory` state. Derive everything from live data:
    - Category init (lines 19-26): validate `params.category` against the live `categories` slugs (`categories?.some(c => c.id === params.category)`).
    - Filter (lines 29-31): `activeCategory ? products.filter(p => p.category === activeCategory) : products` over the live array (already server-side published-filtered — no client-side is_active hiding).
    - Counts (lines 42-43, 70, 82): derive `categoryCount` and the all-tab `(products.length)` from the live `products`.

    Add states (RESEARCH Pattern 9, D-05/06/07) BEFORE rendering the grid; guard for both queries:
    - Loading (`isLoading`): render a skeleton grid using the SAME classes as line 88 (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8`) with ~8 placeholder cards, each a `<Skeleton>` for the square image + a `<Skeleton>` for the title — mirroring the card layout so there is no layout shift (D-05). Keep the header/tabs visible or show a tabs skeleton; no full-page blank.
    - Error (`isError`): inline centered friendly message ("We couldn't load the collection. Please try again." — wording is discretion, D-06) + a Retry button calling `refetch()`. Keep the user on the page; do NOT render an empty grid.
    - Empty: per-category empty -> REUSE the exact existing pattern `<p className="text-center text-foreground/50 py-16">No products found in this category.</p>` (lines 98-102, D-07). Add a globally-empty variant ("No products available yet." or similar) when the whole live catalog is empty.

    Keep the ProductDetail modal wiring and the Instagram CTA section unchanged. Do NOT import anything from the static data array path (`products`/`getProductsByCategory`).
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node -e "const s=require('fs').readFileSync('client/src/pages/Shop.tsx','utf8'); const hooks=/useProducts\b/.test(s)&&/useCategories\b/.test(s); const states=/isLoading/.test(s)&&/isError/.test(s)&&/refetch/.test(s); const skel=/Skeleton/.test(s); const empty=/No products found in this category/.test(s); const noStatic=!/from ['\"]@\/data\/products['\"][^;]*\bproducts\b/.test(s)||!/\bproducts,\s*\n/.test(s); if(!hooks){console.error('FAIL hooks');process.exit(1)} if(!states){console.error('FAIL loading/error/refetch states');process.exit(1)} if(!skel){console.error('FAIL skeleton loading');process.exit(1)} if(!empty){console.error('FAIL reused empty-state copy');process.exit(1)} console.log('OK Shop live data + states')"</automated>
  </verify>
  <acceptance_criteria>
    - Shop.tsx imports `useProducts` and `useCategories` from `@/lib/catalog` and no longer imports the `products`/`categories` data values from `@/data/products` (type-only imports from there are fine).
    - Tabs, counts, and the all-tab total derive from live `products`/`categories`.
    - A loading branch renders shadcn `Skeleton` cards in a grid matching the products grid classes (no layout shift).
    - An error branch renders an inline message + a Retry button calling `refetch()`.
    - The per-category empty copy `No products found in this category.` is preserved; a global-empty variant exists.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>Shop reads live published products/categories with skeleton loading, inline error+retry, and both empty variants; no static data array consumed; UX parity with the prior static Shop.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire Home.tsx + ProductGrid.tsx to live data (featured + category showcase) and confirm static array is off the runtime path</name>
  <files>client/src/components/ProductGrid.tsx, client/src/pages/Home.tsx</files>
  <read_first>
    - client/src/components/ProductGrid.tsx (FULL — `getFeaturedProducts()` line 8, featured grid line 24 `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`, detail modal wiring)
    - client/src/pages/Home.tsx (FULL — `import { categories } from "@/data/products"` line 5, Category Showcase map lines 53-76 using cat.id/cat.image/cat.label/cat.description)
    - client/src/lib/catalog.ts (useProducts/useCategories; toCategory provides `image` = bundled placeholder for showcase tiles)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-CONTEXT.md (D-08 featured = first published per category by sort_order, deterministic, always 3 cards)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-RESEARCH.md (Pattern 8 featured derivation; Pitfall 6 ordering drift — sort categories by sort_order, products already .order('slug'))
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-PATTERNS.md (sections "Home.tsx" and "ProductGrid.tsx")
  </read_first>
  <action>
    ProductGrid.tsx:
    - Replace `getFeaturedProducts()` (line 8) with live derivation. Call `useProducts()` and `useCategories()`. Derive featured (RESEARCH Pattern 8, D-08): sort `categories` by `sortOrder`, then for each category pick the first product whose `category === category.id` from the already-`slug`-ordered `products` array; filter out undefined. This yields the first published product per category, deterministically, always (up to) 3 cards.
    - Loading: while `isLoading`, render a skeleton grid mirroring line 24's classes (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8`) with 3 skeleton cards (D-05). On error, keep it graceful (render nothing or a small inline note — Home featured is secondary; the Shop owns the prominent error+retry). Do not crash on empty.
    - Keep the heading, "View All Products" link, and the ProductDetail modal wiring unchanged.

    Home.tsx:
    - Replace `import { categories } from "@/data/products"` (line 5) with `useCategories()` from `@/lib/catalog`. The Category Showcase map (lines 53-76) uses `cat.id`, `cat.image`, `cat.label`, `cat.description` — the live `CategoryInfo` from `toCategory` supplies all four (`image` = bundled category placeholder per Plan 02). While categories load, render the existing static section chrome and either a skeleton or the tiles once data arrives; do not block the rest of the page.
    - Keep all other Home sections (Hero, philosophy, Why Sutravan, promise, CTA) unchanged.

    After these edits, the static `products`/`getFeaturedProducts`/`getProductsByCategory`/`categories` data exports in `products.ts` have NO runtime consumer (ROADMAP success criterion #5). Confirm by grepping the component/page tree for value-imports of those symbols.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node -e "const fs=require('fs'); const pg=fs.readFileSync('client/src/components/ProductGrid.tsx','utf8'); const home=fs.readFileSync('client/src/pages/Home.tsx','utf8'); if(/getFeaturedProducts/.test(pg)){console.error('FAIL ProductGrid still calls getFeaturedProducts');process.exit(1)} if(!/useProducts\b/.test(pg)||!/useCategories\b/.test(pg)){console.error('FAIL ProductGrid hooks');process.exit(1)} if(!/useCategories\b/.test(home)){console.error('FAIL Home useCategories');process.exit(1)} if(/from ['\"]@\/data\/products['\"]/.test(home)&&/\bcategories\b/.test(home.split('\\n')[4]||'')){console.error('FAIL Home still imports static categories value');process.exit(1)} console.log('OK Home + ProductGrid live')"</automated>
    <automated>bash -c "grep -RInE \"getFeaturedProducts|getProductsByCategory\" client/src/components client/src/pages | grep -v -E '\\.test\\.' || echo 'NO_RUNTIME_CONSUMERS'"</automated>
  </verify>
  <acceptance_criteria>
    - ProductGrid.tsx no longer calls `getFeaturedProducts()`; it derives featured from `useProducts()`/`useCategories()` (first published per category by sortOrder, always up to 3 cards) and has a loading skeleton.
    - Home.tsx imports `useCategories()` and no longer value-imports `categories` from `@/data/products`.
    - The Category Showcase still renders label/description/image per live category.
    - Grep across `client/src/components` + `client/src/pages` finds no runtime call to `getFeaturedProducts`/`getProductsByCategory` (static array off the runtime path — success criterion #5).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>Home and the featured grid render live categories/products with deterministic featured selection and loading skeletons; the static products.ts data array has no remaining runtime consumer.</done>
</task>

<task type="auto">
  <name>Task 4: End-of-phase parity walkthrough (production build + manual UX verification)</name>
  <files>client/src/pages/Shop.tsx, client/src/pages/Home.tsx, client/src/components/ProductGrid.tsx</files>
  <read_first>
    - client/src/pages/Shop.tsx, client/src/pages/Home.tsx, client/src/components/ProductGrid.tsx (the rewired surfaces — confirm no static data-array value imports remain)
    - .planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-VALIDATION.md (Manual-Only Verifications + the phase-gate sampling row)
  </read_first>
  <action>
    No code changes unless verification surfaces a defect (fix in the relevant rewired file and re-verify).

    Run the full automated gate, then perform the manual UX parity walkthrough below. `workflow.human_verify_mode` is end-of-phase, so this manual check is expressed as a `<human-check>` (NOT a blocking checkpoint) and is the phase's UX verification gate — the executor runs `npm run dev`, the owner confirms parity, and any regression is fixed before the phase is marked verified.

    Automated: `npm run check` (tsc green) and `npm run build` (production bundle builds with live read path; static glob image bundling no longer needed). Optionally re-run `bash scripts/check-no-secret.sh` to confirm dist/ stays secret-free.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>npm run build</automated>
    <human-check>
Run `npm run dev`, open http://localhost:3200, and confirm (this is the end-of-phase UX gate):
1. Home: Category Showcase shows 3 categories (Soaps/Scrubs/Creams) with images; "Curated Essentials" shows exactly 3 featured cards (one per category), stable across reloads.
2. Shop (`/shop`): tabs read All Products (28), Soaps (13), Scrubs (10), Creams (5); switching tabs filters correctly; soap cards show real photos, scrub/cream show the category placeholder; every card price reads "Price on request".
3. Click a soap product: detail modal shows multiple images with working arrows/dots; click a scrub/cream: single placeholder image, NO arrows/dots; price reads "Price on request".
4. Loading/error: throttle devtools to Offline (or block the supabase request) and reload Shop — skeleton cards appear (no layout shift), then an inline error message with a Retry button; restore network and click Retry — products load.
5. Deep-link `/shop/soap` resolves to the Soaps tab; an unknown category falls back to All Products.
6. Parity: compare against the prior static catalog — same products, categories, ordering; no visual regression.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 and `npm run build` completes successfully against the live read path.
    - Manual walkthrough confirms 28/13/10/5 counts, "Price on request" on every product, soap photos from Storage, scrub/cream placeholders, working loading/error/retry, and detail-modal carousel behavior — no UX regression vs the static catalog.
  </acceptance_criteria>
  <done>The production build succeeds on the live read path and the owner confirms full UX parity with the prior static catalog plus correct loading/empty/error states — the phase's read-path deliverable is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser -> Supabase (anon key) | The only path active here; what renders is whatever the published-only query returns. |
| Supabase content -> rendered DOM | Catalog text + image URLs rendered to the public; image URLs come from getPublicUrl (Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | draft products visible on the public Shop/detail | mitigate | Components consume `useProducts()` whose query applies the server-side `.eq('is_active', true)` filter (Plan 02) — the UI never receives drafts, so it cannot leak them; there is no client-side "hide" toggle to bypass. (HIGH if violated) |
| T-02-07 | Denial of Service (UX) | Supabase fetch failure leaves a blank/broken Shop | mitigate | Error branch renders an inline message + Retry (refetch) rather than a silent empty grid (D-06); loading renders skeletons (D-05) — the user is never stranded. |
| T-02-08 | Tampering | malformed/absent image path renders a broken tile | mitigate | Empty `images[]` falls back to a bundled category placeholder (productImageUrls, Plan 02); soap URLs are getPublicUrl-encoded — no broken `<img>` for the expected data shapes. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (shadcn Skeleton already present; no install). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `npm run check` exits 0 after each task.
- No raw `{product.price}` remains in any component (grep-verified); all prices via formatPrice.
- Shop/ProductGrid use `useProducts`/`useCategories`; Home uses `useCategories`.
- No runtime consumer of `getFeaturedProducts`/`getProductsByCategory` in components/pages (static array off the runtime path — success criterion #5).
- Manual end-of-phase human-verify checkpoint confirms parity + loading/error/empty behavior (human_verify_mode is end-of-phase; this is the phase's verification gate).
</verification>

<success_criteria>
- PUB-01: the public Shop and Home render live products/categories from Supabase via TanStack Query with working loading (skeleton), empty, and error (retry) states and no UX regression.
- PUB-02: the product detail view renders entirely from Supabase and only published (is_active=true) products are reachable.
- ROADMAP success criterion #5: the static `client/src/data/products.ts` data array is removed from the runtime read path (no remaining value-consumers).
- All prices render via formatPrice ("Price on request" until Phase 4 sets prices).
</success_criteria>

<output>
Create `.planning/phases/02-live-catalog-data-migration-public-shop-rewire/02-03-SUMMARY.md` when done.
</output>
