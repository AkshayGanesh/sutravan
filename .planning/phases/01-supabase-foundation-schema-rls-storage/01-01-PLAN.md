---
phase: 01-supabase-foundation-schema-rls-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vite.config.ts
  - .gitignore
  - .env.example
  - client/src/lib/queryClient.ts
  - client/src/lib/supabase.ts
  - scripts/verify-skeleton.ts
  - scripts/check-no-secret.sh
  - server/index.ts
  - server/routes.ts
  - server/static.ts
  - server/storage.ts
  - server/vite.ts
  - shared/schema.ts
  - drizzle.config.ts
  - script/build.ts
autonomous: true
requirements: [DATA-01, DATA-04]
must_haves:
  truths:
    - "App builds (npm run build exits 0) and dev-serves on port 3200 after the Express/Drizzle/Passport stack is deleted"
    - "The Supabase client singleton exists, reads only VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY, and throws at boot if either is missing"
    - "No server/, shared/schema.ts, drizzle.config.ts, or script/build.ts remain, and package.json has no express/passport/drizzle/pg deps"
    - "The secret-leak guard script finds no service_role token in the built bundle"
  artifacts:
    - path: "client/src/lib/supabase.ts"
      provides: "createClient singleton exported as `supabase`, VITE_ env-or-throw guard"
      exports: ["supabase"]
      contains: "createClient"
    - path: ".env.example"
      provides: "Documents VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY (no secrets)"
      contains: "VITE_SUPABASE_URL"
    - path: "scripts/check-no-secret.sh"
      provides: "Build-time guard: fails if service_role appears in dist/"
      contains: "service_role"
    - path: "scripts/verify-skeleton.ts"
      provides: "Walking-skeleton script: anon-key select on products against live project"
      contains: "from('products')"
    - path: "package.json"
      provides: "Supabase deps added, Express/Drizzle stack removed, dev script = vite dev --port 3200"
      contains: "@supabase/supabase-js"
  key_links:
    - from: "client/src/lib/supabase.ts"
      to: "import.meta.env.VITE_SUPABASE_URL"
      via: "createClient(url, anonKey)"
      pattern: "import\\.meta\\.env\\.VITE_SUPABASE_URL"
    - from: "client/src/App.tsx"
      to: "client/src/lib/queryClient.ts"
      via: "import { queryClient }"
      pattern: "import \\{ queryClient \\}"
---

<objective>
Convert the repo to clean Supabase-direct shape: delete the dead Express + Drizzle + Passport scaffolding (D-13), rewire build/dev config and TanStack Query (D-14, D-15), add the Supabase client runtime + CLI dev dependency, and create the Supabase client singleton plus the validation scaffolds (secret-leak guard + walking-skeleton script) that Plan 03 runs against the live project.

This is the foundation slice of the Walking Skeleton: after this plan the app still builds and dev-serves, but now imports a real `@supabase/supabase-js` client singleton wired to `VITE_` env. No user-facing behavior changes (the Shop keeps reading static `products.ts` until Phase 2).

Purpose: Satisfy DATA-04 (remove dead backend) and the client half of DATA-01 (env-based Supabase config, anon key in client; service-role key never `VITE_`-prefixed). Establish the secret-leak guard so success criterion #1 is verifiable.
Output: Slimmed `package.json` with `@supabase/supabase-js` + `supabase` CLI; `client/src/lib/supabase.ts`; `.env.example`; `scripts/check-no-secret.sh`; `scripts/verify-skeleton.ts`; cleaned `queryClient.ts`, `tsconfig.json`, `vite.config.ts`, `.gitignore`; deleted `server/`, `shared/schema.ts`, `drizzle.config.ts`, `script/build.ts`.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-supabase-foundation-schema-rls-storage/01-CONTEXT.md
@.planning/phases/01-supabase-foundation-schema-rls-storage/01-RESEARCH.md
@.planning/phases/01-supabase-foundation-schema-rls-storage/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the dead Express + Drizzle + Passport stack and rewire config (D-13, D-15)</name>
  <files>package.json, package-lock.json, tsconfig.json, vite.config.ts, server/index.ts, server/routes.ts, server/static.ts, server/storage.ts, server/vite.ts, shared/schema.ts, drizzle.config.ts, script/build.ts</files>
  <read_first>
    - package.json (current scripts lines 6-13, dependencies, devDependencies, optionalDependencies — exact removal list)
    - tsconfig.json (line 2 include array; lines 18-21 paths)
    - vite.config.ts (lines 12-18 resolve.alias)
    - .planning/phases/01-supabase-foundation-schema-rls-storage/01-RESEARCH.md §"Runtime State Inventory" Removal checklist (lines 294-299) and Anti-Patterns (line 268)
    - .planning/phases/01-supabase-foundation-schema-rls-storage/01-PATTERNS.md §package.json / tsconfig.json / vite.config.ts (lines 93-111)
    - .github/workflows/deploy.yml (confirms deploy uses `npx vite build`, not npm run build)
  </read_first>
  <action>
    Delete the directories/files: server/ (index.ts, routes.ts, static.ts, storage.ts, vite.ts), shared/schema.ts (and the shared/ directory if it becomes empty), drizzle.config.ts, script/build.ts (also remove the script/ directory if empty). Per RESEARCH Runtime State Inventory: nothing persistent exists (MemStorage was in-memory, no DB connected, no deployed server), so deletion cannot break runtime or deploy.
    In package.json: strip dependencies express, express-session, passport, passport-local, drizzle-orm, drizzle-zod, pg, connect-pg-simple, memorystore, ws, zod-validation-error. Strip devDependencies drizzle-kit, esbuild, tsx, @types/express, @types/express-session, @types/passport, @types/passport-local, @types/connect-pg-simple, @types/ws. Strip the optionalDependencies bufferutil block entirely. KEEP @tanstack/react-query, zod (react-hook-form resolvers use it), all @radix-ui/*, react, react-dom, wouter, framer-motion, and the rest of the client stack. Rewrite scripts: set "dev" to "vite dev --port 3200" (promote today's dev:client per D-15), delete the dev:client / start / db:push entries, set "build" to "vite build" (aligns with deploy.yml `npx vite build`), KEEP "check" as "tsc". Optionally add "verify:skeleton" running "tsx scripts/verify-skeleton.ts" only if tsx is retained — but since tsx is being removed, instead add "verify:skeleton" as "node --import tsx scripts/verify-skeleton.ts" is NOT valid without tsx; reference Plan 03 which uses npx for the one-off skeleton run, so do NOT add a script depending on tsx here.
    In tsconfig.json: change include from ["client/src/**/*", "shared/**/*", "server/**/*"] to ["client/src/**/*", "scripts/**/*"]; in compilerOptions.paths remove "@shared/*" and KEEP "@/*": ["./client/src/*"].
    In vite.config.ts: in resolve.alias remove the "@shared" entry; remove the "@assets" entry (verified: no client/src file imports the @assets alias — products.ts uses relative ../assets paths). KEEP the "@" alias and KEEP base: process.env.VITE_BASE_PATH ?? "/" (GitHub Pages sub-path mechanism).
    Run `npm install` after the edits to regenerate package-lock.json (this also removes the stripped packages from node_modules). Do NOT add the new Supabase packages in this task — that is Task 2.
  </action>
  <verify>
    <automated>test ! -d server && test ! -f shared/schema.ts && test ! -f drizzle.config.ts && test ! -f script/build.ts && ! grep -Eq '"(express|passport|passport-local|drizzle-orm|drizzle-kit|pg|connect-pg-simple|memorystore|express-session)"' package.json && grep -q '"dev": "vite dev --port 3200"' package.json && grep -q '"build": "vite build"' package.json && ! grep -q '@shared' tsconfig.json && ! grep -q '@shared' vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/` directory does not exist; `shared/schema.ts`, `drizzle.config.ts`, `script/build.ts` do not exist
    - `grep -E '"(express|passport|drizzle-orm|drizzle-kit|pg|connect-pg-simple|memorystore|express-session|ws)"' package.json` exits 1 (no match)
    - package.json scripts: `"dev": "vite dev --port 3200"`, `"build": "vite build"`, `"check": "tsc"`; no `start`, `db:push`, or `dev:client` keys remain
    - tsconfig.json `include` is `["client/src/**/*", "scripts/**/*"]`; `paths` has `@/*` and no `@shared/*`
    - vite.config.ts `resolve.alias` has `@` only (no `@shared`, no `@assets`); `base` line unchanged
    - `npm install` exits 0 and package-lock.json no longer references the stripped packages
  </acceptance_criteria>
  <done>The dead Express/Drizzle/Passport stack is fully removed, config files repoint to client-only, and the lockfile is regenerated. `npm run check` (tsc) still passes — confirmed in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Add Supabase deps, create the client singleton, .env.example, and the validation scaffolds (DATA-01)</name>
  <files>package.json, package-lock.json, client/src/lib/supabase.ts, client/src/lib/queryClient.ts, .env.example, .gitignore, scripts/check-no-secret.sh, scripts/verify-skeleton.ts</files>
  <read_first>
    - .planning/phases/01-supabase-foundation-schema-rls-storage/01-RESEARCH.md §"Standard Stack" (lines 30-61 — exact versions and install commands; do NOT use `npx --yes supabase`) and §Pattern 1 (lines 130-148, the singleton + env-or-throw)
    - .planning/phases/01-supabase-foundation-schema-rls-storage/01-PATTERNS.md §`client/src/lib/supabase.ts` (lines 27-55) and §queryClient.ts (lines 59-66) and §.gitignore/.env.example (lines 113-115)
    - client/src/lib/queryClient.ts (lines 1-57 — keep 44-57 QueryClient, delete 1-42 Express helpers, drop default queryFn line 47)
    - client/src/App.tsx (line 2 imports `{ queryClient }` — this import must keep working)
    - .gitignore (current 11 lines)
    - client/src/data/products.ts (top of file — confirms the `products` table column shape the skeleton select targets)
  </read_first>
  <action>
    Install the runtime client: `npm install @supabase/supabase-js@^2.106.2` (official package, [VERIFIED] in RESEARCH Package Legitimacy Audit — ~19.7M downloads/wk, no postinstall). Install the CLI as a devDependency: `npm install --save-dev supabase@^2.102.0` (official, [VERIFIED]). Do NOT run `npx --yes supabase`.
    Create client/src/lib/supabase.ts mirroring the queryClient.ts singleton pattern (one module-level named export) and the drizzle.config env-or-throw precedent, adapted to browser/Vite env: import createClient from '@supabase/supabase-js'; read const url = import.meta.env.VITE_SUPABASE_URL and const anonKey = import.meta.env.VITE_SUPABASE_ANON_KEY; throw new Error('Missing VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY') if either is falsy; export const supabase = createClient(url, anonKey). Use import.meta.env (NOT process.env) — this is browser code, matching App.tsx:15. Lift the exact code from RESEARCH Pattern 1.
    Edit client/src/lib/queryClient.ts (D-14): delete throwIfResNotOk, apiRequest, and getQueryFn (current lines 1-42) and their `fetch(... credentials: "include")` calls bound to the deleted Express server; keep only the `export const queryClient = new QueryClient({...})` construction, and remove the `queryFn: getQueryFn({ on401: "throw" })` default line so each Phase-2 Supabase query supplies its own queryFn. Keep the remaining defaultOptions (refetchInterval: false, refetchOnWindowFocus: false, staleTime: Infinity, retry: false). The named export `queryClient` consumed by App.tsx must remain.
    Create .env.example (committed) documenting only VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY with placeholder values and a comment that the service-role key is NEVER placed in a VITE_-prefixed var and never committed. Append to .gitignore: `.env.local` and `.env*.local` so the anon key and the local service-role key never commit.
    Create scripts/check-no-secret.sh: a bash guard that runs `npm run build` then `grep -r service_role dist/` and exits 1 if `service_role` (or a `service_role` JWT) is found in the built bundle, exits 0 otherwise. This backs success criterion #1 / threat T-01-SECRET.
    Create scripts/verify-skeleton.ts: the walking-skeleton script that imports createClient from '@supabase/supabase-js', reads SUPABASE_URL + SUPABASE_ANON_KEY from process.env (Node context, run with explicit env — NOT VITE_ prefixed since this is a script not the bundle), performs `const { data, error } = await supabase.from('products').select('id, slug').limit(1)`, and asserts error is null (an empty rows array is a PASS — proves public-read RLS allows the anon select with no RLS error). Print PASS/FAIL and exit non-zero on error. This script will FAIL until Plan 03 pushes the schema to a live project — that is expected (it is the failing-test-first artifact); Plan 03 runs it GREEN.
  </action>
  <verify>
    <automated>grep -q "@supabase/supabase-js" package.json && grep -q '"supabase"' package.json && grep -q "createClient" client/src/lib/supabase.ts && grep -q "import.meta.env.VITE_SUPABASE_URL" client/src/lib/supabase.ts && ! grep -q "apiRequest" client/src/lib/queryClient.ts && grep -q "queryClient" client/src/lib/queryClient.ts && grep -q "VITE_SUPABASE_URL" .env.example && ! grep -Eq "VITE_.*SERVICE|VITE_.*SECRET" .env.example && grep -q "service_role" scripts/check-no-secret.sh && grep -q "from('products')" scripts/verify-skeleton.ts && grep -q ".env.local" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/lib/supabase.ts` exports `supabase`, contains `createClient`, reads `import.meta.env.VITE_SUPABASE_URL` and `import.meta.env.VITE_SUPABASE_ANON_KEY`, and throws if either is missing
    - `client/src/lib/queryClient.ts` no longer contains `apiRequest`, `getQueryFn`, or `throwIfResNotOk`; still exports `queryClient`; no `queryFn:` default in defaultOptions
    - `.env.example` documents `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` and contains NO `VITE_`-prefixed var whose name contains `SERVICE` or `SECRET`
    - `.gitignore` contains `.env.local`
    - `scripts/check-no-secret.sh` greps `dist/` for `service_role` and exits non-zero on a match
    - `scripts/verify-skeleton.ts` calls `supabase.from('products').select(...)` and asserts `error === null`
    - package.json lists `@supabase/supabase-js` (dependencies) and `supabase` (devDependencies); neither declares a postinstall (RESEARCH verified)
  </acceptance_criteria>
  <done>The Supabase client singleton, env contract, and validation scaffolds exist; React Query is retained without the dead Express path. Build/type verification happens in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the app still builds and dev-serves after cleanup (DATA-04 success criterion #5)</name>
  <files>package.json</files>
  <read_first>
    - package.json (final scripts after Tasks 1-2)
    - client/src/main.tsx and client/src/App.tsx (entry + queryClient consumer — confirm nothing imports a deleted module)
    - .planning/phases/01-supabase-foundation-schema-rls-storage/01-RESEARCH.md §"Validation Architecture" (lines 457-492 — DATA-04 smoke check)
  </read_first>
  <action>
    Run `npm run check` (tsc) and confirm zero type errors — this proves no source file still imports server/, shared/, drizzle, or the deleted queryClient helpers. If tsc reports an unresolved import to a deleted module or the `@shared` alias, fix the offending import (there should be none: only App.tsx imports `{ queryClient }`, which still exists). Run `npm run build` (vite build) and confirm exit 0 and that dist/public/ is produced. Run `npm run dev` long enough to confirm Vite serves on port 3200 (then stop it) — this is a smoke check; the app must render even without `.env.local` present? NO: the supabase singleton throws if VITE_ vars are missing, and the singleton is imported transitively only when a module that imports it loads. Since Phase 1 does NOT yet import client/src/lib/supabase.ts anywhere in the render tree (Shop still reads static products.ts), `npm run build` and `npm run dev` succeed without VITE_ env. Confirm this: grep that no file under client/src imports './lib/supabase' yet (the singleton is created but not consumed until Phase 2). If any consumer exists, the dev server would need .env.local — document that in the SUMMARY. Do NOT wire the singleton into the render tree in this phase.
    Run `bash scripts/check-no-secret.sh` and confirm it exits 0 (no service_role in the just-built bundle) — this proves success criterion #1's secret-absence guard works on a real build.
  </action>
  <verify>
    <automated>npm run check && npm run build && test -d dist/public && bash scripts/check-no-secret.sh && ! grep -rq "from ['\"].*lib/supabase['\"]" client/src/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (no type errors, no unresolved imports to deleted modules)
    - `npm run build` exits 0 and `dist/public/` exists
    - `npm run dev` serves on port 3200 (manual smoke; record in SUMMARY)
    - `bash scripts/check-no-secret.sh` exits 0 (no `service_role` in `dist/`)
    - No file under `client/src/` imports `./lib/supabase` yet (singleton created, not wired into render tree this phase)
  </acceptance_criteria>
  <done>App builds and dev-serves cleanly post-cleanup; the secret-leak guard passes against a real build. DATA-04 (criterion #5) is verifiably satisfied; the client half of DATA-01 (singleton + env contract) is in place pending live-project wiring in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build (Vite) → client bundle | Only `VITE_`-prefixed env is inlined into the public bundle; anything else stays out |
| developer machine → npm registry | Package installs cross an untrusted-supply-chain boundary |
| browser (anon) → Supabase | The shipped anon key is public-by-design; real authz is RLS (built in Plan 02/03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SECRET | Information Disclosure / Elevation | Vite env inlining → client bundle | mitigate | Service-role key NEVER `VITE_`-prefixed; `.env.example` documents only anon URL+key; `scripts/check-no-secret.sh` greps `dist/` for `service_role` and fails the build (Task 2/3) [VERIFIED: vite env rules] |
| T-01-ENVLEAK | Information Disclosure | `.env.local` committed to git | mitigate | `.gitignore` adds `.env.local` + `.env*.local` (Task 2); `.env.example` carries placeholders only |
| T-01-SC | Tampering | npm installs (`@supabase/supabase-js`, `supabase`) | accept | Both are first-party official Supabase packages, [VERIFIED] in RESEARCH Package Legitimacy Audit (registry + official repo + ~19.7M/1.75M weekly downloads, no postinstall). Not [ASSUMED]/[SUS]/[SLOP] → no blocking checkpoint required |
| T-01-DEADCODE | Elevation | Lingering Express/Passport server code | mitigate | Full deletion of `server/`, `shared/schema.ts`, `drizzle.config.ts`, `script/build.ts` and all related deps (Task 1) — removes an unguarded attack surface entirely (D-13) |
</threat_model>

<verification>
- `npm run check` (tsc) green — no source references deleted modules
- `npm run build` exits 0; `dist/public/` produced
- `bash scripts/check-no-secret.sh` exits 0 (no `service_role` in bundle)
- `grep -E '"(express|passport|drizzle-orm|pg)"' package.json` exits 1
- `client/src/lib/supabase.ts` exports `supabase` with the VITE_ env-or-throw guard
</verification>

<success_criteria>
- DATA-04: Express + Drizzle + Passport scaffolding removed; app builds and dev-serves (ROADMAP success criterion #5)
- DATA-01 (client half): env-based Supabase client singleton exists, anon key in client only, service-role key absent from any `VITE_`-prefixed var and from the built bundle (ROADMAP success criterion #1 — secret-absence guard)
- Validation scaffolds (`check-no-secret.sh`, `verify-skeleton.ts`) created for Plan 03 to run GREEN against the live project
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation-schema-rls-storage/01-01-SUMMARY.md` when done
</output>
