---
phase: 01-supabase-foundation-schema-rls-storage
plan: 02
subsystem: database
tags: [supabase, postgres, rls, security-definer, storage, migrations, sql]

# Dependency graph
requires: []
provides:
  - "supabase/config.toml — Supabase CLI workspace (project_id placeholder, set at Plan 03 link)"
  - "0001_init_schema.sql — private schema + non-recursive is_admin() SECURITY DEFINER + all six full-column tables"
  - "0002_rls_policies.sql — RLS enabled on all six tables with the D-12 default-deny posture"
  - "0003_storage_buckets.sql — product-images + site-content buckets with public-read/admin-write storage.objects policies"
  - "rls_assertions.sql — DATA-02 invariant assertions (failing-test-first artifact for Plan 03)"
affects: [01-03 (db push + run rls_assertions live), 02 (seed upserts on slug), 03 (auth/signup profile rows), 04 (admin portal writes + image uploads), 05 (questionnaire submit policy)]

# Tech tracking
tech-stack:
  added: [supabase-cli (versioned SQL migrations)]
  patterns: [versioned-SQL-migration (D-05), non-recursive is_admin() SECURITY DEFINER with locked search_path, default-deny RLS, owner-scoped (select auth.uid()) policies, storage.objects bucket-scoped policies]

key-files:
  created:
    - supabase/config.toml
    - supabase/migrations/0001_init_schema.sql
    - supabase/migrations/0002_rls_policies.sql
    - supabase/migrations/0003_storage_buckets.sql
    - supabase/tests/rls_assertions.sql
    - supabase/.gitignore
  modified: []

key-decisions:
  - "Used the available global Homebrew Supabase CLI (v2.90.0) for `supabase init` because Plan 01's devDependency is not present in this worktree (Plan 01 runs in parallel); init only generates local config, installs nothing"
  - "Named migration files 0001/0002/0003 with explicit numeric prefixes for unambiguous ordering, as the plan permits (sort order is what matters)"
  - "Split schema (0001) and RLS policies (0002) into separate files for readability; ordering holds because 0002 sorts after 0001 so tables + is_admin already exist"

patterns-established:
  - "Versioned-SQL-migration (D-05): all schema changes are ordered files in supabase/migrations/, applied via supabase db push"
  - "Non-recursive is_admin(): SECURITY DEFINER in private schema, set search_path = '', fully-qualified objects — the canonical profiles-recursion fix"
  - "Default-deny RLS: enable RLS on every table, then open only specific reads/writes; all auth.uid() wrapped as (select auth.uid())"

requirements-completed: [DATA-02]

# Metrics
duration: ~12min
completed: 2026-05-31
---

# Phase 01 Plan 02: Supabase Schema, RLS & Storage Migrations Summary

**Versioned Supabase migrations defining all six tables, default-deny RLS with a non-recursive `is_admin()` SECURITY DEFINER helper, two public-read/admin-write Storage buckets, and an RLS-invariant assertion test — authored and committed (not yet pushed; Plan 03 applies them live).**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-31
- **Completed:** 2026-05-31
- **Tasks:** 3
- **Files created:** 6 (config.toml, 3 migrations, 1 test, supabase/.gitignore)

## Accomplishments
- `supabase init` workspace with `config.toml` (single hosted cloud project, D-06; no local Docker)
- `0001_init_schema.sql`: `private` schema + non-recursive `is_admin()` (SECURITY DEFINER, `set search_path = ''`, in the `private` schema so it is never API-exposed) + all six full-column tables (categories, products, profiles, site_content, customization_submissions, wishlists), in dependency-safe order
- `0002_rls_policies.sql`: RLS enabled on all six tables (deny-all baseline) with the exact D-12 posture — public read on products/categories/site_content, owner-scoped profiles/wishlists, admin+owner read on customization_submissions, admin-only catalog/content writes; profiles self-read avoids `is_admin()` (recursion-safe); every `auth.uid()` in `(select ...)` form
- `0003_storage_buckets.sql`: two buckets (product-images, site-content, `public=true`, idempotent) + 8 `storage.objects` policies (public read + admin insert/update/delete per bucket, gated on `bucket_id` + `private.is_admin()`)
- `rls_assertions.sql`: five DATA-02 invariant groups, each raising an exception on failure — runs GREEN against the live project in Plan 03

## Task Commits

Each task was committed atomically:

1. **Task 1: Init workspace + schema/is_admin() migration** - `28573fd` (feat)
2. **Task 2: RLS-policies migration (D-12 default-deny posture)** - `0505f70` (feat)
3. **Task 3: Storage buckets migration + RLS assertion test** - `b7c6eb2` (feat)

## Files Created/Modified
- `supabase/config.toml` - Supabase CLI project config (`project_id` placeholder; the real ref is set at Plan 03 `supabase link`)
- `supabase/.gitignore` - generated by `supabase init`; ignores `.temp`, `.branches`, `.env*.local`
- `supabase/migrations/0001_init_schema.sql` - private schema, `is_admin()`, six tables with full columns
- `supabase/migrations/0002_rls_policies.sql` - enable RLS x6 + D-12 read/write policies
- `supabase/migrations/0003_storage_buckets.sql` - two buckets + 8 storage.objects policies
- `supabase/tests/rls_assertions.sql` - DATA-02 invariant assertions (failing-test-first artifact)

## Decisions Made
- See `key-decisions` frontmatter. Notably: used the global Homebrew Supabase CLI v2.90.0 for `supabase init` (the Plan 01 devDependency is absent in this worktree because Plan 01 executes in a parallel wave-1 worktree); `init` only scaffolds local config and installs no packages, so this is a safe substitution for the one offline operation this plan needs.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used global Supabase CLI instead of the missing node_modules/.bin/supabase**
- **Found during:** Task 1 (`supabase init`)
- **Issue:** The plan action says to use Plan 01's `supabase` devDependency or `node_modules/.bin/supabase`, but neither exists in this worktree — Plan 01 (which adds the devDependency) runs in a separate parallel wave-1 worktree, and `package.json` has no `supabase` entry here.
- **Fix:** Ran the already-installed, vetted global Homebrew Supabase CLI (`/opt/homebrew/bin/supabase`, v2.90.0) to perform `supabase init`. This generated `supabase/config.toml` and the `supabase/` scaffold. No package was installed (init is a local scaffold operation), so the package-legitimacy exclusion in Rule 3 does not apply — `supabase` is the official first-party CLI already vetted in 01-RESEARCH.md.
- **Files modified:** supabase/config.toml, supabase/.gitignore (both generated)
- **Verification:** Task 1 automated verify passed (`config.toml` contains `project_id`; migration file present with all required statements).
- **Committed in:** `28573fd` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope creep. The CLI version (2.90.0 vs the research's 2.102.0) differs only in tooling; the generated `config.toml` is standard and `db push` in Plan 03 is unaffected. The `project_id` defaulted to the worktree directory name (`agent-a8d7bee20e577a01a`) — a placeholder that Plan 03 overwrites at `supabase link --project-ref <real-ref>`.

## Issues Encountered
None — all three tasks executed and verified as planned.

## Known Stubs
None. `config.toml`'s `project_id` is a CLI-generated placeholder (not an app stub) and is explicitly set live in Plan 03; this is the intended foundation state.

## Notes for Plan 03 (live application)
- These migrations are authored but NOT pushed. Plan 03 must: `supabase link --project-ref <real-ref>`, set `project_id` in `config.toml`, run `supabase db push` (consider `--dry-run` first per RESEARCH Pitfall 3), then run `psql "$DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/rls_assertions.sql` expecting the "ALL DATA-02 RLS INVARIANTS PASSED" notice.
- **RESEARCH A2 flag:** `0003_storage_buckets.sql` inserts into `storage.buckets (id, name, public)`. If the live `storage.buckets` column set differs, the Plan 03 `db push --dry-run` will surface it — adjust there.
- Migration files sort 0001 → 0002 → 0003 so dependencies (private schema, is_admin, tables) precede references.

## Next Phase Readiness
- Database foundation (DATA-02) is fully authored and committed; ready for Plan 03 to apply it to the live project and run the assertion suite GREEN.
- No blockers from this plan. The only external prerequisite (a created hosted Supabase project + credentials) is handled at the Plan 03 human-verify checkpoint.

## Self-Check: PASSED

All 6 created files exist on disk and all 4 commits (`28573fd`, `0505f70`, `b7c6eb2`, `c40a516`) are present in git history.

---
*Phase: 01-supabase-foundation-schema-rls-storage*
*Completed: 2026-05-31*
