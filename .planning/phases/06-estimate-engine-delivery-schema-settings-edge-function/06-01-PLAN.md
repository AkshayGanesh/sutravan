---
phase: 06-estimate-engine-delivery-schema-settings-edge-function
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0014_delivery_settings_seed.sql
  - supabase/migrations/0015_pincodes.sql
  - supabase/migrations/0016_delivery_rate_slabs.sql
  - supabase/migrations/0017_delivery_estimate_cache.sql
  - supabase/migrations/0018_profiles_default_pincode.sql
  - scripts/verify-delivery-seed.ts
autonomous: true
requirements: [DLVR-05]
must_haves:
  truths:
    - "All five delivery data structures exist live via idempotent migrations (SC5)"
    - "delivery_rate_slabs holds exactly 20 monotonic rows (5 zones x 4 weight bands) seeded inline (SC5)"
    - "delivery_estimate_cache has RLS enabled with NO policies — unreachable via anon/authenticated PostgREST (SC4 schema half)"
    - "Five delivery site_content keys are seeded (origin 000000, default weight 250g, lead 1, cod_rules JSON, free-ship null) (SC5)"
    - "profiles.default_pincode column exists (SC5)"
    - "Re-running the migrations / verify converges with no error (idempotent)"
  artifacts:
    - path: "supabase/migrations/0014_delivery_settings_seed.sql"
      provides: "Five delivery site_content keys (D-06/07/08/09/10/18/19/20)"
      contains: "on conflict (key) do nothing"
    - path: "supabase/migrations/0015_pincodes.sql"
      provides: "pincodes serviceability table + public-read/admin-write RLS (D-16)"
      contains: "create table public.pincodes"
    - path: "supabase/migrations/0016_delivery_rate_slabs.sql"
      provides: "zone-weight slab table + 20-row grid seed (D-01..05)"
      contains: "unique (zone, weight_band)"
    - path: "supabase/migrations/0017_delivery_estimate_cache.sql"
      provides: "deny-direct cache table, service-role sole writer (D-17)"
      contains: "enable row level security"
    - path: "supabase/migrations/0018_profiles_default_pincode.sql"
      provides: "profiles.default_pincode column for Phase 8 persistence (DLVR-10)"
      contains: "add column default_pincode"
    - path: "scripts/verify-delivery-seed.ts"
      provides: "Node service-role assertions for SC4 (deny-direct RLS) + SC5 (counts/idempotency/monotonicity)"
      exports: ["main"]
  key_links:
    - from: "supabase/migrations/0016_delivery_rate_slabs.sql"
      to: "delivery_rate_slabs grid"
      via: "20 inline insert rows with on conflict (zone, weight_band) do nothing"
      pattern: "on conflict \\(zone, weight_band\\)"
    - from: "scripts/verify-delivery-seed.ts"
      to: "delivery_estimate_cache"
      via: "anon select returns 0 / denied (deny-direct probe)"
      pattern: "delivery_estimate_cache"
---

<objective>
Land ALL Phase 6 delivery data structures as five idempotent migrations and push them live, so a real estimate can return before any UI exists (SC5) and the estimate cache is provably deny-direct (SC4 schema half). This is the schema foundation the pincode seed (Plan 02) and the `delivery-estimate` Edge Function (Plan 03) both read.

Purpose: Establish the seeded zone-weight slab grid, the pincodes serviceability table, the deny-direct estimate cache, the five delivery `site_content` settings keys, and the `profiles.default_pincode` column — every structure Phases 7-10 depend on.
Output: `supabase/migrations/0014`–`0018`, the `scripts/verify-delivery-seed.ts` assertion harness, and a live schema (pushed) verified green.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-CONTEXT.md
@.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-RESEARCH.md
@.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01 scope)

- Migrations: `supabase/migrations/0014_delivery_settings_seed.sql`, `0015_pincodes.sql`, `0016_delivery_rate_slabs.sql`, `0017_delivery_estimate_cache.sql`, `0018_profiles_default_pincode.sql`
- Tables: `public.pincodes`, `public.delivery_rate_slabs`, `public.delivery_estimate_cache`
- Column: `public.profiles.default_pincode` (text, nullable)
- site_content keys: `delivery_origin_pincode`, `delivery_default_weight_g`, `delivery_dispatch_lead_days`, `delivery_cod_rules` (JSON), `delivery_free_ship_threshold` (null)
- Verification harness: `scripts/verify-delivery-seed.ts` (consumed by Plan 02 + the phase gate)

<tasks>

<task type="auto">
  <name>Task 1: Table & column migrations — pincodes (0015), deny-direct cache (0017), profiles.default_pincode (0018)</name>
  <read_first>
    - `supabase/migrations/0011_product_variants.sql` (lines 1-63) — the canonical NEW-table pattern: header banner explaining sort order + why a new table legitimately creates policies; `create table public.X (...)`; `create index ... on public.X (...)`; `alter table ... enable row level security`; `create policy "X_public_read" ... to anon, authenticated using (...)`; `create policy "X_admin_write" ... for all to authenticated using (private.is_admin()) with check (private.is_admin())`.
    - `supabase/migrations/0004_auth_profiles.sql` (line 32) — `alter table public.profiles add column name text;` — the exact single-nullable-column-add shape to clone for `default_pincode`.
    - 06-RESEARCH.md "Proposed Schema" (DDL lines 364-425) — the exact column lists for `pincodes`, `delivery_estimate_cache`, and the `profiles` alter.
    - 06-PATTERNS.md "Shared Patterns → Deny-Direct RLS" (lines 228-236) — for 0017, enable RLS and write NO `create policy` statements; add an explicit header comment that the absence of policies is intentional so no future reader "adds a missing policy".
  </read_first>
  <files>supabase/migrations/0015_pincodes.sql, supabase/migrations/0017_delivery_estimate_cache.sql, supabase/migrations/0018_profiles_default_pincode.sql</files>
  <action>
    Write three migrations cloning the analogs above, each opening with the migration-header-comment convention (banner naming the file, the decision IDs it satisfies, and any divergence).

    0015_pincodes.sql (per D-14/D-15/D-16): `create table public.pincodes` with columns `pincode text primary key`, `state text not null`, `district text`, `circle text`, `region text`, `is_metro boolean not null default false`, `is_remote boolean not null default false`, `serviceable boolean not null default true` (D-16 — lets a real pincode be switched off without deleting the row), `created_at timestamptz default now()`. Add `create index pincodes_state_idx on public.pincodes (state)`. Enable RLS; add `pincodes_public_read` for select to anon, authenticated using (true) (unconditional — pincodes are not draft-scoped); add `pincodes_admin_write` for all to authenticated using `private.is_admin()` with check `private.is_admin()`. SCHEMA ONLY — the ~19.5k rows load via `scripts/seed-pincodes.ts` in Plan 02; do NOT inline a data INSERT here (RESEARCH Anti-Patterns).

    0017_delivery_estimate_cache.sql (per D-17 — DENY-DIRECT): `create table public.delivery_estimate_cache` with `id uuid primary key default gen_random_uuid()`, `origin_pincode text not null`, `dest_pincode text not null`, `weight_bucket int not null`, `serviceable boolean not null`, `cost integer` (nullable — null when not serviceable, D-13), `eta_min_days int`, `eta_max_days int`, `cod_available boolean not null default false`, `zone text`, `fetched_at timestamptz not null default now()`, `expires_at timestamptz not null` (fetched_at + 24h), and `unique (origin_pincode, dest_pincode, weight_bucket)` (the cache key, D-17). Then `alter table public.delivery_estimate_cache enable row level security` and DELIBERATELY NO `create policy` lines — header comment must state the deny-direct rationale (service-role Edge Function in Plan 03 is the sole I/O; client cache-poisoning is prevented by there being no anon/authenticated policy).

    0018_profiles_default_pincode.sql (per DLVR-10): `alter table public.profiles add column default_pincode text;` — inherits the existing `profiles_self_update` RLS (0002); no new policy. Header note: column lands here; Phase 8 wires read/write.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/0015_pincodes.sql` contains `create table public.pincodes`, `serviceable boolean not null default true`, `pincodes_public_read`, and `private.is_admin()`; contains NO bulk-row `insert into public.pincodes`.
    - `supabase/migrations/0017_delivery_estimate_cache.sql` contains `enable row level security` AND, ignoring comment lines, contains ZERO `create policy` statements (deny-direct); contains `unique (origin_pincode, dest_pincode, weight_bucket)`.
    - `supabase/migrations/0018_profiles_default_pincode.sql` contains `alter table public.profiles add column default_pincode text`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'create table public.pincodes' supabase/migrations/0015_pincodes.sql && grep -q 'serviceable boolean not null default true' supabase/migrations/0015_pincodes.sql && ! grep -qiE '^\s*insert into public.pincodes' supabase/migrations/0015_pincodes.sql && grep -q 'enable row level security' supabase/migrations/0017_delivery_estimate_cache.sql && [ "$(grep -v '^[[:space:]]*--' supabase/migrations/0017_delivery_estimate_cache.sql | grep -ci 'create policy')" -eq 0 ] && grep -q 'unique (origin_pincode, dest_pincode, weight_bucket)' supabase/migrations/0017_delivery_estimate_cache.sql && grep -q 'add column default_pincode text' supabase/migrations/0018_profiles_default_pincode.sql && echo PASS</automated>
  </verify>
  <done>Three migrations exist, follow the 0011/0004 conventions, and the cache table is provably deny-direct (no policies) by static check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed migrations — settings keys (0014), slab grid (0016) + verify-delivery-seed.ts scaffold</name>
  <files>supabase/migrations/0014_delivery_settings_seed.sql, supabase/migrations/0016_delivery_rate_slabs.sql, scripts/verify-delivery-seed.ts</files>
  <read_first>
    - `supabase/migrations/0006_seed_site_content.sql` (lines 15-46) — idempotent `insert into public.site_content (key, value) values (...) on conflict (key) do nothing`; note the doubled-single-quote escaping rule for string values.
    - 06-RESEARCH.md "Proposed Schema" 0014 block (lines 355-362) — the exact five key/value pairs; `delivery_cod_rules` is a JSON STRING in the text `value` column (`{"enabled":true,"fee":30,"valueCap":5000}`), `delivery_free_ship_threshold` seeds SQL `null`.
    - 06-RESEARCH.md "Seed Slab Grid" (lines 321-350) — the 20-row monotonic grid (zone, band g, cost ₹, eta_min, eta_max) and the monotonicity check; ETA = D-05 verbatim.
    - 06-RESEARCH.md "Proposed Schema" 0016 block (lines 383-402) — `delivery_rate_slabs` columns + `zone in (...)` / `weight_band between 1 and 4` CHECKs + `unique (zone, weight_band)`.
    - `scripts/verify-seed.ts` (lines 15-118) — the anon-vs-service-role assertion harness: build clients from non-VITE_ `process.env`, `.select('x', { count: 'exact', head: true })`, fail-fast `console.error` + `process.exit(1)`, `PASS:`/`FAIL:` lines, top-level `.catch`.
  </read_first>
  <behavior>
    - verify-delivery-seed.ts asserts the 5 delivery site_content keys are present.
    - It asserts `delivery_rate_slabs` count == 20 AND every (zone,band) cost is strictly monotonic across zones (per band) and across bands (per zone).
    - It asserts `profiles.default_pincode` column is selectable.
    - SC4 probe: an anon `.select('*')` on `delivery_estimate_cache` returns 0 rows / permission-denied (deny-direct).
    - Pincode count: PASS when `>= 15000`; when env `PINCODES_OPTIONAL=1` is set, the pincode assertion is SKIPPED (so Plan 01's pre-seed push can run the script; Plan 02 runs it WITHOUT the flag for the full gate).
  </behavior>
  <action>
    Write 0014_delivery_settings_seed.sql cloning 0006: `insert into public.site_content (key, value) values` for the five keys — `delivery_origin_pincode` = `'000000'` (D-18 deliberately-fake placeholder so the owner must configure the real origin in Phase 9), `delivery_default_weight_g` = `'250'` (D-10), `delivery_dispatch_lead_days` = `'1'` (D-20), `delivery_cod_rules` = the JSON string `{"enabled":true,"fee":30,"valueCap":5000}` (D-06 flat ₹30 fee / D-07 ₹5000 cap / D-08 global toggle on / D-09 single jsonb-in-text key), `delivery_free_ship_threshold` = SQL `null` (D-19 present but off) — closing with `on conflict (key) do nothing`. site_content itself is unchanged (rides the existing `['siteContent']` upsert pattern; no admin plumbing in Phase 6).

    Write 0016_delivery_rate_slabs.sql cloning the 0011 table+RLS pattern: `create table public.delivery_rate_slabs` with `id uuid primary key default gen_random_uuid()`, `zone text not null check (zone in ('local','regional','metro','national','remote'))` (D-01), `weight_band int not null check (weight_band between 1 and 4)` (D-02), `weight_min_g int not null`, `weight_max_g int not null`, `cost integer not null` (base ₹; the engine round-ups to ₹10 — D-11/D-13; no separate buffer field — D-12), `eta_min_days int not null`, `eta_max_days int not null`, `created_at`/`updated_at timestamptz default now()`, `unique (zone, weight_band)` (D-03 cartesian grid). Enable RLS; `delivery_rate_slabs_public_read` select to anon, authenticated using (true); `delivery_rate_slabs_admin_write` for all to authenticated using/`with check` `private.is_admin()`. Then inline-seed the 20 rows from the RESEARCH grid (weight_min_g/weight_max_g = 0/250, 251/500, 501/1000, 1001/2000 for bands 1-4) with `on conflict (zone, weight_band) do nothing`. Costs are explicit placeholders (D-04) the owner replaces in Phase 10.

    Write scripts/verify-delivery-seed.ts per the `<behavior>` block, cloning verify-seed.ts structure. Run with `node --env-file=.env.seed.local scripts/verify-delivery-seed.ts`. Implement the monotonicity assertion in JS over the 20 fetched slab rows; implement the deny-direct probe as an anon-client select on `delivery_estimate_cache`; honor `process.env.PINCODES_OPTIONAL` to skip the pincode-count assertion.
  </action>
  <acceptance_criteria>
    - 0014 contains all five `delivery_*` keys and `on conflict (key) do nothing`; the file has exactly 5 seeded value rows.
    - 0016 contains `unique (zone, weight_band)`, the zone/weight_band CHECK constraints, and exactly 20 inline slab value rows with `on conflict (zone, weight_band) do nothing`.
    - scripts/verify-delivery-seed.ts exists, references `delivery_rate_slabs`, `delivery_estimate_cache`, the five site_content keys, `profiles`, and reads `PINCODES_OPTIONAL`.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE "^\s*\('delivery_" supabase/migrations/0014_delivery_settings_seed.sql)" -eq 5 ] && grep -q 'on conflict (key) do nothing' supabase/migrations/0014_delivery_settings_seed.sql && grep -q 'unique (zone, weight_band)' supabase/migrations/0016_delivery_rate_slabs.sql && [ "$(grep -cE "^\s*\('(local|regional|metro|national|remote)'" supabase/migrations/0016_delivery_rate_slabs.sql)" -eq 20 ] && grep -q 'PINCODES_OPTIONAL' scripts/verify-delivery-seed.ts && grep -q 'delivery_estimate_cache' scripts/verify-delivery-seed.ts && echo PASS</automated>
  </verify>
  <done>The settings + slab-grid migrations are idempotent and complete (5 keys, 20 monotonic rows), and the verify harness exists with a skippable pincode gate.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push migrations live + verify seeded structures</name>
  <read_first>
    - Memory `supabase-live-ops.md` — `echo y | ./node_modules/.bin/supabase db push --linked` (CLI v2.102.0 devDep, cached auth — no SUPABASE_ACCESS_TOKEN / DB password needed); `supabase migration list --linked` to confirm; the pooler URL has no password so `psql`/`COPY` are unavailable.
    - 06-RESEARCH.md "Validation Architecture" (lines 552-565) — SC5/SC4 assertions the verify script must prove after push.
  </read_first>
  <files>supabase/migrations/0014_delivery_settings_seed.sql, supabase/migrations/0015_pincodes.sql, supabase/migrations/0016_delivery_rate_slabs.sql, supabase/migrations/0017_delivery_estimate_cache.sql, supabase/migrations/0018_profiles_default_pincode.sql</files>
  <action>
    BLOCKING gate — run AFTER Tasks 1-2 wrote all five migrations and the verify scaffold, and BEFORE Plan 02/03 do any seeding or smoke-testing. The live schema MUST exist or the pincode seed and the Edge Function both fail (type-checks pass without the push — that is a false-positive; the live tables are what Plan 02/03 read).

    Apply the migrations to the linked live project: `echo y | ./node_modules/.bin/supabase db push --linked`. Then prove the structures with the Plan 01 harness run in pre-seed mode (pincodes are still empty until Plan 02): `PINCODES_OPTIONAL=1 node --env-file=.env.seed.local scripts/verify-delivery-seed.ts` — expect `PASS:` (5 settings keys present, `delivery_rate_slabs` count == 20 + monotonic, `profiles.default_pincode` selectable, anon `delivery_estimate_cache` select denied/0 rows). Re-run `db push --linked` once to confirm idempotency (no error, nothing to apply). Do NOT mark autonomous:false — `echo y |` suppresses the only interactive prompt.
  </action>
  <acceptance_criteria>
    - `supabase migration list --linked` shows 0014-0018 as applied remotely.
    - `PINCODES_OPTIONAL=1 node --env-file=.env.seed.local scripts/verify-delivery-seed.ts` prints `PASS` (slabs=20 monotonic, 5 keys, profiles col, cache deny-direct).
    - A second `db push --linked` reports nothing to apply (idempotent).
  </acceptance_criteria>
  <verify>
    <automated>echo y | ./node_modules/.bin/supabase db push --linked && PINCODES_OPTIONAL=1 node --env-file=.env.seed.local scripts/verify-delivery-seed.ts</automated>
  </verify>
  <done>All five migrations are live; the slab grid, settings keys, profiles column, and deny-direct cache are verified green; re-applying is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated client → PostgREST → Postgres | Untrusted clients can read public tables and (for the cache) MUST be denied entirely |
| migration author → live DB | Schema changes applied via cached-auth Supabase CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | `delivery_estimate_cache` | mitigate | Deny-direct RLS in 0017: enable RLS, write ZERO policies → table unreachable via anon/authenticated PostgREST; service-role Edge Function (Plan 03) is the sole writer (D-17). Static gate asserts no `create policy` in 0017. |
| T-6-02 | Information Disclosure | `pincodes`, `delivery_rate_slabs` public-read | accept | Public-read rate slabs + serviceability are non-sensitive placeholder data the customer estimator needs; admin-write gated by `private.is_admin()`. No PII. |
| T-6-03 | Elevation of Privilege | `pincodes`/`delivery_rate_slabs` admin-write | mitigate | `for all to authenticated using (private.is_admin()) with check (private.is_admin())` — the canonical 0002/0011 admin gate; client UI (Phases 9-10) is convenience only. |
| T-6-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies this phase (06-RESEARCH.md Package Legitimacy Audit: none). No install tasks → supply-chain vector N/A; nothing to slopcheck. |
</threat_model>

<verification>
- `echo y | ./node_modules/.bin/supabase db push --linked` applies 0014-0018 with no error; a second run is a no-op (idempotent).
- `PINCODES_OPTIONAL=1 node --env-file=.env.seed.local scripts/verify-delivery-seed.ts` prints PASS: 5 settings keys present, slabs=20 + monotonic, profiles.default_pincode exists, cache deny-direct (anon select 0/denied).
- Static: 0017 contains no `create policy` (deny-direct); 0015/0016 carry public-read + `private.is_admin()` admin-write.
</verification>

<success_criteria>
- SC5 (schema half): all delivery structures exist live via idempotent migrations with seeded defaults — 5 site_content keys, 20-row slab grid, pincodes table (schema), deny-direct cache, profiles.default_pincode.
- SC4 (schema half): `delivery_estimate_cache` is RLS-enabled with no policies; anon cannot read or write it.
</success_criteria>

<output>
Create `.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-01-SUMMARY.md` when done.
</output>
