---
phase: 06-estimate-engine-delivery-schema-settings-edge-function
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/transform-pincodes.ts
  - scripts/seed-pincodes.ts
  - scripts/data/pincodes.ndjson
  - .gitignore
autonomous: false
requirements: [DLVR-05]
user_setup:
  - service: data.gov.in
    why: "One-time download of the All-India Pincode Directory CSV — the serviceability dataset source (D-14). No automated fetch exists (RESEARCH Open Question 3 / Environment Availability)."
    dashboard_config:
      - task: "Download the All-India Pincode Directory CSV (dataset 6818292) and save it as scripts/data/pincodes-raw.csv"
        location: "https://www.data.gov.in/datasets_webservices/datasets/6818292"
must_haves:
  truths:
    - "scripts/data/pincodes.ndjson holds ~19.5k unique-pincode rows, one normalized JSON object per line (SC5)"
    - "Every row's state is a canonical value in the adjacency/remote sets (& → and, title-case) so zone derivation cannot silently miss (Pitfall A)"
    - "is_metro / is_remote flags are derived at seed time (8-metro prefix set; fixed remote-state set) (D-15)"
    - "The pincodes table holds ~19.5k serviceable rows live after an idempotent chunked upsert (SC5)"
    - "Re-running the seed converges to the same count, never duplicates (onConflict: pincode)"
  artifacts:
    - path: "scripts/transform-pincodes.ts"
      provides: "CSV → normalized NDJSON transform (dedupe, derive first3/is_metro/is_remote, normalize StateName)"
      exports: ["main"]
    - path: "scripts/data/pincodes.ndjson"
      provides: "Committed transformed unique-pincode dataset (<2MB)"
    - path: "scripts/seed-pincodes.ts"
      provides: "Service-role chunked upsert of the NDJSON into public.pincodes"
      exports: ["main"]
  key_links:
    - from: "scripts/seed-pincodes.ts"
      to: "public.pincodes"
      via: "chunked upsert onConflict pincode"
      pattern: "onConflict: 'pincode'"
    - from: "scripts/transform-pincodes.ts"
      to: "scripts/data/pincodes.ndjson"
      via: "writes one JSON object per unique pincode"
      pattern: "pincodes.ndjson"
---

<objective>
Acquire, transform, and seed the India Post serviceability dataset so the `pincodes` table holds ~19.5k normalized, zone-classifiable rows live (SC5 data half). Membership in this table — plus the `serviceable` boolean (D-16) — is the authoritative serviceability source the Edge Function (Plan 03) checks before rating.

Purpose: Without seeded pincodes, the engine cannot check serviceability or derive an origin-relative zone, so no real estimate returns. This plan delivers the dataset and the idempotent loader.
Output: `scripts/transform-pincodes.ts`, committed `scripts/data/pincodes.ndjson`, `scripts/seed-pincodes.ts`, and ~19.5k live rows verified by the Plan 01 harness (full gate).
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-CONTEXT.md
@.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-RESEARCH.md
@.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02 scope)

- `scripts/transform-pincodes.ts` — one-time CSV→NDJSON transform (Claude-owned; normalization is load-bearing per Pitfall A)
- `scripts/data/pincodes.ndjson` — committed transformed dataset (`{ pincode, state, district, circle, region, is_metro, is_remote }` per line)
- `scripts/seed-pincodes.ts` — service-role chunked upsert loader (`onConflict: 'pincode'`)
- `.gitignore` entry for the transient raw CSV
- ~19.5k rows in `public.pincodes` (live)

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [HUMAN] Acquire the India Post All-India Pincode Directory CSV</name>
  <action>The human operator downloads the All-India Pincode Directory CSV (data.gov.in dataset 6818292) and saves it to `scripts/data/pincodes-raw.csv` — the source serviceability dataset consumed by the transform/seed in Tasks 2-3. No code is written in this task; it is a manual data-acquisition gate. See `<how-to-verify>` for the exact steps and `<resume-signal>` for the continue condition.</action>
  <what-built>Plan 01 created the empty `pincodes` table live. This plan needs the source serviceability dataset, which has no automated fetch (RESEARCH Open Question 3 / Environment Availability: "must be downloaded once by a human; no automated fallback").</what-built>
  <read_first>
    - 06-RESEARCH.md "Pincode Seed Strategy" (lines 312-319) — source = data.gov.in *All India Pincode Directory* (dataset 6818292); columns `CircleName, RegionName, DivisionName, OfficeName, Pincode, OfficeType, Delivery, District, StateName, Latitude, Longitude`; raw file ~165k post-office rows (~10-15 MB).
  </read_first>
  <how-to-verify>
    1. Download the All-India Pincode Directory CSV from https://www.data.gov.in/datasets_webservices/datasets/6818292
    2. Save it to `scripts/data/pincodes-raw.csv` in the repo (the `scripts/data/` directory is created here).
    3. Confirm the header row contains `Pincode` and `StateName` columns and the file has >100,000 data rows (`wc -l scripts/data/pincodes-raw.csv`).
  </how-to-verify>
  <verify>
    <automated>test -f scripts/data/pincodes-raw.csv && [ "$(wc -l < scripts/data/pincodes-raw.csv)" -gt 100000 ] && head -1 scripts/data/pincodes-raw.csv | grep -qiE 'pincode' && echo PASS</automated>
  </verify>
  <resume-signal>Type "downloaded" once scripts/data/pincodes-raw.csv exists, or describe any access issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Transform CSV → normalized NDJSON (dedupe + derive flags + normalize StateName)</name>
  <read_first>
    - 06-RESEARCH.md "Pincode Seed Strategy" step 2 (lines 316-318) — dedupe to unique `Pincode` (~19.5k), keep one representative `StateName/District/CircleName/RegionName`, derive `first3`/`is_metro`/`is_remote`, normalize StateName casing; write one JSON object per line.
    - 06-RESEARCH.md "Zone-Derivation Algorithm → Per-pincode facts" (lines 248-254) — `first3` = `substring(pincode,1,3)`; `is_metro` true when first3 ∈ `{110,400,700,600,560,500,380,411}`; `is_remote` true when state ∈ the fixed 12-entry remote set.
    - 06-RESEARCH.md "Pitfall A" (lines 442-446) — StateName normalization is load-bearing: directory uses `JAMMU & KASHMIR` / `ANDAMAN & NICOBAR ISLANDS`; canonicalize `&`→`and` + title-case so the function's adjacency/remote-set compares match. Add a transform-time assertion that every distinct state is in a known canonical set.
  </read_first>
  <files>scripts/transform-pincodes.ts, scripts/data/pincodes.ndjson, .gitignore</files>
  <action>
    Write `scripts/transform-pincodes.ts` (native Node 22 runner, no tsx/dotenv — run `node scripts/transform-pincodes.ts`). Read `scripts/data/pincodes-raw.csv`; parse rows; dedupe by `Pincode` keeping the first representative row; for each unique pincode emit `{ pincode, state, district, circle, region, is_metro, is_remote }` where: `state` = StateName normalized to canonical title-case with `&`→`and`; `is_metro` = `pincode.slice(0,3)` ∈ the 8-metro prefix set; `is_remote` = `state` ∈ the canonical remote set `{ Arunachal Pradesh, Assam, Manipur, Meghalaya, Mizoram, Nagaland, Tripura, Sikkim, Jammu and Kashmir, Ladakh, Andaman and Nicobar Islands, Lakshadweep }`. Write `scripts/data/pincodes.ndjson` (one JSON object per line). The transform MUST assert every distinct normalized `state` is in a known canonical set (the union of the remote set + the adjacency-map keys + all other Indian states/UTs) and exit non-zero listing any unknown state value (Pitfall A guard). Print the unique-pincode count + distinct-state count on success.

    Append `scripts/data/pincodes-raw.csv` to `.gitignore` (the raw ~15MB CSV is a transient input; only the <2MB `pincodes.ndjson` is committed — RESEARCH: committing the transformed NDJSON removes the runtime data.gov.in dependency and makes the seed reproducible).
  </action>
  <acceptance_criteria>
    - `scripts/data/pincodes.ndjson` exists with between 15,000 and 21,000 lines (deduped unique pincodes).
    - Every line is valid JSON with keys `pincode, state, district, circle, region, is_metro, is_remote`.
    - Spot-check: pincode starting `110` → `is_metro:true`; a Jammu and Kashmir / Andaman and Nicobar Islands pincode → `is_remote:true` and `state` contains no `&`.
    - `.gitignore` contains `scripts/data/pincodes-raw.csv`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/transform-pincodes.ts && L=$(wc -l < scripts/data/pincodes.ndjson) && [ "$L" -ge 15000 ] && [ "$L" -le 21000 ] && node -e "const fs=require('fs');const ls=fs.readFileSync('scripts/data/pincodes.ndjson','utf8').trim().split('\n').map(JSON.parse);if(ls.some(r=>/&/.test(r.state)))throw new Error('un-normalized state with &');if(!ls.some(r=>r.is_metro))throw new Error('no metro rows');if(!ls.some(r=>r.is_remote))throw new Error('no remote rows');console.log('PASS',ls.length)" && grep -q 'scripts/data/pincodes-raw.csv' .gitignore</automated>
  </verify>
  <done>The committed NDJSON holds ~19.5k normalized unique pincodes with correct is_metro/is_remote flags and no `&`-style state values; the raw CSV is gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Service-role chunked seed of pincodes + full verification</name>
  <read_first>
    - `scripts/seed.ts` (lines 14-31, 94-124) — service-role client from non-VITE_ `process.env` (`SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`), `{ auth: { persistSession: false } }`, idempotent `.upsert(rows, { onConflict: '...' })`, `process.exit(0/1)` + top-level `.catch`. Run convention: `node --env-file=.env.seed.local scripts/seed-pincodes.ts`.
    - 06-RESEARCH.md "Pincode Seed Strategy" steps 3-4 (lines 318-319) — chunked upsert ~1000 rows/batch with `onConflict: 'pincode'`; use PostgREST upsert NOT `COPY` (pooler has no password — live-ops memory); `serviceable` defaults true in the table, the seed never sets it false.
    - Memory `supabase-live-ops.md` — `.env.seed.local` carries `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`; service role bypasses RLS via PostgREST.
    - `scripts/verify-delivery-seed.ts` (created in Plan 01) — run it WITHOUT `PINCODES_OPTIONAL` for the full SC5 gate (pincodes count ≥ 15000).
  </read_first>
  <files>scripts/seed-pincodes.ts</files>
  <action>
    Write `scripts/seed-pincodes.ts` cloning `seed.ts`: build the service-role client from non-VITE_ `process.env` (fail fast if `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` unset); read `scripts/data/pincodes.ndjson`; map each line to a `pincodes` row (`pincode, state, district, circle, region, is_metro, is_remote` — let `serviceable` default true per D-16, do not set it); chunk into ~1000-row batches and `admin.from('pincodes').upsert(batch, { onConflict: 'pincode' })`; on any batch error throw with a generic message; print the total upserted count and `process.exit(0)`. Idempotent — re-running converges to the same ~19.5k rows, never duplicates.

    Then run the seed live and prove SC5 with the full gate: `node --env-file=.env.seed.local scripts/seed-pincodes.ts` followed by `node --env-file=.env.seed.local scripts/verify-delivery-seed.ts` (no PINCODES_OPTIONAL → asserts pincodes ≥ 15000 alongside slabs=20, settings keys, profiles col, cache deny-direct). Re-run the seed once to confirm idempotency (same count).
  </action>
  <acceptance_criteria>
    - `scripts/seed-pincodes.ts` uses `onConflict: 'pincode'`, chunks ~1000 rows, and reads from non-VITE_ `process.env` only.
    - After the seed, `node --env-file=.env.seed.local scripts/verify-delivery-seed.ts` prints `PASS` including a pincodes count ≥ 15000.
    - A second seed run leaves the count unchanged (idempotent).
  </acceptance_criteria>
  <verify>
    <automated>node --env-file=.env.seed.local scripts/seed-pincodes.ts && node --env-file=.env.seed.local scripts/verify-delivery-seed.ts</automated>
  </verify>
  <done>~19.5k serviceable pincodes are live and idempotently re-seedable; the full Plan 01 verify gate passes including the pincode count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed operator → live DB (service role) | The seed uses the service-role key, which bypasses RLS via PostgREST |
| committed dataset → zone derivation (Plan 03) | Un-normalized state strings silently break zone classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Information Disclosure | `SUPABASE_SERVICE_ROLE_KEY` in the seed script | mitigate | Key read from non-VITE_ `process.env` via `node --env-file=.env.seed.local` only; `.env*.local` is gitignored; the script is never imported by client code so it never reaches the bundle (`scripts/check-no-secret.sh` enforces). Clones the proven `seed.ts` posture. |
| T-6-04 | Tampering | StateName normalization in the transform | mitigate | Canonicalize `&`→`and` + title-case once at transform time; transform-time assertion fails on any state value outside the known canonical set (Pitfall A), preventing silent mis-zoning of remote shipments. |
| T-6-12 | Information Disclosure | committed `pincodes.ndjson` | accept | Dataset is public India Post directory data (no PII beyond office names already public); committing the <2MB transformed file is intentional (reproducible seed, no runtime data.gov.in dependency). |
| T-6-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (reuses the existing `@supabase/supabase-js`). No install tasks → supply-chain vector N/A. |
</threat_model>

<verification>
- `node scripts/transform-pincodes.ts` exits 0, writes 15k-21k NDJSON lines, asserts every state is canonical (no `&`).
- `node --env-file=.env.seed.local scripts/seed-pincodes.ts` upserts ~19.5k rows; re-run is a no-op count-wise (idempotent).
- `node --env-file=.env.seed.local scripts/verify-delivery-seed.ts` prints PASS with pincodes ≥ 15000 (full SC5 gate).
</verification>

<success_criteria>
- SC5 (data half): the `pincodes` serviceability dataset exists live (~19.5k rows) via an idempotent service-role seed, with correct is_metro/is_remote flags and canonical state values so the engine can derive zones.
</success_criteria>

<output>
Create `.planning/phases/06-estimate-engine-delivery-schema-settings-edge-function/06-02-SUMMARY.md` when done.
</output>
