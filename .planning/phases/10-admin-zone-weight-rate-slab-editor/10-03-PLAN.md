---
phase: 10-admin-zone-weight-rate-slab-editor
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: []
autonomous: false
requirements: [DLVR-03]

must_haves:
  truths:
    - "The owner can open /admin/rates, see the seeded 5×4 rate card prefilled from the live table, and edit any cost or per-zone ETA inline (SC1)"
    - "A saved cost change is reflected in a live customer estimate (product detail estimator) without any redeploy — the estimate-cache purge makes the new rate appear on the next lookup (SC2/D-11)"
    - "The editor blocks Save while any cell is invalid (₹0/blank/negative cost, eta_min > eta_max), rendering an inline error — SC3 validation holds and no ₹0 slab can be saved (D-07/D-08)"
    - "The grid exposes no add-row/delete-row control, so every serviceable destination+weight still resolves to a slab (SC3 structural, D-01)"
    - "Customer estimates continue to flow through the unchanged { serviceable, cost, etaDays, codAvailable } contract sourced from the slab table (SC4)"
  artifacts: []
  key_links:
    - from: "admin /admin/rates Save"
      to: "customer estimate reflects new cost"
      via: "upsert → cache purge → recompute (no redeploy)"
      pattern: "purge"
---

<objective>
Human-verify the end-to-end slice: an owner edits a rate on /admin/rates, saves,
and confirms the change flows live into a customer estimate with no redeploy —
plus confirms validation blocks bad input. This is the SC2/SC3 capstone. No new
code, no deploy, no migration: the feature runs against the already-live
Supabase table (0016) and the already-deployed delivery-estimate function; a
`npm run dev` session is sufficient to verify.

Purpose: Prove the live-reflection (SC2) and validation (SC3) claims with a real
human before the phase closes — these are the two claims that cannot be fully
proven by an automated type/grep check.
Output: owner sign-off; no file changes.
</objective>

<execution_context>
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akshayg/Downloads/Earthen-Luxury-Sutravan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-zone-weight-rate-slab-editor/10-CONTEXT.md
@client/src/pages/admin/RateSlabs.tsx
@client/src/lib/admin.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner verifies live rate edit + validation on /admin/rates</name>
  <files>none (verification-only; no file changes)</files>
  <read_first>
    - client/src/pages/admin/RateSlabs.tsx — the page under test
    - client/src/lib/admin.ts — useDeliveryRateSlabs / useSaveRateSlabs (the read + save behaviour being verified)
    - .planning/phases/10-admin-zone-weight-rate-slab-editor/10-CONTEXT.md — SC1/SC2/SC3/SC4 + D-01/D-07/D-08/D-11
  </read_first>
  <what-built>
    A new /admin/rates spreadsheet editor (5 zones × 4 weight bands) that reads the
    live delivery_rate_slabs grid, edits cost per cell and one ETA min/max per zone,
    and on a single Save bulk-upserts all 20 rows and purges the estimate cache so
    customer estimates recompute immediately (no redeploy). Validation blocks Save
    while any cell is invalid.
  </what-built>
  <action>
    Pause and present the how-to-verify steps below to the owner. Do NOT auto-advance
    (a human-verify checkpoint is never auto-approvable). Wait for the resume signal.
    If the owner reports an issue, capture it verbatim for gap closure and stop.
  </action>
  <how-to-verify>
    1. Start the app: `npm run dev` and log in as an admin, then open `/admin/rates`.
    2. Confirm the grid shows 5 zone rows (local/regional/metro/national/remote) ×
       4 weight-band columns with gram-range headers (0–250 / 251–500 / 501–1000 /
       1001–2000g), prefilled with the current rates — and that there is NO add-row
       or delete-row button (SC1/D-01/D-03).
    3. Validation (SC3/D-07/D-08): set the `local` band-1 cost to `0` → an inline
       error appears and the "Save rate slabs" button is DISABLED. Set it to a blank,
       then a negative, then `2.5` → each shows an inline error and keeps Save
       disabled. Set a zone's ETA min greater than its max → inline error, Save
       disabled.
    4. Save path (SC2): set a recognisable new `local` band-1 cost (e.g. `41`), fix
       any other invalid cells so Save enables, click Save → a success toast
       ("Rate slabs updated.") appears. Reload /admin/rates → the new value persists
       (proves the upsert committed).
    5. Live reflection (SC2/D-11): open a product detail page, enter a serviceable
       pincode that resolves to the `local` zone, and confirm the shown estimate
       reflects the new base rate (allow for the engine's ₹10 round-up — the figure
       should track your edit, not the old value). Because the cache was purged, the
       new rate appears on this fresh lookup without any redeploy.
    6. Contract (SC4): confirm the estimate still renders serviceability + cost +
       ETA range + COD availability exactly as before — the edit changed only the
       numbers, not the shape.
  </how-to-verify>
  <verify>
    <human-check>Owner confirms all four acceptance criteria below (grid loads, validation blocks bad input, saved rate reflects live with no redeploy, contract unchanged).</human-check>
  </verify>
  <acceptance_criteria>
    - Grid loads prefilled from the live table with 20 cells + 5 ETA pairs; no add/delete affordance (SC1/D-01)
    - A ₹0 / blank / negative / decimal cost and an eta_min > eta_max each block Save with an inline error (SC3/D-07/D-08)
    - A saved cost persists across reload and a customer estimate for that zone reflects the new rate with no redeploy (SC2/D-11)
    - The estimate still shows serviceability + cost + ETA range + COD (contract unchanged, SC4)
  </acceptance_criteria>
  <done>Owner types "approved" after confirming the four acceptance criteria; DLVR-03 verified end-to-end.</done>
  <resume-signal>Type "approved" to close the phase, or describe any issue (wrong value, stale estimate, validation gap) to reopen for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → delivery_rate_slabs write | The verification confirms the RLS-gated write path behaves; a non-admin path is not exercised here but is covered by the pre-existing policy (10-02 T-10-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege | slab write authorization | mitigate | Confirmed at the code layer in 10-02 (pre-existing `delivery_rate_slabs_admin_write` RLS, `private.is_admin()`). This checkpoint verifies the admin happy-path; the non-admin denial is guaranteed by the same policy that already gates every other admin write (site_content, products) — no new surface. |
| T-10-SC | Tampering | npm installs | accept | No installs — verification-only plan. |
</threat_model>

<verification>
- Human confirms all four acceptance criteria (SC1/SC2/SC3/SC4)
- No file changes; no deploy required (table + edge function already live)
</verification>

<success_criteria>
- Owner signs off that live rate edits reflect in customer estimates with no redeploy (SC2)
- Owner confirms validation blocks bad input and no ₹0/missing slab is possible (SC3)
- DLVR-03 verified end-to-end
</success_criteria>

<artifacts_this_phase_produces>
Verification-only — produces no new symbols. It exercises the artifacts shipped by
10-01 (`rateSlabsSchema`, mapping fns) and 10-02 (`useDeliveryRateSlabs`,
`useSaveRateSlabs`, `RateSlabs` page, `/admin/rates` route, `["deliverySlabs"]`
query key). See 10-02 for the full inventory.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-admin-zone-weight-rate-slab-editor/10-03-SUMMARY.md` when done.
</output>
